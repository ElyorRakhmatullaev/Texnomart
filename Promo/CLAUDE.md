# Texnomart Promo

Internal, role-based B2B workspace for planning and approving **planned & unplanned promo campaigns** in a promo calendar. Bootstrapped from `docs/promo_prompt_pack.md` (derived from the ТЗ v6.0 spec).

> For shared tech stack, design system, patterns, and conventions see the root [CLAUDE.md](../CLAUDE.md).
> For current state and known issues see [docs/AI_CONTEXT.md](../docs/AI_CONTEXT.md).
> For full screen specs see [docs/promo_prompt_pack.md](../docs/promo_prompt_pack.md) (Foundation + Master + S1–S8 + Appendices A–D).
> For lessons and gotchas see [tasks/lessons.md](../tasks/lessons.md).

## Status

**Bootstrap + Master shell + S1 complete; S2 complete (Phases 1–5); S3 complete (Phases 1–3); S4 complete (Phases 1–3); S5 complete; S6 complete; S7 complete; S8 complete — the Promo prompt pack (S1–S8) is fully built. Раунд фидбэка #3 — под-проекты #0+A (auth & accounts) и C (Профиль/Настройки) complete** — #0+A: стор пользователей, вход без 2FA с проверкой пароля, временный пароль + обязательная смена, экран управления учётками (`/users`), живой аудит-лог. C: персональные экраны `/profile` (Tabs «Основная информация» — редактируемое ФИО + read-only роль/статус/даты; «Безопасность» — добровольная смена пароля через переиспользованный `NewPasswordForm` с опциональным полем «Текущий пароль») и `/settings` (Язык display-only · Тема — `.dark`-переключатель · role-gated кросс-ссылки); новые `AuditActionType` «смена пароля»/«изменение профиля» пишутся живьём; shared `AppShell` не менялся (его дропдаун уже ссылался на `/profile` + `/settings`). Под-проект B (тёмная тема) complete — полноценная рабочая тёмная тема: `ThemeProvider` (`app/theme-context.tsx`, `"light"|"dark"|"system"`, persists `promo:pref-theme`, live `prefers-color-scheme` listener) + inline boot-script в `index.html` (применяет `.dark` до первой отрисовки — без FOUC) + theme-aware `<Toaster>`; переписан `.dark`-палитр в `theme.css` (слоистые тёмные поверхности, **бренд `#FFD60A` сохранён**, `--status-*` подстроены); shared `AppShell` получил **опциональный controlled `theme` prop** (header ↔ Settings синхронизированы через провайдер; Dashboard не затронут) + `dark:`-варианты на shared-паттернах; ~270 хардкод-цветов на 49 экранах мигрированы (hybrid: семантические токены + `dark:`-варианты; бренд-хекс → `primary`-токен). Под-проект D (матрица прав) complete — read-only **«Матрица прав»** at `/permissions` (nav-gated + in-screen guard к **КД + Администратор**): prompt-pack **Appendix D** как матрица 9×5 («документ») + новый канонический `lib/permissions.ts`, консолидирующий разрозненный role→permission-гейтинг в один источник истины («консолидация») — `ACCESS_MATRIX` (ячейки с формулировками спека, 4 уровня доступа) и `CAPABILITIES` (17 детальных действий, чьи `allowed(role)` **выводятся вживую** из существующих `getXAccess`/`canX`-хелперов — сами хелперы не тронуты, ноль регрессий S2–S8); два Pattern-J таба (Сводная матрица + легенда/§11.1 + `AccessMatrixTable` · Детальные права + `CapabilityList` с чипами ролей и code-pointers), активная роль брендово-подсвечена. Promo-local only (shared не менялся). **Все четыре под-проекта 3-го раунда (#0+A, B, C, D) готовы.** The app runs (`pnpm dev:promo`): auth flow, 7-module nav, the 9-role switcher, Promo primitives, seed mock data, the real **Краткий промо-календарь (S1)** screen, the **Полный промо-календарь (S2)** editable grid (Pattern F split-pane, 38-field dictionary, role gating, column-group chooser; inline editing of КМ fields with live validation, остаток ✏️ + warehouse-breakdown popover, installment auto-recalc, gift conditional fields, «В рекламу» checkboxes + bulk-select, functional draft/submit action bar; nomenclature entry via a searchable 1С Command picker (no free-text) + duplicate-check confirm dialog with a persistent «дубль» marker + line history + gift-nomenclature picker; Excel/CSV bulk import with per-row validation preview + the 1С-availability flow («Ожидает проверки 1С» banner + re-check + submit gate); unplanned-campaign creation («Создать акцию» / «встроить в плановую») + edit-until-first-send + campaigns lifted into page state + mobile full-screen per-line edit Sheet), and the **Согласование и проверка (S3)** approval workspace at `/approvals` (+ `/approvals/:id`): **Phase 1** — role-aware review queue (one item per Promo + КМ) with a working-days SLA timer (Pattern C table → Mode-B cards, тип/КМ/«только просроченные» filters); **Phase 2** — split-view detail (read-only submitted snapshot + per-line/bulk Принять·Отклонить, `ReasonDialog` comment) with status transitions (Старший КМ → КД auto-forward → «Принято КД»; rejecting any line returns the whole КМ set to «Не заполнено»), persisted line rejection feedback + a review-comment log; **Phase 3** — full **«Не участвует»** lifecycle (КМ «Мои участия» panel raises a request → Старший КМ → КД; КД can set it directly), **live auto-escalation** to КД after the 2-working-day breach («Авто-передано» tag), a non-blocking **просрочка** note, a per-campaign **advance gate** («Готовность кампании: N из M» — can't advance until every КМ has a final decision), review comments surfaced in the **`VersionHistoryDrawer`**, and a mobile fixed bottom action bar — all role-gated. **S4 (Управление изменениями) — Phase 1**: the `VersionHistoryDrawer` is fleshed out into a **three-view** drawer (**«Только изменения»** per-field diff · **«Полный актуальный отчёт»** live current snapshot · **«История версий»** version list + S3 review-comment/просрочка surfacing) with a **«Создать корректировку»** action (editor roles; rollback unsupported §5.2.1), opened from a per-campaign **«История»** button in the full-calendar grid band (all roles). **S4 Phase 2** adds **edit-after-approval** (spec §5.1/§11.8/§11.5): editing a КМ field on an approved campaign («Согласовано и отправлено смежным отделам») is diffed against a **baseline** → amber **changed-cell highlight** + a **«N изм. после согл.»** band badge + a **«Черновик — не отправлено»** section in the drawer's «Только изменения»; value/period changes derive **«Ожидает повторного согласования маркетинга»** (additions skip it); **Сотрудник маркетинга** «Согласовать (маркетинг)» → **КД** «Согласовать и отправить смежным отделам» appends a **live** new version + clears the draft (incremental send); **period change** (КД «Изменить период») renders the period **bold + ✏️**. **S4 Phase 3** adds the cancellation & deadline layer (spec §5.3/§4.7): **campaign cancellation** (КД only → required-reason `ReasonDialog` → separate `cancelled` state + «Отменена» + red/struck band + live «Отмена» version + "departments notified" toast), **line exclusion** (КМ requests via a frozen-pane Ban action / mobile Sheet with reason → `removalPending` «ожидает исключения» → КД confirms «исключена»/rejects; distinct from the S3 `rejected` flag; dropped from validation & counts), the **«Скрыть отменённое»** FilterBar `Switch` (**ON by default** — hides cancelled campaigns + removed lines), and **deadline change** (КД «Изменить дедлайн» with reason → `deadlineChange` pending band chip → **Операционный директор** «Утвердить дедлайн» → `fillDeadlineOverride` effective + logged in the `VersionHistoryDrawer`). **S5 (Отчёты для смежных подразделений)** at `/reports`: read-only, versioned reports for campaigns at «Согласовано и отправлено смежным отделам», with **three role-gated department views** (Маркетинг / Закуп / Аналитика — Tabs on sm+, Select on mobile) + a campaign picker; per-department Appendix-C **M/P/A field subsets** (Marketing widest incl. installment block + both «В рекламу»; Закуп/Аналитика the narrow компенсация set), a desktop group-header table → **Mode-B cards below md**; **versioning** («Получено …» + version + a **17-кал.-дн. overdue marker** + «История версий» drawer + «Только изменённые» toggle, amber changed-cell ring + emerald added rows); **ознакомление** («Ознакомиться со всеми»/per-line clears the highlight, explicitly **≠ согласование**, §11.7); and the **marketing-only** editable «В рекламу (выбрано маркетингом)» checkbox + bulk-select + «Согласовать выбор (маркетинг)» (Сотрудник маркетинга, §7.2) with a §11.8 re-approval reminder. **S6 (Центр уведомлений)** at `/notifications`: a notifications center + a live top-bar bell sharing one read/unread store via a `NotificationsProvider` mounted **above** the AppShell (so the bell, the sidebar «Уведомления» badge, and the page all update live on «Ознакомлен» — unlike the S3 below-shell badge); ~8 seed notifications across **6 types** (новые/изменённые данные · отмена акции · удаление позиции · повторное согласование маркетинга · назначение КМ · утверждение «В рекламу») with **role-based visibility** (§11.3.1), **date grouping** («Сегодня»/«Вчера»/дата), a **«Непрочитанные»** section over a muted **«Прочитано»** section, a **type filter**, **per-item + bulk «Ознакомлен»**, and quick links to the campaign/report; the shared AppShell bell «Показать все» navigates to `/notifications` (additive optional `notificationsHref`). **S7 (Настройки типов промо)** at `/promo-types` (+ `/promo-types/:ruleId`): a role-gated two-pane settings screen where **Коммерческий директор / Администратор** define which full-calendar fields are **required per promo type** as reusable rules. A `PromoTypesLayout` mounts a `PromoTypesProvider` (useReducer rules store) above both the list and the selected-rule child routes (S3 layout pattern); the left **RuleListPanel** (search, «Создать правило», per-row «Копировать», status badge + type names + field count) selects a rule into the right **RuleEditor** (status banner + confirmer/date, name, promo-type **chip multi-select** over the 7 `PROMO_TYPES`, a required-field **checklist grouped by the 5 Appendix-C categories** — derived from the single `gridFields` `COLUMNS`/`COLUMN_GROUPS` + the 3 frozen identity columns — with per-group select-all `indeterminate` checkbox, a **§9.3 effect-preview** note «Для типа X станут обязательными N полей», and the gated action row). Workflow `Черновик → На подтверждении → Утверждено` (+ `Архив`, no hard delete): «Отправить на подтверждение» (КД/Админ) → «Утвердить правило» (**КД only** — the §9 confirmation); **editing an approved rule's fields/types knocks it back to Черновик + logs «требуется повторного утверждения» (§9.5)**. A **§9.5 change-history** list per rule. Below lg the two-pane becomes a list ↔ editor stack driven by the URL (deep-linkable). **Scope note:** S7 is the settings screen + effect-preview *explanation* only — it does **not** rewire the full-calendar required-field validation (still the static `gridFields.required` flag); logged as an optional follow-up. **S8 (Аудит-лог)** at `/audit`: a read-only two-tab screen (spec §11.9). **Tab 1 «Аудит-лог»** — a dense, filterable action log (~21 reconstructed events) with monospace ids, tabular date+time, a tinted action chip (8 action types), an object cell (4 object types: акция/строка/отчёт/план), a **статус до→после** pair of `PromoStatusBadge`s, and a comment; filters (пользователь / роль / тип действия / объект / диапазон дат) sit in a sticky-header `Card` table that becomes Mode-B cards below md with the filters in a deferred **«Фильтры» Sheet**. **Tab 2 «Свод контрольных событий»** — a per-campaign milestone timeline (план → отправка данных КМ → согл./откл. старшим КМ → согл./откл. КД → «Не участвует» → отправка отчёта; +отмена terminal) rendered **horizontal on md+ / vertical on mobile**, with red **overdue nodes** (`OverdueTag` + responsible КМ) on the КМ-fill (start − 21 кал. дн.) and report (start − 17 кал. дн.) deadline breaches, a desktop notes legend, and a summary strip (campaign count · overdue-event count · avg approval time in **рабочие дни**). Read-only, all roles. The log + timeline are **reconstructed from the existing seeds** (campaign statuses, version chains, review comments, cancellation/non-participation/report seeds) and are seed-stale — in-session actions on other screens are not appended. **Краткий промо-календарь client feedback (2026-06-19) applied** (8 items): split Операционные/Контрольные/Распределение filters (+ period date-range, КМ-status, distribution filters that auto-expand the block); collapsible «Распределение по категориям» block (День/дата once-per-group + Категория + Ответственный КМ as structured sub-rows, variable row height synced across both Pattern-F panes); merged «Общий статус / готовность акции» column (status + «X из Y», excl. «Не участвует») replacing the separate готовность/статус columns, with per-КМ columns moved last; one bottom horizontal scrollbar + a page-sticky header (vertical scroll stays on the main page; no top scrollbar / no internal table scroll) + full-bleed width; lock icons removed; «План акций» per-campaign 3-director approval-stage table (Озн/Отпр + срок/просрочка/ожидает); filter-aware CSV export per tab. **Краткий-календарь client feedback V2 (2026-06-29) — COMPLETE (all 13 items).** Фаза A + §1/§13: global `KmStatus` taxonomy rework (§5 — «Согласовано КД»; split «Не заполнено» + «Переотправлено на корректировку КМ»; +«Отменена» auto on full cancellation; removed «Согласовано ст. КМ» — auto-flip to «На согл. у КД»; propagated through S3/S8/badges/export); `ReadinessCell` → **«Статус готовности акции»** per-cell collapsible **5-segment proportional bar** (green/orange/yellow/red/gray, tooltips, expanded labeled blocks; §2 drops the status badge, §3) with `aggregateKmStatuses` split into `atSeniorKm`+`atKd`; single **«Статус КМ по акции»** filter (6 statuses, promo+cell filtering, §5); shortened status labels (§6a); a **sticky TOP horizontal scrollbar synced with the bottom** (§1/§13). **Фазы B–D:** §6b `formatPromoNo` → **«26-N»** № промо (short-calendar table/detail/export; full calendar keeps `PR-`/`UN-`); §9 № промо filter → searchable **multi-select** `PromoNoFilter` (Popover + `Command` checkbox, `promoId`→`promoIds[]`); §11 **compactness** (`BASE_ROW_H` 104→80, header `h-12`→`h-10`, frozen `gap-3 px-4`→`gap-2.5 px-3`); §7 **collapsible filter block** («Фильтры» toggle + `countActiveFilters` badge + «Очистить» when collapsed); §10 **clickable КМ-status** badges → `kmStatusDeepLink` (review stages → `/approvals?promo=&km=`, final → `/full-calendar?promo=`; `ApprovalsPage` searches all items + banner, `FullCalendarPage` focuses the campaign + banner); §8 **sidebar collapse** verified (shared AppShell «Свернуть», no code change); §12 `getReportSendStatus` → **«Срок отчёта»** (start−17 кал. дн.) + **«Отправка смежным отделам»** («✓ Отправлено … · в.N» / «Не отправлено» + «отчёт просрочен») columns + mobile card + 2 CSV columns. Mock limits: §12 seed-stale (report-send from seeded version chains); §10 no per-person identity. **Post-V2 follow-ups (2026-06-29):** the §7 «Фильтры» toggle moved into the `PageHeader` beside the title (calendar tab only), reordered **«Фильтры → Экспорт»** (built-in export disabled, re-rendered in `actions`) + widened (`min-w-[140px]` + `justify-between`); the short-calendar seed grew **7→16 planned campaigns** (PR-2026-008…016, line-less by design — short-calendar-only enrichment) so the calendar shows **15 rows**. **Полный промо-календарь client feedback (2026-07-01) — COMPLETE (all 14 items).** Applied to `/full-calendar` in 4 phases (all builds green): **§1/§2** synced sticky TOP scrollbar + sticky header (short-calendar Pattern-F scroll; the scroll ancestor is the page's internal `overflow-auto`, so `top-0` — no `-top-4`); **§9** darker-gray bold header + `border-r` column dividers; **§12** unified alignment (text left / numbers·money·%·dates·checkboxes·badges centered, money not bold — `EditableCell` gained `align="center"`); **§4** Тип/Название/**Период** (two date columns merged → one `period`) render as columns, the band is simplified to «PR-2026-001 · N позиций» + actions, and the **дедлайн chip + the S4 «Изменить/Утвердить дедлайн» buttons are removed** from the full calendar (the deadline data model + the short-calendar «крайний срок» stay); **§13** filter-aware «Показано: N промо · M позиций» counter + a flat CSV **«Экспорт»** (`buildFullCalendarCsv` — one row per nomenclature, № промо repeated); **§3** per-line **«Изменить»** (opens the per-line Sheet, all viewports) + **«Удалить»** (hard delete, drafts only) in the always-visible frozen pane + a `delete` reducer + an «Изменения сохраняются автоматически» indicator (no «Сохранить черновик»), editability status-gated via `isCampaignFreshEditable` (fresh add/edit/delete · approved inline correction · under-review read-only); **§5/§6** «Бренд» + «Наличие в магазинах, %» columns before Остаток (`NomenclatureItem.brand`, `getStoreAvailability`/`formatAvailabilityPct` — active-stores directory, central warehouses incl. «Ташкент — Центральный» excluded, «94,87%») via `StoreAvailabilityCell`; **§7** a plain **КМ** sees only their own filled promos/lines (`ownKmId` = a representative КМ in the mock; Старший КМ/КД/Администратор keep the full view + the КМ filter); **§10** a searchable multi-select **«№ промо»** (`PromoNoFilter`, full PR-/UN- format) + a **«Период акции»** date-range added to the FilterBar (others unchanged); **§11** all 5 column groups ON by default; **§14** the create dialog defaults to **«Выбрать плановое промо»** (auto-pulls тип/название/период with a preview → integrate); **§8** gifts reworked — `PromoLine.giftNomenclatureId`/`giftStock` → `gifts: GiftItem[]`, a new promo type **«Подарок на выбор»** (`giftChoice`), fixed gift types render **«Подарок №1»/«Подарок №2»** columns (номенклатура + наличие % + остаток; the last two from 1С) and «Подарок на выбор» renders **one gift per sub-row** with the main nomenclature (and all non-gift columns) **merged** across them + a **«+ Добавить подарок»** button + per-gift remove (variable line height computed once, applied to both Pattern-F panes), rippled through `missingRequiredFields`, the S4 `TRACKED_FIELDS` diff, the S5 `reportFields` accessors, the S7 rule seed (`gift1Nomenclature`), `buildFullCalendarCsv`, and the mobile `LineEditSheet` (`GiftEditor`). Seeds: PR-2026-003 line gets a 2nd fixed gift; PR-2026-014 → «Подарок на выбор» (km-1, one line with 4 choice gifts). Promo-local only (shared untouched → Dashboard unaffected). Mock limits: §7 representative КМ (no per-person identity); store-availability + gift 1С numbers are deterministic mock; the flat export reflects only visible/filtered rows; in-browser visual QA pending (no browser tool in the build session). **Согласование client feedback (2026-07-02, 10 items) complete** — the **S3 «Согласование и проверка»** module reworked in 4 phases (all builds green + in-browser QA at 1440/390px): reviewer-queue filters «Срок согласования» (Все/Требуют решения сегодня/Только просроченные — replaces the overdue toggle, §1) · «№ промо» (`PromoNoFilter`) + «Период акции» date-range (§2) · «Статус согласования» stage Select (§3) · «Плановое/внеплановое» (§5), the «КМ» filter now a **multi-select** (`KmMultiSelect`) **persisted per user** (`promo:approvals-km-filter:<id>`, §4), and **Старший КМ + КД see BOTH review stages** (`visibleReviewQueue`) while acting stays gated by `effectiveReviewer`/`canAct` (§3); **stage-separated SLA** (`stageSlaStart`/`itemSla` + new `ReviewItem.kdStageStartedAt` — КД stage counts from the auto-forward moment / forward time, §9) shown as «N раб. дн. (до DD.MM.YYYY)» / «+N дн. просрочено» (§7); auto-escalated items **display «На согл. у КД»** (`displayKmStatus`) + «Авто-передано: просрочка у старшего КМ» (§8) with the senior-overdue note on the card + История (`VersionHistoryDrawer` `autoForward` prop, §9); the queue table (`ReviewQueueTable`) rewritten to the **calendar band layout** — page-sticky header + synced top/bottom scrollbars + unified dividers, **single pane (no frozen columns)**, natural row height (§6); the КМ «Мои участия» panel (`MyParticipationsPanel`) reworked — own-promos-only (representative КМ), card fields (№/тип/название/период/отправлено/статус) + «SLA КМ» (seeded `kmSubmissionSla`: В срок / +N раб. дн. просрочено / —) + 4 filters (№ промо/тип/период/статус) + the «Заявить о неучастии» → «Заявка о неучастии отправлена» → «Не участвует» transition (§10). Promo-local only (shared untouched → Dashboard unaffected). **5th-part feedback (Отчёты · Уведомления · Аудит-лог · Управление пользователями) — sub-project E-1 «Отчёты смежным отделам» complete (2026-07-06):** the S5 `/reports` module reworked per §1–§3 — report columns projected from full-calendar `gridFields`, band-layout table (frozen «Изменение»+«Номенклатура», sticky header, synced top/bottom scroll, gridlines), «Изменение» plashki (Добавлено/Изменено/Исключено) + before→after cell tooltip + version-total counters + «Только изменения», per-column typed «Фильтры» + «Сбросить фильтры» + «Показано: N», real `.xlsx` export (SheetJS), per-version read-only snapshots + «Текущая версия», per-user acknowledgement store + «Ознакомиться со всеми изменениями (N)», role-gated «Кто ознакомился» drawer; Promo-local only. **5th-part feedback E-2 «Уведомления» (live + persistent) complete (2026-07-06)** — the S6 `/notifications` module reworked from seed-stale/in-memory to **live + persistent**: a persistent `NotificationsProvider` exposing an imperative `notify(input)` (merges rebuilt seeds with a localStorage live-list [`promo:notifications-live`, cap 50, newest-first] + a per-user read-set [`promo:notifications-read:<userId>`, anon fallback], fires one SPA-safe sonner toast via `navigate`, **no toast on load**; `acknowledge`/`acknowledgeMany` persist reads); live emission wired into 5 existing handlers — `FullCalendarPage` (`onCancelConfirm`→campaign-cancelled, `onApproveRemoval`→line-removed, `onPeriodApply`→marketing-reapproval, `onSendToDepartments`→data-changed) + `DepartmentReportView` (`marketingApprove`→ad-approval), each replacing its own `toast.success`; new `lib/notifications-store.ts`; additive `promo-mock-data` exports (`NotificationInput`, `notificationAudienceFor`, `createLiveNotification`). Shared-shell bell count already updated live → **no `@texnomart/shared` change**, no bell animation. **Scope note:** no discrete client items were in the session docs, so the scope (**Live + persistent + toast**, deliberately lean UX) was proposed+approved in brainstorming; the original PDF E-2 asks were then built as **sub-project E-2b «Уведомления» per-role config, complete (2026-07-06)** — an Администратор-editable **per-role notification config** at `/notification-settings` (which of the 6 categories each of the 9 roles receives; new `lib/notification-settings-store.ts` [`promo:notification-role-config`, `DEFAULT_ROLE_CONFIG` = faithful inversion of the old audiences → no regression] + a `NotificationSettingsProvider` mounted ABOVE the AppShell wrapping `NotificationsProvider`; `notificationsForRole(role,list,config)` filters by it, Администратор bypasses [sees all]), a **«для роли X» tag** on each notification (`rolesForType`, active role brand-highlighted), and **«Открыть промо/согласование/отчёт» deep-links** (`notificationLinksFor` — the report link gated on `getSentCampaigns()`; `ReportsPage` `?promo=` pre-select). The «для роли X» tag shows the full configured target set — per-user multi-role filtering is deferred to E-4 (single-active-role mock). Promo-local only. **With E-2 + E-2b, «Уведомления» is fully covered. 5th-part feedback sub-project E-3 «Аудит-лог и контроль сроков» (2→4 tabs) complete (2026-07-07)** — the **S8 `/audit`** screen renamed «Аудит-лог и контроль сроков» and reworked from 2 tabs to **4** (focus: сроки/ответственные/просрочки, not campaign statuses): **Сроки по плану** + **Сроки по промо и отчётам** share a band-layout `ControlDeadlinesTable` (sticky header, synced top/bottom scroll, gridlines → Mode-B cards) + `ControlDeadlinesFilters` (период/№/ответственный/роль/точка/результат incl. «Только просроченные»); the old `ControlEventsTimeline` (horizontal timeline) **deleted**; **Показатели участников** — a role selector (КМ ranked / director roles aggregated) → место·ФИО·промо-с-дедлайном·вовремя·с-просрочкой·своевременность %+band (Высокая/Средняя/Низкая)·ср.просрочка·возвраты·повторные + a `ParticipantTasksDrawer` drill-down + a «Справочно» block (post-approval add/change/exclude counts out of the rating); **Аудит-лог** — the reworked `AuditLogTable` with a **«Ключевые действия» (default) / «Все действия» (Администратор-only)** toggle. A new pure `lib/audit-control.ts` derives a flat `ControlPoint[]` from existing seeds (`PLAN_APPROVALS` + a seeded PR-2026-006 correction chain; `buildReviewItems`/`seniorOverdueInfo`/`stageSlaStart` + report helpers — auto-forward attributes overdue to Старший КМ; `CATEGORY_MANAGERS`) so Tab 3 metrics **reconcile with the Tab 1–2 rows by construction**; page-level date-range/роль/reset on the deadline tabs only; access-gated (plain КМ → own rows on Tabs 2–4 via `OWN_AUDIT_KM_ID="km-3"` + a plan-tab notice; all other roles see all; «Все действия» Администратор-only); read-only + seed-stale; Promo-local only (shared/Dashboard untouched). New exports: `KM_FILL_SLA_CALENDAR_DAYS`, `OWN_AUDIT_KM_ID`; new files `lib/audit-control.ts` + `audit/{ControlDeadlinesTable,ControlDeadlinesFilters,PlanDeadlinesTab,PromoDeadlinesTab,ParticipantMetricsTab,ParticipantTasksDrawer}.tsx`. **5th-part feedback sub-project E-4 «Управление пользователями, ролями и временным замещением» complete (2026-07-07)** — `/users` reworked from the #0+A account screen into a full **user/role/substitution** module. **Data layer:** `users-store` extended additively (`PromoUser` gains optional `roles[]` [read via `rolesOf`], `department`/`position`/`managerId`, `adminScope` [«Администратор подразделения»], `kmId`; `DEPARTMENTS`; `updateUser`/`setUserRoles`/`setDeptAdmin`/`effectiveAdminScope`/`canManageUser`; `usableAdminCount`/`canRevokeAdmin` retargeted to `rolesOf` — every existing export unchanged, so `/profile`+auth+≥2-admin guard don't regress); new `kd-substitution-store.ts` (localStorage `promo:kd-substitution`: `getActiveSubstitution`/`assignSubstitution`/`revokeSubstitution` + pure `canActAsKd`/`isSubstituteConflicted`, seeded active substitution); `AuditEvent.targetUserId` + 3 new `AuditActionType` («изменение ролей»/«назначение замещения»/«снятие замещения»). **`/users` screen:** effective admin scope from the logged-in `useCurrentUser()` (global «Администратор» → all + create/grant/substitution; «Администратор подразделения» → own-department only, no create/global-grant/substitution + a notice), `UsersFilters` (search/подразделение/роль/статус), real `.xlsx` export (`users-xlsx.ts`, SheetJS), `UserFormDialog` (create+edit, multi-role chips, dept/position/manager — replaces `CreateUserDialog`), `KdSubstitutionPanel` host (assign/revoke/history), reworked `UsersTable` (Роли chips/Подразделение/Должность/Руководитель columns + «Открыть» + `canManage` scoping). **`/users/:id` detail page** (new, Pattern D): Профиль (InfoRows + «Редактировать») · Роли и доступ (role chips + admin scope + dept-admin grant + reset/block, admin-role chip locked for non-global admins + ≥2-admin guard on the edit path) · Журнал действий (`getLiveAuditEvents()`+`buildAuditLog()` filtered by `targetUserId`). **КД substitution → S3 (functional):** `ApprovalDetailPage` gains `substituteActing/conflicted/canAct/actingAsRole` — an active substitute (logged-in user) acts on КД-stage items (approve/reject stamped as «Коммерческий директор»), a same-person conflict-of-interest guard blocks acting on their own submission («Конфликт интересов» + no buttons), a «замещение до DD.MM» banner + «Замещение КД» chip; `ApprovalsPage` shows the substitute the КД queue (`showsQueue`). **`permissions.ts`** +4 capabilities (group «Пользователи и доступ»). Identity = seeds (u-6 = dept-admin «Маркетинг»; u-8 «Тошматов Фаррух» km-5 = seeded active substitute, conflicted on UN-2026-014/PR-2026-002, clear on PR-2026-001~km-3); god-mode switcher kept; create is global-admin-only. Executed via subagent-driven development (13 impl tasks / 5 phases, per-task review + a Task-8 Critical fix, whole-branch **opus** review, one fix wave, in-browser QA). **Opus review caught cross-task seam defects the per-task reviews missed:** a **Critical** — the `/users/:id` edit dialog bypassed the admin-grant + ≥2-admin gates the row-menu path enforced (privilege escalation + guard regression) — and an **Important** scope-precedence mismatch between `UsersPage` and `UserDetailPage`, plus a duplicate «Экспорт»; all fixed + re-reviewed → APPROVED. Both builds green; in-browser QA passed (dept-admin scope, substitute acting + conflict block, /users/:id, /permissions). Promo-local only (shared/Dashboard untouched). New files: `lib/kd-substitution-store.ts` · `lib/users-xlsx.ts` · `users/{UsersFilters,UserFormDialog,UserDetailPage,KdSubstitutionPanel}.tsx`; deleted `users/CreateUserDialog.tsx`; new route `/users/:id`. **Mock limits:** dept-admin/substitute experiences reached by logging in as the seeded users (default login is the global admin); create is global-admin-only; god-mode «Коммерческий директор» is treated as the real КД (so the conflict guard is a no-op in that god-mode, by design). **With E-4, all four 5th-part sub-projects (E-1 · E-2/E-2b · E-3 · E-4) are complete.** **Краткий промо-календарь «8-я часть» client feedback complete (2026-07-09, 5 items)** — applied «Тест первой MVP промо-календаря» to **S1**. **Context:** the feedback screenshots are from **`promo.tm.uz`** (a **backend-integrated** deploy — «Статусы/решения загружаются из сервера», raw role codes `marketing_director: approved`, «Пользователь #undefined», real data «Мазза нарх тест»/«Бонус тест»), **NOT this mock repo** (GitHub Pages; grep = zero hits) — the plan-approval UI there is a server rewrite absent here; the calendar grid IS this repo's `ShortCalendarTable`. Fixed **in the mock repo** (user-confirmed; backend mirrors). **№4** — `getReportSendStatus` returns `overdueDays > 0` **only on actual late send** (`getOverdueDays(deadline, sentAt)`; not-sent → 0); `ShortCalendarTable` «Срок отчёта» = crayний-срок date only (dropped the premature `<OverdueTag>`), «Отправка смежным отделам» = fact only («Не отправлено» / «Отправлено · дата · в.N» + «+N дн.» on the sent-late branch, «отчёт просрочен» removed); mobile card split into «Срок отчёта» + «Отправка смежным» lines; `promo-export` CSV label aligned. **№3 (+№1+№2)** — `PlanApprovalTable` gained a `selectable` mode (leading checkbox column + header «Выбрать все» + per-row «Согласовано/Отклонено» badge + row tint, backward-compatible — plain display when off); `PlanMode` models per-row decisions per reviewer stage (КД/ОД) with a selection strip «Согласовать выбранные (N)» / «Отклонить выбранные (N)» + progress «Согласовано N из M на этапе X» — approve one/several/all, advance (КД→ОД→Утверждён) only when **all** rows approved, reject selected → «Отклонён» (`ReasonDialog`) → «Вернуть на доработку» resets; since the same table shows № / тип / название / период, **№1 + №2 (full data at the decision point) are satisfied by it** (already largely present in the mock; the backend build had regressed them). **№5** — new pure `isPlanApprovedByDirectors(campaign)` (both `PLAN_APPROVALS` director stages decided, not «waiting»; cancelled = never) gates the calendar list for the **Категорийный менеджер (КМ)** role → sees only fully-approved plans (fixes «Найдено 0 акций»; other roles keep the full view). 6 files (`promo-mock-data.ts`, `ShortCalendarTable.tsx`, `ShortCalendarPage.tsx`, `PlanApprovalTable.tsx`, `PlanMode.tsx`, `promo-export.ts`), Promo-local (shared/Dashboard untouched); `build:promo` green + in-browser QA (КД/ОД/КМ, 1440/390px): №4 «Срок отчёта» 0 overdue tags + 1 sent-late «+N дн.» + «отчёт просрочен» gone; №3 partial 2/15 stays → select-all advances КД→ОД→«Утверждён» → reject→«Отклонён»→return, 15 mobile checkboxes; №5 КМ «Найдено 5 акций» (26-1/2/3/5/7), 26-6 (ОД `waiting`)/26-4 (cancelled)/26-8…16 (no approval record) hidden, others 15. **Mock limits:** per-row plan decisions are session-local (reload → «На согл. с КД»); КМ visibility keys off seeded `PLAN_APPROVALS` (line-less 008–016 hidden from КМ by design); the plan tab itself is not КМ-gated (only the calendar view, §5). **Краткий промо-календарь «6-я часть» client feedback complete (2026-07-10, 7 items)** — applied the «Комментарии общие» PDF (08.07.2026), the тракер's «🆕 новое» items **V2·14–18** (were ⛔ «ждёт корректировки ТЗ») + two new asks, to the **S1** «План акций» tab. Reworked `PlanMode` around a **per-row send lifecycle** `Черновик → выбрать → Отправлено → Согласовано/Отклонено` layered over the «8-я часть» reviewer decisions; the seed splits by `getPlanApproval` (campaigns with a `PLAN_APPROVALS` record start «Отправлено», the rest «Черновик»). **№1** — free-text «№ промо» → read-only auto number (`nextPlanPromoNo` → next `PR-2026-0NN`). **№2** — тип промо optional at creation → «Черновик» (ред./удал.); a red «Тип не выбран» cell marker (`typeMissing`) + «Черновиков без типа промо: N» banner; typeless drafts excluded from the send pool (тип required to send). **№3** — «Директор маркетинга» added to the `/promo-types` nav roles + a `getPromoTypeSettingsAccess` case (`canEdit:true`, `canConfirm:false` — utверждение stays КД's). **№4** — fixed the rejected-plan stepper (all stages green ✓ when rejected, because `actorForPlanStatus("Отклонён")=undefined` → `activeIndex=length`): `PlanMode` records `rejectedStage` → `PlanStepper` shows the rejecting stage **red ✗ «— отклонил»**, earlier green, later neutral; per-row «Отклонено» via `rowDecision`. **№5** — explicit «Отправить на согласование (N)» (drafts are no longer auto-sent at creation). **№6** — a reused create/edit dialog + `overrides`/`deletedIds` state make drafts editable/deletable; editing an already-**sent** row (`handleEdit`) reverts it to «Черновик» + clears its decision → re-send/re-approval (mirrors S4/S7). **№7** — a send-stage checkbox column (`rowCheckable` = sendable drafts) + «Отправлено» excludes already-sent from re-send; a new pure `findCoverageGaps(periods)` powers a **coverage-gap hint** (amber banner listing uncovered calendar-day ranges + a warning toast on send). `PlanApprovalTable` gained the «Статус строки» lifecycle column (`RowLifecycleBadge`), the `TypeCell` marker, send/review `selectable` modes, and a marketing «Действия» column (Изменить/Удалить). **4 files** (`PlanMode.tsx`, `PlanApprovalTable.tsx`, `promo-mock-data.ts` [+`nextPlanPromoNo`/`findCoverageGaps`/`CoverageGap` + the access case], `shell-config.tsx`), Promo-local; `build:promo` green + in-browser QA (Дир. маркетинга/КД, 1440/390px): №1 «PR-2026-017» read-only, №2 typeless draft «Тип не выбран» + banner + no checkbox, №5/№7 select-all→«Отправлено» + gap toast, №4 КД reject → red ✗ stepper + per-row «Отклонено», №6 edit sent → «Черновик» + pool 9→10, №3 «+ Создать правило» visible. **Mock limits:** gap rule = uncovered days within the promos' min→max span; the plan-level status (stepper) is shared while per-row send/decision state layers on top. **«Группа A» — сводный тракер комментариев complete (2026-07-17, subagent-driven, 11 commits e946822..b215803)** — closed all open tracker items: **T1/V1-1** dist sub-rows filtered by the active день/категория/КМ (`matchesDistFilter` in rowHeights + body render); **T2/V2-12** «Отправка смежным отделам» filter facet (Контрольные) + reference plashka (check ONLY on-time, red «Не отправлено»); **T3/V1-3.3** compact date cols 150/132/120 + 2-line clamp Тип/Название; **T4/V2-1** sticky-BOTTOM viewport scrollbar = 4th synced scroller (top kept then — removed by «7-я часть» §4; body native h-scrollbar hidden); **T5/V2-13** «План акций» sticky header (HEAD constant `sticky top-[-16px]`, wrapper overflow dropped → page h-scroll); **T6/V1-3.4** AppShell maxWidth full-width += exact `/reports`/`/approvals`/`/audit`; **T7/V1-8** per-tab filter-aware `.xlsx` export in 4 audit tabs + two-sheet «Матрица прав» export (new `lib/audit-xlsx.ts`, `lib/permissions-xlsx.ts`); **T8/V2-9** unified «26-N» display in 18 files (plan tab «Код акции», create-dialog display split `displayNo`/`displayNoLabel`, audit builders, breadcrumbs incl. composite `~km` split, CSV/xlsx; internal ids stay full — zero display→lookup leaks; 3 pre-existing raw-id PromoNoFilter option bugs fixed); **T11** strict role-gate «Срок отчёта»+«Отправка смежным» cols/filter/CSV/mobile (`canSeeReportSend`: КД/Сотрудник маркетинга/Администратор/active КД-substitute); **T12** plan lifecycle **persisted** to localStorage — new `lib/plan-store.ts` (`promo:plan-state`, 7 slices lazy-hydrated from one snapshot, single persist effect, Date↔ISO, seed fallback) → drafts/sends/decisions survive reload. Final whole-branch opus review READY (0 Critical/Important). Deferred: plan-store enum casts; substitution midnight rollover (reject-reason persistence — RESOLVED by «7-я часть» §9 below). **«7-я часть» client feedback complete (2026-07-22, 6 items)** — the client's review of the «Группа A» delivery («Комментарии общие по сделанной работе», 21.07.2026; screenshots from OUR GitHub Pages deploy, mapped 1:1 to this repo). Confirmed-done (no action): §1.1/§2/§5/§7/§8/§9.4. **§1.2** «Крайний срок заполнения КМ» = только дата — premature `OverdueTag` dropped from `ShortCalendarTable`, the mobile card and `ShortCalendarDetailPage` (просрочка lives only in «Отправка смежным отделам» by the fact of a late send). **§1.3** on-time-sent example: seed **PR-2026-007** → «Согласовано и отправлено смежным отделам» + a `CAMPAIGN_VERSIONS` chain (первичная отправка 30.05 < дедлайн отчёта 05.06) → the short calendar shows the green **«Отправлено ✓ · 30.05.2026 · в.1»** plashka; PR-2026-003 stays the late «+8 дн.» example; ripples: `/reports` gains a clean 26-7 v1 report, review queue 16→15, stale `PR-2026-007~km-5` seeds removed. **§3/§6.3** 2-line wrap instead of truncation — the webkit-line-clamp-2 idiom (`CLAMP2` consts) applied to the full calendar (Номенклатура/Тип/Название/Бренд/ФИО КМ/gift cells/headers), reports (`DepartmentReportView` — nomenclature, text-kind cells, headers) and approvals (Название/КМ); fixed Pattern-F row heights preserved (56px full-calendar rows / 52px report rows fit 2 × text-sm leading-tight). **§4** один нижний закреплённый скролл — the top scrollbar strips removed from ALL band tables (incl. the short calendar: the client explicitly overrode the July «keep top» decision) and the viewport-pinned `sticky bottom-0` track (body's native h-scrollbar hidden) added to `FullCalendarGrid`, `ReviewQueueTable`, `ReportBandTable` (ShortCalendarTable already had it); «План акций» + the audit tables already scroll via viewport-bounded containers (page `<main>` / `max-h` boxes) so their scrollbars are always visible. **§6.2** compact columns on the full-width screens — approvals `COLS` (№ 104/Тип 132/Отправлено 150/SLA 200), report `LOCAL_COLUMNS` widths (№ 104/даты 110/Признак 110/КМ 160), explicit short-column widths on the audit `<th>`s (remainder → «Комментарий»). **§9 — история причин отклонения плана (the deferred product decision, now built):** `plan-store.ts` gains the persisted **`rejectionLog`** slice (per-row `PlanRejectionEvent[]` {kind kd/od/return · by ФИО · role · at ISO · comment}, newest-first, backward-compatible hydration); `PlanMode.rejectSelected(reason)` **saves the ReasonDialog comment** (label = единое поле «Комментарий», §9.2) and `returnForRework` logs a return event; the per-row **«Отклонено» badge is a clickable button** (desktop + mobile) opening the new **`PlanRejectionDrawer`** (right Sheet: latest rejection details on top, «История отклонений» below, §9.1–9.3; graceful fallback for pre-journal rejections); `buildPlanCsv` += «Статус строки» + «Отклонил/Роль согласующего/Дата и время отклонения/Комментарий отклонения» and `ShortCalendarPage.effectivePlanRows()` makes the plan CSV reflect the persisted drafts/edits/deletions (§9.5); audit-log side untouched per §9.4. **Bonus fix:** `sendSelected` now re-opens «На согл. с КД» when the plan is at a marketing stage — before, re-sending after «Вернуть на доработку» left the plan stuck «На обсуждении» (the §9.3 reject→return→resend cycle was unreachable). In-browser QA (1440/390, Админ/КД/ДМ): full reject cycle + reload persistence + mobile drawer; only pre-existing ref warnings. Mock limits: journal per-browser localStorage; «кто отклонил» = logged-in user's ФИО (god-mode role = роль согласующего); pre-journal rejections show a fallback note. **«10-я часть» client feedback — Волны 1 + 2 complete (2026-07-31); Волны 3–6 pending.** Two «10я часть · Общее по дизайну» PDFs (29.07) + a corrected unified tracker xlsx (73 rows) mapped to the repo, split into 6 waves. **Волна 1 — quick display fixes + a diagnostic pass on the client's «кнопка не работает» reports (13 files, 2 commits):** R25 «в.N» dropped from the short calendar (kept in `/reports` + «История версий»); R54 «Авто-передано» plashka → `Forward` icon + «просрочка … на N раб. дн.» tooltip (the spec-ref «(§8)» read as «(58)»); R29.6 «Ожидает этапа» → «Ожидает согласования»; R29.7 plan rows sorted by период; R29.2 «Согласовать/Отклонить выбранные» pinned bottom-of-viewport; R56 «Мои участия» cards show the send date+time for «Согласовано КД»; R73 toasts `closeButton`+5s. **Diagnosis:** three «не работает» shared one root — off-screen Radix menus under the shared `<Button>` asChild (R29.3 plan «Экспорт», R69.2 `/users` «Сбросить пароль», login language menu) → native `<button>`+`buttonVariants`; R31.4 «созданная строка не видна» = DismissableLayer self-close (`setTimeout(0)`) + tombstone № reuse (number pool now includes `deletedIds`); R29.1/R44.5 not reproducible (not bugs). **Волна 2 — полный промо-календарь: построчная модель статусов (R46, Блоки 1–7) + подписи подарков (R44); spec → plan → 8 tasks inline, 11 commits `15be3bf..def1747`, both builds green, in-browser QA + `/code-review` high (1 regression fixed).** New **`lib/full-calendar-status.ts`** — `LineStatus` (10 values) + `lineDisplayStatus` (per-line status: removed → exclusion-pending → rejected-repeat → pending-repeat → cancelled → campaign.status/line.rejected) + a single **«Все статусы»** filter applied **per line** (+ synthetic «На согласовании (общее)») + `isRepeatActionPending`/`lineHasRejection`/`mergePendingChange`. **`PromoLine.pending` (`LinePendingChange`)** — the table keeps showing the last **approved** data, new values live only in `pending` + the panel until re-approval (Блок 2; seeded L-0015 change / L-0022 addition / L-0023 rejected). **Grid:** light-orange row **only** for repeat actions awaiting approval (Блок 1.3/1.4), «Черновик» chip (Блок 3.1), cancelled/removed → gray+strikethrough+«Отменено/Удалено» (Блок 5.5), a per-row **eye icon** «просмотр деталей» + КМ red rejection dot (Блок 6); **removed** the amber changed-cell ring, the band «N изм. после согл.»/«Ожидает маркетинга» badge (`ChangeBadge` deleted), and the status plashки from `LineMarkers` (Блок 1.2/13). **Panel:** new read-only **`LineDetailsDrawer`** «Детали изменений» (Информация об акции → Изменение Поле/Было/Стало → Детали запроса → Отклонение). **Page:** the per-campaign «Статус» filter → the per-line «Все статусы» filter (`displayLinesFor` + a campaign shows if ≥1 line matches); `onEdit` on an approved campaign routes a **КМ data-field** edit into `line.pending` (reducer `setPending`/`clearPending`); hosts the drawer + a per-user **`lib/full-calendar-rejection-store.ts`** (localStorage `promo:fc-rejection-seen:<user>`). R44 — «Подарок (1)/(2)» + «Подарок на выбор» caption + a band mechanic chip. **Code-review fix:** `onEdit` initially routed EVERY approved-campaign edit into pending → the «В рекламу (выбрано маркетингом)»/«рекомендация КМ» checkboxes (§7.2) stopped applying → restricted to genuine `TRACKED_FIELD_LABEL` data-field edits. **Mock limits:** «кто отправил/отклонил» = role label; rejection-seen per-browser; the S4 Phase-2 live send/version-drawer diff is now dormant (edits go to `pending` — the re-approval workflow is Волна 3 / R57); Блок 4.4 reports concern = Волна 5. Spec `docs/superpowers/specs/2026-07-31-promo-full-calendar-status-model-gifts-design.md`; plan `docs/superpowers/plans/2026-07-31-promo-full-calendar-status-model-gifts.md`. Promo-local; запушено (`cd70df6`). **«10-я часть» Волна 3 — карточка согласования (R57, 16 пунктов) complete (2026-08-03)** — раздел «Дополнение к комментариям. Согласование. Карточка согласования» применён к **S3 `/approvals/:id`**; волна замыкает контур Волны 2 (повторные действия `line.pending` было негде решать). **Данные:** `ReviewKind += "repeat"` — `buildReviewItems` порождает заявку на (согласованную акцию × КМ) со строками, ждущими решения по повторному действию (группировка по `line.kmId`), этап **КД** (промо уже отправлено смежным отделам ⇒ решение одностадийное и сразу применяет значения); `lineNeedsRepeatDecision`/`repeatActionAt`/`receivedAt`, `PromoLine.removalRequestedAt`, `LinePendingChange.fields[].value`, сид L-0024 (запрос на исключение). **Решения (§16):** `lib/line-decision-store.ts` (localStorage `promo:line-decisions`) + чистый `applyLineDecisions` (согласовано → значения актуальны и подсветка снята · отклонено → `pending.rejected` + возврат КМ · отклонённое исключение оставляет КМ «Было/Стало» + причину, Волна 2 Блок 6.2/6.6); полный календарь сеет строки через тот же хелпер ⇒ решение видно на обоих экранах и переживает перезагрузку. **UI:** `lib/approval-card.ts` (классификация строк + счётчики), переработанный `SubmittedLinesPanel` (весь список номенклатур акции §4 · «Согласовано ранее» + замок §5 · светло-оранжевые повторные §6 · клик по строке §7 · «Все строки / Только изменения» §11 · счётчик §12 · без колонки статуса §13), новый `LineChangeDrawer` (§7–§10, §15), шапка «Получено на согласование» + «Авто-передано КД» (§1–§3), решения только по строкам с решением (§14). Первичный поток S3 не изменился. **«10-я часть» Волна 4 — «План акций» (R29.5 / R30.1 / R30.2) complete (2026-08-04, 9 из 9 задач, коммиты `ed9cb06..ee0aadc`).** Закрывает последний крупный незакрытый блок по плану акций. **Диагноз:** колонки КД/ОД рендерились только из сида `getPlanApproval`, живые решения в них не попадали (R29.5); дата отправки строки нигде не хранилась (`sendStatus` — лишь `draft|sent`), поэтому «дату повторной отправки» и старт SLA этапа взять было неоткуда (R30.1); согласованную строку нельзя было удалить вовсе (R30.2). **Данные:** `plan-store.ts` += `PlanStageDecision`/`PlanApprovalCycle`/`PlanRemovalRequest`/`PlanRowJournal` + слайс `rowJournal` (защитный разбор, старый снапшот → `{}`); `rejectionLog` → **legacy read-only** (новые отклонения только в журнале — двойной записи нет). **Новый `lib/plan-approval.ts`** — единственная деривация состояния строки для таблицы, панели и экспорта, сид как fallback ВНУТРИ неё: чтение (`currentCycle`/`openCycle`, `marketingStageCell`/`directorStageCell`, `planRowLifecycle`, `approvedStages`/`removalNeedsApproval`/`pendingRemovalStageFor`/`removalFullyApproved`/`removalRejected`, `latestJournalRejection`) + запись (`withSend`/`ensureOpenCycle`/`withDecision`/`withCycleClosed`/`withRemovalRequest`/`withRemovalDecision`/`withRemovalArchived`, все чистые и иммутабельные). Сроки: маркетинг — КАЛЕНДАРНЫЕ дни от `startDate − PLAN_MARKETING_SUBMIT_LEAD_DAYS`, КД/ОД — РАБОЧИЕ по `+PLAN_DIRECTOR_SLA_WORKING_DAYS`; старт этапа ОД = согласование КД; пять исходов, включая «ожидает, но срок уже прошёл». Сид-строка при первом живом решении материализуется как цикл №1 с датой отправки из `PLAN_APPROVALS` — SLA считается от реальной отправки, а не от клика. **UI:** `PlanApprovalTable` рендерит этапы из деривации + подписи единиц («кал. дн.»/«раб. дн.») + метка «Цикл N» + статус **«Удаление на согласовании»** + кнопка «История» для всех ролей; новый **`PlanRowHistoryDrawer`** заменил `PlanRejectionDrawer`; `PlanMode` стемпит все пять переходов (отправка/согласование/отклонение/возврат/правка) и пишет живые события в аудит **существующими типами действий** (объект «план»). **R30.2:** «Удалить» на согласованной строке → запрос с обязательной причиной, строка остаётся в плане, решают согласовавшие её этапы **из боковой панели, пер-строчно и вне цепочки `planStatus`** (при «Утверждён» у цепочки нет актора вообще); полное согласование убирает строку и пишет два события, отклонение возвращает её в план; строки с активным запросом исключены из выбора и из проверки продвижения плана (`gatingRows`). **QA настоящими кликами пройден** по всем трём требованиям, включая перезагрузку, 390px и тёмную тему. **Побочно починен дефект вёрстки:** `<Card className="overflow-clip">` отрезал переполнение таблицы (по замыслу «Группы A» V2-13 оно должно уходить в горизонтальный скролл страницы) — на 1440px «Удалить» была физически некликабельна; клип перенесён с карточки на саму закреплённую полосу (+`rounded-b-xl`), sticky-шапка и «один нижний скролл» сохранены. **T8:** `buildPlanCsv` читает этапы через ту же деривацию, что таблица и панель (сид — fallback внутри неё) + колонка «Цикл согласования» + статус «Удаление на согласовании» + отклонение из журнала (legacy `rejectionLog` — только при отсутствии журнала) + единицы срока в подписях; проверено на выгруженном файле. **T9 — расхождение сида:** даты `PLAN_APPROVALS` писались «на глаз» и нарушали собственное правило (6 расхождений в 4 записях), из-за чего «План акций» расходился с `/audit` → «Сроки по плану», а после Волны 4 строка меняла «В срок» на «Просрочка» просто потому, что по ней появился живой цикл и деривация пересчитывала статус. Даты пересчитаны (отправка — 60 кал. дн. до старта, решения директоров — 3 раб. дн. от старта этапа) с сохранением намерения каждой записи; **результат** совпадает на обоих экранах, различие остаётся только в единицах **величины** (план печатает раб. дни, `/audit` — кал.) — выравнивание отнесено к Волне 5. **Осталось:** `/code-review` (запускает пользователь). **«10-я часть» Волна 5 — 5A «Отчёты» + 5B «Уведомления» complete (2026-08-05, `457d210` + `4fbe1e2`); 5C «Аудит» и 5D «Пользователи» впереди.** Волна разбита на четыре блока (~40 подпунктов трекера); каждый начинается со сверки комментария с кодом, потому что значительная часть замечаний писалась до E-1…E-4 и уже реализована. **5A `/reports`:** воронка фильтра в заголовке КАЖДОЙ колонки (`ReportHeaderFilter` + `ReportChangeHeaderFilter` + `ReportAckHeaderFilter` в `ReportFilters.tsx`; общий `ColumnFilterBody` для панели и поповера, `applyReportFilters` не тронута, панель осталась входом для мобильных); выравнивание по образцу полного календаря — `cellJustify(kind)` (текст влево, числа/суммы/проценты/даты/чекбоксы по центру) применяется и к шапке, и к ячейкам; `REPORT_CHANGE_SETS` посеян для **PR-2026-003** — акции, открывающейся по умолчанию (её v3 «изменена цена по 2 позициям» существовала, а колонка «Изменение» показывала «—»; помечено только «Изменено», т.к. добавление L-0022 и запрос на исключение L-0024 не согласованы — Блок 4.4); «Показано: N позиций» получило склонение через `pluralRu`. **5B `/notifications`:** `NotificationType` 6 → **15** (+`report-new`, `review-new`, `review-returned`, `review-resubmitted`, `kd-approved`, `non-participation`, `auto-forwarded`, `sla-overdue`, `deadline-today`) с иконками/тинтами, `REVIEW_AUDIENCE`/`REVIEWERS_AUDIENCE`, ветками в `notificationAudienceFor` и `notificationLinksFor`, 9 новых сидов; `DEFAULT_ROLE_CONFIG` пересобран из наборов OPS/REPORTS/REVIEW (КМ не получает «поступило на согласование» и «повторно отправлено» — их инициирует сам КМ); живая эмиссия в `ApprovalsProvider` (approve/reject/оба «Не участвует») — в экшен-креаторах, редьюсер чистый; `reportChangeSummary(campaignId)` даёт краткое описание изменений для уведомления о новой версии отчёта; `NotificationsProvider` дедуплицирует по «тип + акция + версия + текст»; карточка показывает № промо + название, «Вам как: <роль>» (Администратору — целевые роли) и «Отметить прочитанным»; `NotificationsPage` для Администратора рисует блоки по ролям + переключатель «По ролям · Единым списком». **Ограничения мока:** «срок истекает сегодня» и «просрочка SLA» сидовые (планировщика нет); уведомления смежным отделам не делятся по подразделению получателя — отчёт формируется сразу для всех трёх. Спека `docs/superpowers/specs/2026-08-04-promo-plan-approval-cycles-removal-design.md`, план `docs/superpowers/plans/2026-08-04-promo-plan-approval-cycles-removal.md`. **Блок 5C «Аудит-лог и контроль сроков» complete (2026-08-05, `48c0d4f..ece9e24`, 9 задач; спека/план `docs/superpowers/{specs,plans}/2026-08-05-promo-audit-control-5c*`).** Gap-анализ 33 подпунктов трекера (строки 61–65): 12 уже сделано в E-3, 20 реальных пробелов. **Новый чистый `lib/audit-access.ts`** — `AuditScope` (`objectTypes` + `kmNames` + `campaignIds` + `label`), `auditScopeFor(role, userId)` (активное уполномоченное лицо КД получает скоуп КД), `checkpointObjectType`, `scopeControlPoints`, `scopeAuditEvents`; скоуп кладётся в `AuditAccess.scope` одним `useMemo` и применяется на ВСЕХ четырёх вкладках **до** пользовательских фильтров, поэтому списки значений в фильтрах строятся из суженного набора и обойти скоуп фильтром нельзя. Пустая вкладка показывает пояснение со `scope.label` вместо «Нет записей». **Вкладки сроков:** `ControlPoint.planPeriod` `string → {start,end,label}` (колонка печатает «01.11.2026 — 30.11.2026», месяц — серая подпись-группировка) + фильтр «Период плана» (рисуется только там, где у точек есть `planPeriod`); подпись **«Период дедлайна»** у пары дат в шапке; `ControlFilters.promo → promoIds` + `PromoNoFilter` (подписи «26-N · Название», значение — полный id); новый фильтр **«Период акции»** (пересечение периодов, только вкладка 2). **Деривация `audit-control.ts`:** `resolve()` возвращает «Просрочено» при пройденном дедлайне без факта; комментарий автопередачи по формулировке клиента + `SENIOR_KM_NAME` из `CATEGORY_MANAGERS` с синхронной правкой `participantsFor` (иначе повторился бы дефект E-3 с пустой строкой старшего КМ); **`ControlPoint.unit` (`cal`/`work`) + `overdueLabel`** — единица живёт в данных (закрыт долг Волны 4); новая точка **«Повторная отправка после корректировки»** через `ReviewItem.returnedAt` + сид-поле `returnedDays` (повторно отправленный набор перезаписывает `submittedAt`, поэтому отдельное поле «переотправлено» не нужно; посеяны просроченный и своевременный случаи). **Показатели участников:** `ParticipantFilters` (период дедлайна / № промо / этап / участник) применяются внутри `roleControlPoints`, поэтому таблица и панель не расходятся; `participantFilterOptions` строит варианты из НЕотфильтрованного набора; `timelinessPct`/`band` стали nullable → «Нет данных» вместо «0% · Низкая», такие строки внизу сортировки; `MetricKey`+`matchesMetric`+`METRIC_LABEL` дают drill-down по числовым ячейкам и заголовок «Задачи: ФИО — Показатель: N»; ростер сужается `scope.kmNames`. **Лог:** `AuditActionType` +4 («автопередача по SLA», «повторная отправка», «новая версия отчёта», «изменение дедлайна») с метой и сидами, даты согласованы с источниками вкладок сроков; `NON_KEY_ACTIONS` += технические и административные действия над учётками. **Нижний скролл:** замечание подтверждено замером (переполнение 1351>1120, полоса на y≈1125 при окне 900) → синхронизированная `sticky bottom-0` дорожка + карточка переведена с `overflow-hidden` на `overflow-clip`. **Ограничение вне блока:** `formatPromoNo` даёт одинаковую подпись «26-14»/«26-15» для пар `PR-`/`UN-`. **Далее:** 5D «Пользователи, роли, замещение», затем Волна 6 (распределение по КМ R74).

## Commands

```
pnpm dev:promo      # start dev server (Vite)
pnpm build:promo    # production build
```

> pnpm is invoked via `corepack pnpm …` in this environment (pnpm is not on PATH; corepack ships with Node).

## Project Structure

```
Promo/
  index.html
  vite.config.ts                    # aliases: @ → ./src, @texnomart/ui, @texnomart/shared
  src/
    main.tsx                        # createRoot + styles
    app/
      App.tsx                       # ThemeProvider → AuthProvider → RoleProvider → RouterProvider + theme-aware Toaster
      theme-context.tsx             # B: ThemeProvider / useTheme — "light"|"dark"|"system", persists `promo:pref-theme`, applies `.dark`, live OS prefers-color-scheme listener (paired with the no-FOUC boot script in index.html)
      routes.tsx                    # guest /login + protected layout (ProtectedLayout wraps AppShell in NotificationSettingsProvider › NotificationsProvider — E-2b config provider is OUTERMOST); all S1–S8 routes are real screens + `/notification-settings` (E-2b, Админ)
      role-context.tsx              # RoleProvider / useRole — 9 roles, sessionStorage-persisted
      current-user-context.tsx      # CurrentUserProvider / useCurrentUser — sessionStorage `promo:current-user-id`; шапка отображает имя + инициалы вошедшего пользователя (а не хардкод)
      shell-config.tsx              # createPromoShellConfig(role, unreadNotifications): nav, breadcrumbs, logos, badges (live «Согласование» + «Уведомления» counts)
      components/
        AppShell.tsx                # thin wrapper over @texnomart/shared AppShell + role switcher + command search; consumes useNotifications() → live bell items + unread count (**E-2b**: filtered by `useNotificationSettings()` config via `notificationsForRole(currentRole, notifications, notificationConfig)` — aliased to avoid the shell-`config` collision), passes notificationsHref
        ModulePlaceholder.tsx       # titled placeholder + demo FilterBar + in-progress empty state — now UNUSED (every route is a real screen); kept for future scaffolding
        short-calendar/             # S1 — Краткий промо-календарь (+ 2026-06-19 feedback, 8 items; + 2026-06-29 feedback V2 — COMPLETE, 13 items; + «8-я часть» 2026-07-09, 5 items)
          ShortCalendarPage.tsx     #   PageHeader + mode Tabs + CalendarFilters (gated by `filtersOpen`, below the tabs) + table / mobile list. **Header actions (§7, calendar tab only):** the «Фильтры» toggle (`countActiveFilters` badge + chevron, widened `min-w-[140px]` + `justify-between` + collapsed-state «Очистить») sits BEFORE a self-rendered filter-aware CSV «Экспорт» `DropdownMenu` (the built-in `PageHeader` export is disabled via `showExport={false}` so the order reads «Фильтры → Экспорт»), then «Создать план» (Дир. маркетинга). Period date-range + multi-select № промо (`promoIds`) filtering, effectiveExpanded (auto-expand on a distribution filter); §10 `onKmStatusClick` → `kmStatusDeepLink` nav; mobile card shows «№ 26-N» (§6b) + split «Срок отчёта» + «Отправка смежным» lines (№4). **«8-я часть» №5:** `isKm = currentRole === "Категорийный менеджер (КМ)"` gates `filtered` (in the memo, deps incl. `isKm`) to `isPlanApprovedByDirectors(c)` — the КМ sees only fully-approved plans (fixes «Найдено 0 акций»); other roles unchanged
          PromoNoFilter.tsx         #   §9: searchable **multi-select** «№ промо» — Popover + `Command` checkbox list (search by «26-N» or name), native-`<button>`+`buttonVariants` trigger; label «Все номера» / «26-N» / «Выбрано: N» + «Очистить выбор»
          ShortCalendarTable.tsx    #   Pattern F: frozen pane + horizontally-scrolling pane; **compact density (§11: `BASE_ROW_H` 80, header `h-10`, frozen `gap-2.5 px-3`)**; header band sticky to the PAGE scroll (`overflow-clip` Card rounds corners without trapping sticky; `-top-4` cancels `<main>`'s padding; §13); **THREE synced scrollers (7-я часть §4 — the former top strip is REMOVED): header + body pane + the single `sticky bottom-0` viewport strip (Card's last child; body's native h-scrollbar hidden)** kept in sync via idempotent `scrollLeft` writes (spacer = frozen-pane width, track = scroll content width via ResizeObserver); **Group A T1/V1-1: dist sub-rows filtered by the active день/категория/КМ (`matchesDistFilter` in rowHeights + body render)**; **Group A T3: date cols 150/132/120 + 2-line clamp Тип/Название**; **Group A T11: «Срок отчёта»+«Отправка смежным» columns render only when `showReportColumns`**; № промо shown as **«26-N»** (`formatPromoNo`, §6b); column order период → крайний срок КМ → **«Срок отчёта» (№4: crayний-срок date ONLY — no overdue tag)** → collapsible «Распределение по категориям» → ReadinessCell («Статус готовности акции», w-340) → **«Отправка смежным отделам» (№4: fact only — «Не отправлено» / «✓ Отправлено · дата · в.N» + `<OverdueTag>` «+N дн.» ТОЛЬКО on the sent-late branch; «отчёт просрочен» removed)** → статусы по КМ (last; §5 filter; **§10: each badge is a `<button>` → `kmStatusDeepLink`, stopPropagation vs the row's detail nav**); per-cell readiness expand grows that row to 148px in BOTH panes; the scrolling-pane row is a `<div role="button">`; variable row height synced across both panes
          ReadinessCell.tsx         #   «Статус готовности акции» (§2/§3, 2026-06-29): per-cell **collapsible block** (collapsed by default, chevron toggle) — NO status badge above the bar (§2); summary «N из M КМ согласовано» (M excl. «Не участвует»/«Отменена»); a continuous **5-segment bar** (green Согласовано КД · orange На согл. у КД · yellow На согл. у ст. КМ · red На корр./Не заполнено · gray Не участвует) with **proportional widths** (`flex-grow: count` over a min-width floor: 0 = min, 1 > 0, 3 > 2 > 1); tooltips per segment; collapsed = markers + counts (no names), expanded = a labeled block (circle + name + count) strictly within each segment
          CalendarFilters.tsx       #   grouped filters: Операционные (**№ промо searchable multi-select via PromoNoFilter, §9** · тип · КМ · **«Статус КМ по акции»** — the single consolidated status filter, §5: 6 statuses, default «Все статусы») / Контрольные (период date-range · **«Отправка смежным отделам» Все/Отправлено/Не отправлено — Group A T2/V2-12, render-gated by `showReportSend` (T11)**) / Распределение (день недели · категория · ответственный КМ) + «Скрыть отменённое» switch + dist-expand toggle + «Очистить»; exports `countActiveFilters` (§7) — `promoId`→`promoIds[]`
          AggregatedIndicators.tsx  #   §4.6 four count-chips cluster (still used by the mobile card)
          PlanMode.tsx              #   «План акций» stepper + role-aware approval + create-row dialog; rows rendered via PlanApprovalTable. **«8-я часть» №3:** per-row decisions per reviewer stage (`DecisionMap` keyed `kd`/`od`) — `selectable = canAct && reviewerStage`; a selection strip «Согласовать выбранные (N)» / «Отклонить выбранные (N)» (`approveSelected`/`rejectSelected`) + progress «Согласовано N из M на этапе КД/ОД»; advance (КД→ОД→«Утверждён») only when ALL rows approved; reject selected → «Отклонён» (`ReasonDialog`) → «Вернуть на доработку» (`resetReview`); Дир. маркетинга «Отправить на согласование» stays atomic. **Group A T12:** the lifecycle slices (now **8** — «7-я часть» §9 added `rejectionLog`) hydrate lazily from `lib/plan-store.ts` (`promo:plan-state`) + one write-through effect → the plan survives reload; **T8:** create-dialog «№ промо» display split (`displayNo` full id → onSubmit; `displayNoLabel` «26-N» → the read-only Input). **«7-я часть» §9:** `rejectSelected(reason)` persists a `PlanRejectionEvent` per rejected row (kind kd/od · ФИО from `useCurrentUser` · роль · ISO · комментарий — the ReasonDialog label is «Комментарий», §9.2) and `returnForRework` logs a `return` event; hosts `PlanRowHistoryDrawer` (`historyRowId` state, opened via the table's `onShowHistory` — Волна 4 переименовала прежние `rejectionRowId`/`onShowRejection`); **fix:** `sendSelected` re-opens «На согл. с КД» whenever the plan is at a marketing stage (was: only terminal states — a re-send after «Вернуть на доработку» left the plan stuck «На обсуждении»). **Волна 4:** слайс `rowJournal` + аксессор `journalOf(id)` + хелпер `logPlan(action, row, comment?, statuses?)` (живой аудит существующими типами действий, объект «план»); все пять переходов стемпятся в журнал (`withSend` при отправке, `withDecision` при согласовании/отклонении, `withCycleClosed` при возврате на доработку и при правке отправленной строки); `rejectionLog` больше НЕ пишется (`const [rejectionLog] = useState(...)` — только чтение для legacy-панели); **R30.2** — `handleDelete` разветвлён (`removalNeedsApproval` → отложенный `ReasonDialog` запроса, иначе прежнее жёсткое удаление), `requestRemoval`/`decideRemoval` (решение считается СИНХРОННО по локально построенному объекту, а не по состоянию после `setRowJournal`), `gatingRows` (строки с активным запросом исключены из `approvedCount`, из строки прогресса и из проверки «все согласованы»), `selectablePool` их не отдаёт; оба новых диалога открываются через `setTimeout(…, 0)`
          PlanApprovalTable.tsx     #   §5 per-campaign 3-director approval stages (Озн/Отпр + «В срок»/«Просрочка +N дн.»/«Ожидает этапа», SLA notes in sub-headers) — shows № / тип / название / период (covers «8-я часть» №1+№2: full data at the decision point); **№3 `selectable` mode** — leading checkbox column + header «Выбрать все» (`allSelected`/`someSelected` → indeterminate) + per-row `DecisionBadge` «Согласовано/Отклонено» (decided rows drop the checkbox) + row tint; backward-compatible (plain display when off); table on md+ / stacked cards on mobile. **Group A T5/V2-13:** page-sticky header — `sticky top-[-16px] z-20` + solid bg on every `<th>` via the shared `HEAD` constant, desktop wrapper's `overflow-x-auto` dropped (wide table overflows into the page's own h-scroll — `<main>` is viewport-bounded, so its bottom scrollbar is always visible, «7-я часть» §4); **T8:** «Код акции» displays «26-N». **«7-я часть» §9:** **Волна 4:** проп `onShowRejection` заменён на `onShowHistory(id)` + добавлен `journalFor(id)`; ячейки этапов рендерятся из `StageCellData` (деривация `marketingStageCell`/`directorStageCell`), `StageStatusBadge` получил `unit` и печатает «кал. дн.»/«раб. дн.» + умеет «Ожидает согласования · просрочка +N»; `CycleTag` («Цикл N», только в маркетинговой колонке, чтобы не троиться); `RowLifecycleBadge` += приоритетная ветка **«Удаление на согласовании»** (оранжевая, кликабельная); колонка «Действия» стала безусловной (ширина 240px, `min-w` таблицы 1320px) и содержит «История» для всех ролей + «Изменить»/«Удалить» для владельца плана, скрытые при активном запросе на удаление. Прежнее: `onShowRejection(id)` — the «Отклонено» pill in `RowLifecycleBadge` becomes a `<button>` (desktop + mobile, dotted underline + hover) opening the rejection-details panel
          PlanRowHistoryDrawer.tsx  #   **Волна 4 new** (заменил `PlanRejectionDrawer.tsx`, «7-я часть» §9) — правая панель истории строки плана: **Запрос на удаление** (кто/когда/причина + список требуемых этапов со статусом + кнопки «Согласовать/Отклонить удаление», которые рендерятся ТОЛЬКО при переданном `removalStage`, т.е. когда текущая роль реально может решать) → **Текущий цикл** («Цикл N · отправлено …», решения КД/ОД с датой) → **История согласования** (прежние циклы, новыми сверху, с пометкой о закрытии возвратом/правкой) → **Завершённые запросы на удаление**. Если журнала по строке нет — рендерит прежний legacy-вид из `rejectionLog` (`LegacyRejections`), без слияния и дедупликации
          ShortCalendarDetailPage.tsx #  Pattern D full page /short-calendar/:promoId («Крайний срок КМ» = date only, 7-я часть §1.2)
        full-calendar/              # S2 — Полный промо-календарь (complete; Phases 1–5) + S4 change-management (Phases 1–3) + client feedback 2026-07-01 (14 items) + **«10-я часть» Волна 2 (R46 построчная модель статусов + R44 подарки)**: single **«Все статусы»** per-line filter (11 values + «На согласовании (общее)»); light-orange row ONLY for repeat actions awaiting approval; «Черновик»/отменённые (серый+зачёркнуто) states; **iconка-глаз** → `LineDetailsDrawer` «Детали изменений»; красный индикатор КМ; убраны янтарная обводка/бейдж «N изм.»/статусные плашки (Блок 1.2/13); «Подарок (1)/(2)» + «Подарок на выбор» caption + band mechanic chip (R44)
          FullCalendarPage.tsx      #   shell: role gate + PageHeader (Экспорт + import/create actions) + FilterBar (Тип/**«Все статусы» (10-я R46 — per-line via STATUS_FILTER_OPTIONS + displayLinesFor + matchesStatusFilter/lineDisplayStatus)**/КМ/Признак + **§10 «№ промо» multi-select + «Период акции» date-range** + «Скрыть отменённое» + column chooser) + editable line store (useReducer Map: edit/delete/add/addMany/recheck1C/bulkAdv + **10-я R46 setPending/clearPending**) + campaign state (visibleCampaigns) + bulk-select strip + 1С banner + autosave fixed-footer bar. **§7**: `ownKmId` — plain КМ sees only own promos/lines. **§8 gifts**: `giftTarget {lineId, slot}` + onGiftPick/onPickForGift/onRemoveGift. **§13**: onExport → buildFullCalendarCsv. **10-я R46**: `onEdit` on an approved campaign routes a genuine КМ data-field edit (key in `TRACKED_FIELD_LABEL`) into `line.pending` (mergePendingChange) instead of mutating fields — adv flags/gifts apply directly (code-review fix); hosts `LineDetailsDrawer` (`detailsLineId` state, deferred open) + the per-user rejection-seen store (`rejectionLineIds` for КМ, `handleOpenDetails` marks seen). Hosts the add/gift pickers, dup-confirm, Excel-import, create/edit-campaign, mobile Sheet, version drawer, period-change dialog
          FullCalendarGrid.tsx      #   Pattern F split-pane grid. Sticky header + single sticky-bottom synced scroller (7-я §4); `CLAMP2` 2-line wrap; gray/bold header + dividers (§9); `cellJustify` alignment (§12); Идентификация Тип/Название/Период columns + band; per-line «Изменить»/«Удалить» (§3); Бренд + StoreAvailabilityCell (§5/§6). **§8 GiftCell/GiftSubCell**: «Подарок (1)/(2)» fixed OR «подарок на выбор» sub-rows (**10-я R44: «Подарок на выбор» caption row + band mechanic chip; lineHeightPx += captionRow**). **10-я R46**: props `onOpenDetails`/`rejectionLineIds` (replaced `changedCells`/`changeBadges`); `lineDisplayStatus`/`isRepeatActionPending` per line → light-orange only for repeat-pending, «Черновик» chip, cancelled gray+strikethrough+«Отменено/Удалено», per-row **eye button** (+ red КМ dot); `ChangeBadge` deleted, amber changed-cell ring removed, `LineMarkers` slimmed to дубль/1С only (Блок 1.2/13)
          LineDetailsDrawer.tsx     #   **«10-я часть» Волна 2 new (R46 Блоки 4.3/5.3/6.7)** — read-only right Sheet «Детали изменений» opened by the eye icon: Информация об акции → Изменение (Поле/Было/Стало for `pending.change` · «Добавлена номенклатура» for addition · «Запрос на исключение» for removalPending) → Детали запроса (Тип/Кто/Дата/Комментарий) → Отклонение (кто/дата/причина). Actions (Согласовать/Отклонить строку) are the approval card (Волна 3)
          EditableCell.tsx          #   inline number/money/percent/text input — commit on blur/Enter, Esc cancels; required marker + ✏️; `align` now `left|right|center` (§12 centered numbers); fixed height keeps panes aligned (grid + Sheet)
          AddNomenclatureDialog.tsx #   searchable 1С-reference Command picker (§8.2.1, no free-text); reused for adding a line AND picking gift nomenclature (any slot/index, §8)
          StoreAvailabilityCell.tsx #   §5: «Наличие в магазинах, %» (getStoreAvailability → «94,87%») with a tooltip (N из M активных магазинов, центральные склады не учитываются); read-only, reused for main + gift nomenclature
          ExcelImportDialog.tsx     #   Excel/CSV bulk import (§8.2.1): campaign select + template + preview (Готово/Дубль/Ошибка); imports non-error rows as drafts (pending 1С)
          CreateCampaignDialog.tsx  #   two-tab Dialog — **§14: «Выбрать плановое промо» is the DEFAULT tab** (auto-pull тип/название/период preview → integrate) / «Новая внеплановая»; also edit-mode for an unplanned not-yet-sent campaign
          LineEditSheet.tsx         #   mobile «редактировать строку» Sheet — fields stacked by group; **§8 GiftEditor** (fixed №1/№2 or choice list + «Добавить подарок» + per-gift наличие/остаток from 1С + remove); S4 P3 «Участие в акции» removal section
          WarehousePopover.tsx      #   per-warehouse остаток breakdown + total (§8.2.2, read-only from 1С)
          gridFields.ts             #   column dictionary (Appendix C + feedback): groups, widths, source→lock, required. **FB**: DEFAULT_VISIBLE_GROUPS = all 5 (§11); Товар += `brand` + `storeAvailability` before `stock` (§5/§6); Идентификация `start`+`end` merged → `period` (§4); Маркетинг gift cols → `gift1/gift2 Nomenclature/Availability/Stock` (§8; **10-я R44 labels «Подарок (1)/(2)»**)
          ColumnGroupToggle.tsx     #   «Колонки N из 5» popover with group checkboxes (all ON by default now, §11)
        reports/                    # S5 — Отчёты для смежных подразделений (complete) + **5th-part feedback E-1 (§1–§3, complete)**
          ReportsPage.tsx           #   shell: role gate + department Tabs/Select + campaign picker; marketing-flag store; **E-1**: `changeKindPage`/`isAckedPage`, `filters` + «Фильтры» toggle (`countActiveReportFilters`) + `applyReportFilters` → `filteredLines` + «Показано: N» (`shownLines`), «Экспорт» → `exportReportXlsx`, **per-user ack** via `report-ack-store` (`useCurrentUser`+`ackTick`), «Кто ознакомился» host (role-gated `canSeeWhoAcked`); passes `snapshotFor` + `totalCount`
          DepartmentReportView.tsx  #   **E-1 rewrite** — band-layout Pattern-F split pane (frozen «Изменение»+«Номенклатура» + scrolling cols + trailing «Ознакомление»; sticky header + **a single synced sticky-BOTTOM viewport scrollbar (7-я часть §4 — top strip removed)** re-measured on dept switch; `border-r`/`border-b`; **7-я часть §3/§6.3: `CLAMP2` 2-line wrap** on the nomenclature/headers + `f.kind === "text"` cells, 52px rows preserved); `ChangePlashka` (Добавлено/Изменено/Исключено via `excludedIds`/`removedLineIds`), amber changed-cell + «Было→Стало·дата» Tooltip; three version-total counters + «Всего позиций» (`totalCount`); «Только изменения»; «Ознакомиться со всеми изменениями (N)» (version-wide); Mode-B cards below md
          reportFields.ts           #   **E-1 rewrite** — columns PROJECTED from full-calendar `gridFields` per dept (`buildColumn` over `MARKETING_IDS`/`COMPENSATION_IDS` + `LOCAL_COLUMNS` + `ACCESSORS`; `GROUP_OVERRIDE` Компенсация/Подарки; Marketing gains Бренд + Наличие в магазинах, %); exports `ReportColumn`/`reportColumnsFor` (+ `ReportField` alias), `MARKETING_EDITABLE_FIELD`
          ReportFilters.tsx         #   **E-1 new** — collapsible «Фильтры»: one typed filter per visible column (text→search, тип/признак→enum multi-select, num/money/%→min–max, date→range) + synthetic «Изменение»/«Ознакомление» + «Сбросить фильтры»; exports `ReportFilterState`/`applyReportFilters`/`countActiveReportFilters`/`EMPTY_REPORT_FILTERS` (decimal-aware `parseNum`, local-midnight date bounds)
          ReportAcknowledgeDrawer.tsx #  **E-1 new** — role-gated «Кто ознакомился» Sheet: per changed/added/excluded position, seeded roster users with ✓«Ознакомлен·дата» / «Не ознакомлен»; reads `getReportRoster` + `getAckRecords`
        notifications/              # S6 — Центр уведомлений (complete)
          NotificationsProvider.tsx #   **E-2 persistent store** — merges rebuilt seeds with the localStorage live-list + applies the per-user read-set at load (seeds bypass `notify` → no toast on load); exposes `notify(input)` (stamps actor/id/sentAt, prepends, persists via `appendLiveNotification`, fires ONE SPA-safe sonner toast via `navigate`) + `acknowledge`/`acknowledgeMany` (persist reads via `addReadIds`); reads `useCurrentUser`/`useRole` for the read-key + actor. Mounted ABOVE the AppShell (in ProtectedLayout) so the bell + nav badge + page share live state
          NotificationsPage.tsx     #   PageHeader (+ «Отметить все прочитанными») + type FilterBar + role-filtered «Непрочитанные» (date-grouped) over a muted «Прочитано» section; empty state. **E-2b**: role filtering now via `notificationsForRole(currentRole, notifications, notificationConfig)` (the `useNotificationSettings()` config)
          NotificationItem.tsx      #   one notification row: type icon (tinted) + unread dot, type chip, «отчёт vN», campaign name, description, actor·role·RuDate, per-item «Ознакомлен». **E-2b**: a «Роли: A, B +N» tag (`rolesForType`, active role brand-highlighted, tooltip = full list) + the `notificationLinksFor` deep-link buttons «Открыть промо/согласование/отчёт» (replaces the single quick Link; `LINK_LABEL`/`n.href` retired)
        notification-settings/      # E-2b — per-role notification config (Администратор), applied live to the center + bell
          NotificationSettingsProvider.tsx # config store above the AppShell (wraps NotificationsProvider): useState(getRoleConfig()) + setRoleCategory (toggle + persist) + resetConfig; useNotificationSettings()
          NotificationSettingsPage.tsx     # `/notification-settings` Админ screen: nav-`roles` gate + in-screen guard; 9 role blocks × 6 category Switches (Администратор block disabled+on, «видит все уведомления») + «Сбросить к умолчаниям»
        permissions/                # D — Матрица прав (3rd-round, complete) — read-only, КД + Администратор
          PermissionsMatrixPage.tsx #   shell: PageHeader + active-role line + role guard («Недостаточно прав») + Pattern-J Tabs (Сводная матрица + legend/§11.1 note · Детальные права) + **«Экспорт» (Group A T7/V1-8: two-sheet .xlsx via lib/permissions-xlsx.ts, visible on both tabs)**
          AccessMatrixTable.tsx     #   Appendix-D 9×5 grid: semantic <table> w/ sticky role column, active-role row brand-tinted + «Вы», level chips (note tooltips) → per-role cards below md; exports AccessLevelLegend
          CapabilityList.tsx        #   «Детальные права»: CAPABILITIES grouped, each row = label + description + monospace enforcedIn + allowed-role chips (active role brand) from rolesWithCapability
        audit/                      # S8 «Аудит-лог» → **E-3 «Аудит-лог и контроль сроков» (2→4 tabs, complete)**
          AuditPage.tsx             #   shell: PageHeader (H1 «Аудит-лог и контроль сроков») + Pattern-J Tabs (Сроки по плану / Сроки по промо и отчётам / Показатели участников / Аудит-лог) + page-level date-range + роль + «Сбросить фильтры» (deadline tabs only) + `AuditAccess` memo (КМ own-scope, isAdmin). **Group A T7/V1-8:** each of the 4 tab components renders its own filter-aware «Экспорт» button (`lib/audit-xlsx.ts` — exports exactly the visible/filtered rows; AuditPage itself untouched)
          PlanDeadlinesTab.tsx      #   Tab 1: plan control points (buildPlanControlPoints) → shared table+filters; КМ gets an access-notice (no per-КМ plan attribution)
          PromoDeadlinesTab.tsx     #   Tab 2: promo/report control points (buildPromoControlPoints); КМ own-rows only (representative OWN_AUDIT_KM_ID); replaces the old horizontal timeline
          ControlDeadlinesTable.tsx #   shared band-layout deadline table (lead=plan|promo): sticky header + `border-r`/`border-b` gridlines + result tint chips → Mode-B cards below md
          ControlDeadlinesFilters.tsx # shared deadline filter set (период/№/ответственный/роль/контрольная точка/результат incl. «Только просроченные») + mobile «Фильтры» Sheet; exports applyControlFilters/EMPTY_CONTROL_FILTERS/countActiveControlFilters
          ParticipantMetricsTab.tsx #   Tab 3 «Показатели участников»: role selector (КМ ranked / director roles aggregated) → место·ФИО·дедлайн·вовремя·просрочка·своевременность %+band·ср.просрочка·возвраты·повторные + «Справочно» (post-approval counts out of the rating); КМ → own row + disabled selector
          ParticipantTasksDrawer.tsx #  Tab 3 drill-down Sheet: a participant's control points (контрольная точка · дедлайн · факт · просрочка · comment)
          AuditLogTable.tsx         #   Tab 4 «Аудит-лог»: filterable action log (mono ids, tabular date+time, action chip, object cell, статус до→после, comment) + «Ключевые действия» (default) / «Все действия» (Администратор-only) toggle over NON_KEY_ACTIONS; КМ own-rows; sticky table → Mode-B cards + deferred «Фильтры» Sheet
          AuditLogFilters.tsx       #   пользователь/роль/тип действия/объект + диапазон дат (date inputs); row layout (desktop) / stack layout (mobile Sheet)
        promo-types/                # S7 — Настройки типов промо (complete)
          PromoTypesProvider.tsx    #   useReducer rules store (save/send/confirm/archive/create/copy) above the list + :ruleId routes; §9.5 re-confirmation (editing an approved rule → draft); usePromoTypes()
          PromoTypesPage.tsx        #   two-pane shell: role gate (getPromoTypeSettingsAccess) + PageHeader; reads :ruleId; list ↔ editor (stack below lg, URL-driven); empty-state hint
          RuleListPanel.tsx         #   left list: search (name/type), «Создать правило», per-row «Копировать», status badge + type names + required-field count
          RuleEditor.tsx            #   right editor: status banner + confirmer/date, name Input, promo-type chip multi-select (7 PROMO_TYPES), category-grouped required-field checklist (per-group select-all indeterminate), §9.3 effect-preview note, gated actions (Сохранить/Отправить/Утвердить КД-only/Архивировать), re-confirm banner, §9.5 history
          ruleFields.ts             #   required-field catalog grouped by the 5 Appendix-C categories, derived from gridFields COLUMNS/COLUMN_GROUPS + the 3 frozen identity columns
        approvals/                  # S3 — Согласование и проверка (Phases 1–3 + **4th-round client feedback 2026-07-02, 10 items**)
          ApprovalsProvider.tsx     #   review store (useReducer) ABOVE the queue + detail routes; actions approve/reject + requestNonParticipation (КМ→Старший КМ) + setNonParticipationByKd (КД direct, final); kind-aware approve **stamps `kdStageStartedAt` on a Старший-КМ forward (§9)**; selectors getItem/queueFor (live-escalation aware)
          ApprovalsPage.tsx         #   role-aware: reviewers get the queue, КМ gets «Мои участия», others an explainer. **FB §1–§5:** FilterBar = Тип · Признак (§5) · «№ промо» (`PromoNoFilter`, §2) · «Период акции» date-range (§2) · «КМ» multi-select (`KmMultiSelect`, **persisted per user** `promo:approvals-km-filter:<id>`, §4) · «Статус согласования» stage Select (§3) · «Срок согласования» Select (Все/Требуют решения сегодня/Только просроченные — replaces the overdue toggle, §1). **§3:** reviewers see `visibleReviewQueue(items)` (both stages) — Администратор sees all `items`; acting still gated on the detail page
          KmMultiSelect.tsx         #   **FB §4** — «КМ» searchable multi-select (Command-checkbox Popover, native-`<button>`+`buttonVariants` trigger), mirrors `PromoNoFilter`; selected VALUES = КМ ids (page persists them per user)
          ReviewQueueTable.tsx      #   **FB §6 rewrite** — calendar band layout (NOT shadcn `<Table>`): page-sticky header (`sticky -top-4`, `overflow-clip` Card) + **a single synced sticky-BOTTOM viewport scrollbar (7-я часть §4 — top strip removed; header + body + bottom via `scrollLeft`/ResizeObserver)** + unified `border-r`/`border-b` dividers + gray/bold header; **single pane (no frozen columns)** → natural row height. Columns № промо/Тип/Название/КМ/Отправлено/Статус/SLA/chevron (**7-я часть §6.2 compact widths: 104/132/280/170/150/240/200/44; §3 `CLAMP2` 2-line wrap on Название/КМ**); **§7/§9 SLA cell** = `itemSla` «N раб. дн. (до DD.MM.YYYY)» / «+N дн. просрочено»; **§8** status = `displayKmStatus` («На согл. у КД» for escalated) + «Авто-передано» tag → Mode-B MobileListCard list below md
          ApprovalDetailPage.tsx    #   /approvals/:id split view (DetailPageHero + SLA InfoRows; lg:grid-cols-[1fr_340px], sticky actions); effectiveReviewer/canAct via live escalation. **FB §7/§8/§9:** hero badge = `displayKmStatus` + «Авто-передано: просрочка у старшего КМ»; SLA InfoRow = `itemSla` (stage-КД) «осталось N раб. дн. (до DD.MM.YYYY)»; amber **senior-overdue note** (`seniorOverdueInfo`) + `autoForward` passed to the VersionHistoryDrawer; hosts the unified ReasonDialog + mobile fixed action bar
          MyParticipationsPanel.tsx #   **FB §10 rewrite** — КМ self-service, **own promos only** (representative `CATEGORY_MANAGERS[0]`); FilterBar (№ промо/тип/период/статус) + cards (№/тип/название/период/отправлено/статус + **«SLA КМ»** via `kmSubmissionSla`) + «Заявить о неучастии» → **«Заявка о неучастии отправлена»** (pending) → «Не участвует» (final)
          SubmittedLinesPanel.tsx   #   **Волна 3 rewrite** — список номенклатур акции по `ApprovalRow[]`: весь промо при повторном согласовании / набор своего КМ в первичном потоке (§4); «Согласовано ранее» + замок вместо чекбокса (§5); светло-оранжевые строки повторных действий (§6, класс общий с полным календарём); клик по строке → `onOpenRow` (§7); тумблер «Все строки / Только изменения» (§11) + счётчик «На согласовании: N · Согласовано ранее: M» (`showCounters`, только карточка повторного согласования, §12); **колонки статуса нет** (§13); rejection tint + comment tooltip сохранены
          LineChangeDrawer.tsx      #   **Волна 3 new** (§7–§10, §15) — боковая панель проверяющего: тип изменения, «Было/Стало» (для исключения синтетическое «Согласована в акции» → «Предложена к удалению»), данные добавленной позиции, комментарий КМ, дата повторной отправки, блок срока/просрочки (`itemSla`) и кнопки «Согласовать/Отклонить строку». Отличается от read-only `LineDetailsDrawer` полного календаря: та панель для КМ, эта — для проверяющего
          ReviewActionsPanel.tsx    #   data vs «Не участвует» actions + КД «Установить Не участвует» + **§8 auto-escalation note («Авто-передано: просрочка у старшего КМ … старший КМ больше не может согласовать»)** + «Готовность кампании» advance gate + «Комментарии проверки» log; exports MobileReviewActionBar (fixed bottom, below lg)
        users/                      # E-4 — Управление пользователями, ролями и временным замещением (reworked from #0+A)
          UsersPage.tsx             #   shell: effective admin scope from useCurrentUser (global «Администратор» / «Администратор подразделения» → own-dept only + notice + null → access-denied); UsersFilters + «Показано: N» + self-rendered «Экспорт» (users-xlsx, PageHeader showExport={false}); «Создать пользователя» (GLOBAL-admin-only) → UserFormDialog create; hosts KdSubstitutionPanel (global || god-mode КД); «Открыть» → navigate(/users/:id); reset/toggle-admin(→setUserRoles, multi-role-safe)/toggle-status; every audit(...) carries targetUserId
          UsersTable.tsx            #   columns ФИО·Email·Роли(chips via rolesOf)·Подразделение·Должность·Руководитель·Статус·Создан + «Открыть» (first) row action; canManage scoping hides mutating items; canToggleGlobalAdmin gates the admin-toggle item to global admins; isAdmin via rolesOf
          UsersFilters.tsx          #   E-4 new — search(ФИО/email)/подразделение/роль(multi-role aware)/статус + applyUserFilters/countActiveUserFilters/EMPTY_USER_FILTERS
          UserFormDialog.tsx        #   E-4 new (replaces CreateUserDialog) — create+edit; multi-role chip select (roles[0]=primary), Подразделение/Должность/Руководитель; adminRoleLocked prop disables the «Администратор» chip for non-global admins; emits UserFormValue (no adminScope)
          UserDetailPage.tsx        #   E-4 new — /users/:id Pattern-D: Профиль (InfoRows + «Редактировать» → UserFormDialog edit; handleEditSubmit re-checks wouldGrant/RemoveAdmin vs isGlobalAdmin + canRevokeAdmin before saving) · Роли и доступ (role chips + admin scope + dept-admin grant + reset/block) · Журнал действий (getLiveAuditEvents()+buildAuditLog() filtered targetUserId). Scope = effectiveAdminScope(currentUser) ?? god-mode-Админ fallback (matches UsersPage)
          KdSubstitutionPanel.tsx   #   E-4 new — «Уполномоченное лицо КД»: active substitution (substitute/window/assigned-by) + «Назначить (другого)»/«Снять замещение» + history; assign/revoke append audit («назначение/снятие замещения», targetUserId=substitute)
          TempPasswordDialog.tsx    #   сброс/создание пароля → новый временный пароль (показывается один раз); reused unchanged
        profile/                    # C — Профиль (все роли)
          ProfilePage.tsx           #   PageHeader + Pattern-J Tabs: «Основная информация» (аватар-инициалы, редактируемое ФИО → updateUserName + useCurrentUser().refresh(), read-only Email/роль/статус/дата/последняя смена пароля) + «Безопасность» (добровольная смена пароля через NewPasswordForm + verifyCurrentPassword; примечание «вход без 2FA»). Смена ФИО/пароля пишется в живой аудит
        settings/                   # C — Настройки (все роли)
          SettingsPage.tsx          #   PageHeader + «Язык интерфейса» (RU / O'zbek Кир. / O'zbek Лат. — display-only, promo:pref-language) + «Тема оформления» (Светлая/Тёмная/Системная — via useTheme()/ThemeProvider, promo:pref-theme; B: полноценная рабочая тёмная тема) + «Связанные настройки» (role-gated кросс-ссылки: Уведомления / Настройки типов промо [КД·Админ] / Управление пользователями [Админ])
        auth/                       # Login, ForgotPassword, ResetPassword, ForcePasswordChange, AuthLayout, AuthContext (re-export), RequireAuth (re-export)
          ForcePasswordChangePage.tsx #  принудительная смена временного пароля; использует NewPasswordForm; перенаправляет в приложение после успешной смены
          NewPasswordForm.tsx       #  переиспользуемая форма «новый пароль + подтверждение» (10+/регистр/цифра/спецсимвол); применяется в ForcePasswordChangePage (принудительно) и ProfilePage→Безопасность (добровольно). Опциональный verifyCurrentPassword рендерит поле «Текущий пароль» и проверяет его перед сменой (forced-flow его не передаёт — backward-compatible)
    components/                     # Promo-specific shared primitives (reused by every screen)
      PromoStatusBadge.tsx          # wraps shared StatusBadge; full status map (Appendix A)
      OverdueTag.tsx                # red «+N дн.» micro-tag, never blocking
      Money.tsx / RuDate.tsx        # wrappers over formatSum / formatDateFull
      ReasonDialog.tsx              # confirm Dialog with required reason Textarea
      VersionHistoryDrawer.tsx      # S4: three-view right Sheet — «Только изменения» (per-field diff + live «Черновик — не отправлено» edit-after-approval draft) / «Полный актуальный отчёт» (live snapshot, removed lines struck) / «История версий» (version list incl. «Отмена»/«Корректировка» from P3 + S3 comments/просрочка + P3 deadlineChange log); footer = re-approval status + Маркетинг/КД actions when a draft exists, else «Создать корректировку» hint. All props optional → backward-compatible with the S3 approvals usage. **FB 2026-07-02:** optional `autoForward` prop renders an «Авто-передано КД: просрочка у старшего КМ» history entry (§9)
      DeadlineChips.tsx             # calendar-deadline chips (46/21/17 дн., «календарные»)
    lib/
      promo-mock-data.ts            # seed: 18 campaigns (16 planned PR-2026-001…016 + 2 unplanned UN-2026-014/015; PR-2026-008…016 are line-less short-calendar enrichment → 15 visible short-calendar rows once the cancelled PR-2026-004 is hidden), 6 КМ, 30 SKUs, 7 promo types; S2 PromoLine model + 9 lines, warehouse breakdown, installment + full-calendar-access helpers, isGiftType / missingRequiredFields / isLineValid validators; Phase 3: createPromoLine factory, detectDuplicate (+DuplicateHit), LineHistoryEntry; Phase 4: IMPORT_COLUMNS, buildImportTemplateCsv/buildImportSampleCsv, parseImportCsv (+ParsedImportRow/ImportParseResult), createImportedLine; Phase 5: createUnplannedCampaign (+UnplannedCampaignInput), validateUnplannedInput, minUnplannedStartDate/addCalendarDays/MIN_UNPLANNED_LEAD_DAYS, PromoCampaign.firstSendDone, «Черновик» CampaignStatus; S3: working-days SLA (isWeekend/addWorkingDays/workingDaysBetween/reviewSla, REVIEW_SLA_WORKING_DAYS), ReviewItem (+ReviewComment/LineFeedback, kind data|non-participation), reviewerForKmStatus/buildReviewItems/reviewQueueFor/reviewItemId, approvedKmStatusFor(actor, kind)/REJECTED_KM_STATUS, line seeds L-0010…L-0014 (every pending review item has nomenclature); Phase 3: isAutoEscalated/effectiveReviewer (live SLA-breach escalation), isFinalKmDecision/campaignDecisionSummary (advance gate), participationsForKm/canRequestNonParticipation (КМ panel), one non-participation seed (PR-2026-006~km-6); S4 Phase 1: version model (VersionChangeType, VersionFieldChange, CampaignVersion, CampaignReportRow), getCampaignVersions (seeded chains for PR-2026-001/003 + single-version fallback), buildCampaignReport (live snapshot for «Полный актуальный отчёт»); S4 Phase 2: PromoCampaign.periodChanged, APPROVED_CAMPAIGN_STATUS/isApprovedCampaign, diffCampaignChanges (+CampaignChangeSet: changes/changedCells/hasValueChange/hasAddition/periodChanged), buildSentVersion, 2 seed lines for the approved PR-2026-003 (1+1, km-4) so edit-after-approval is demoable; S4 Phase 3: PromoLine.removed/removalPending/removalReason/removalRequestedBy, PromoCampaign.cancelReason/cancelledBy/cancelledAt/fillDeadlineOverride/deadlineChange, DeadlineChangeRequest, effectiveFillDeadline, canCancelCampaign/canRequestLineRemoval/canApproveLineRemoval/canManageDeadline/canApproveDeadline, buildCancellationVersion/buildLineRemovalVersion, buildCampaignReport removed-aware, seed lines on cancelled PR-2026-004 + approved UN-2026-015 (one removalPending); S5: ReportDepartment + DEPARTMENT_LABELS/DEPARTMENT_SHORT, getReportAccess (+ReportAccess, Appendix D «Отчёты»), getReportDeadline (start − 17 кал. дн.), getSentCampaigns, getReportSentAt/getReportVersionNo, ReportChangeSet + getReportChangeSet (seeded on UN-2026-015), a UN-2026-015 version chain (on-time first send + incremental correction), enriched lines on PR-2026-003 + UN-2026-015 (incl. new L-0021 «added») so the M/P/A subsets populate; S6: NotificationType (6 types) + NOTIFICATION_TYPE_META (label+tint), PromoNotification (+ optional visibleTo roles), buildNotifications (~8 seed, live relative timestamps), notificationsForRole (§11.3.1), groupNotificationsByDate (Сегодня/Вчера/дата); S7: PromoTypeRule (name/promoTypeIds/requiredFieldIds/status/history/confirmedBy/At), PromoTypeRuleStatus (draft→pending→approved+archived) + PROMO_TYPE_RULE_STATUS_LABEL/_TINT, RuleHistoryEntry, PROMO_TYPE_RULES (3 seed: approved/draft/archived), getPromoTypeSettingsAccess (+PromoTypeSettingsAccess — КД edit+confirm / Админ edit / else read-only), promoTypeNamesFor, nextRuleId; S8: AuditActionType (8) + AUDIT_ACTION_META (tint) + AuditObjectType (4) + AUDIT_OBJECT_LABEL, AuditEvent + AUDIT_EVENTS_SEED (~21 reconstructed events) + buildAuditLog (newest-first, stable AUD-#### ids), ControlMilestone/ControlMilestoneState + buildControlTimeline (per-campaign milestones; firstSend date from the seeded version chain only, КМ-fill breach start−21 кал. дн. + report breach start−17 кал. дн., dataSent derived from status), getAuditCampaigns, AuditSummary + auditSummary (count · overdue events · avg approval раб. дн.); **Краткий-календарь feedback (2026-06-19)**: CategoryDistributionEntry {date, category, responsibleKmId} + PromoCampaign.categoryDistribution (seeded PR-2026-001/002/003/007), CampaignReadiness + campaignReadiness, PlanStageStatus/PlanStageMarketing/PlanStageDirector + CampaignPlanApproval + PLAN_APPROVALS + getPlanApproval, PLAN_MARKETING_REVIEW_LEAD_DAYS (63 кал.) / PLAN_MARKETING_SUBMIT_LEAD_DAYS (60 кал.) / PLAN_DIRECTOR_SLA_WORKING_DAYS (3 раб.); **Краткий-календарь feedback V2 (2026-06-29)**: `KmStatus` reworked (§5) — «Согласовано КД» (was «Принято коммерческим директором»), «Не заполнено» + «Переотправлено на корректировку КМ» (split from «Не заполнено / Ожидание корректировки»), «Отменена» (auto on full cancellation; «Согласовано старшим КМ (ожидает КД)» removed — `approvedKmStatusFor(Старший КМ)` returns «На согл. у КД», `REJECTED_KM_STATUS` = «Переотправлено на корректировку КМ»); `KmAggregate`/`aggregateKmStatuses` split «на согласовании» into `atSeniorKm`+`atKd` (+`cancelled`); `CampaignReadiness` gained `atSeniorKm`, denominator excl. «Не участвует» **and** «Отменена»; cancelled PR-2026-004 seed → km-1 «Отменена»; `buildControlTimeline` (S8) + `AUDIT_EVENTS_SEED` status strings updated to the new taxonomy; **Краткий-календарь feedback V2 Фазы B–D**: `formatPromoNo(id)` → «26-N» (§6b), `kmStatusDeepLink(campaignId, kmId, status)` → /approvals?promo=&km= (review stages) or /full-calendar?promo= (final) (§10), `ReportSendStatus` + `getReportSendStatus(campaign)` (sent/sentAt/versionNo + 17-кал.-дн. report deadline + days-overdue-if-unsent) (§12); **Полный-календарь feedback (2026-07-01)**: `NomenclatureItem.brand` (§6); `ACTIVE_STORE_COUNT` + `CENTRAL_WAREHOUSES` + `StoreAvailability` + `getStoreAvailability` + `formatAvailabilityPct` (§5); `isCampaignFreshEditable` (§3 status gate); `GiftItem` + `PromoLine.gifts[]` (was giftNomenclatureId/giftStock), `PromoTypeRef.giftChoice` + «Подарок на выбор» type + `isGiftChoiceType` (§8); PR-2026-014 → «Подарок на выбор» seed + L-0015 2nd gift; **Согласование feedback (2026-07-02)**: `ReviewItem.kdStageStartedAt?` (КД-stage start for §9); `visibleReviewQueue` (both-stage view, §3) + `ReviewStage`/`reviewStageOf` (stage filter); `stageSlaStart`/`itemSla` (stage-aware SLA — КД counts from the auto-forward moment / `kdStageStartedAt`, §9); `SeniorOverdueInfo`/`seniorOverdueInfo` (§9); `displayKmStatus` (escalated → «На согл. у КД», §8); `KmSubmissionSla`/`kmSubmissionSla` + `KM_SUBMISSION_OVERDUE` seed («SLA КМ», §10); `approve` in the provider stamps `kdStageStartedAt` on a Старший-КМ forward; **5th-part E-1 (Отчёты)**: `ReportChangeSet` reshaped — `changedCells: ReportCellChange[]` (prevValue/newValue/changedAt) + `removedLineIds` (+`reportCellChange`); seeds UN-2026-015 added(L-0021)/changed(L-0019, values matched to live)/excluded(L-0020, keeps `removalPending`); `getReportSnapshot` + `REPORT_SNAPSHOT_OVERRIDES` (per-version read-only snapshot, latest = live); `ReportRosterUser` + `REPORT_ROSTER` + `getReportRoster`, `REPORT_ACK_SEED` + `getReportAckSeed` (who-acknowledged seed); **5th-part E-3 (Аудит-лог и контроль сроков)**: `export KM_FILL_SLA_CALENDAR_DAYS` (was module-private) + `OWN_AUDIT_KM_ID = "km-3"` (representative КМ for audit own-scope); `CampaignPlanApproval` gained an optional correction chain (`returnedAt`/`returnedBy`/`returnComment`/`resentAt`/`deliveredToKmAt`, seeded on PR-2026-006) — all consumed by the new `lib/audit-control.ts` derivation layer; **«10-я часть» Волна 2 (R46)**: `LinePendingChange` (action change|addition · fields[{field,label,was,now}] · by/at/comment/requestType · rejected{by,at,reason}) + `PromoLine.pending?` (unapproved repeat action — table shows approved data, Блок 2), threaded through `LineSeed`/`PROMO_LINES`; demo seeds on the approved PR-2026-003 — L-0015 `pending.change`, L-0022 `pending.addition` (km-1), L-0023 `pending.rejected` (km-1) — plus «Подарок (1)/(2)» label rename in gridFields (R44)
      notifications-store.ts       # **E-2 new** — localStorage persistence for the live notifications store: `promo:notifications-live` (live-emitted list, **cap 50**, newest-first, `sentAt` Date↔ISO) + `promo:notifications-read:<userId>` (per-user read-set, **anon fallback**); getLiveNotifications/appendLiveNotification + getReadIds/addReadIds; mirrors report-ack-store's defensive `typeof window`/JSON idiom
      notification-settings-store.ts # **E-2b new** — localStorage `promo:notification-role-config` (role×category): `DEFAULT_ROLE_CONFIG` (faithful inversion of the pre-E-2b audiences → no regression), getRoleConfig (merges stored over defaults, filters unknown types)/persistRoleConfig/resetRoleConfig, rolesForType (drives the «для роли X» tag)
      report-ack-store.ts          # **E-1 new** — localStorage `promo:report-ack`; per-user acknowledgement keyed (campaignId, department, version) × userId × lineId; getAckedLines/acknowledgeLine/acknowledgeLines + getAckRecords (seed+live merge, deduped by user+line) for «Кто ознакомился»
      report-xlsx.ts               # **E-1 new** — real `.xlsx` via SheetJS (`xlsx` dep): `exportReportXlsx` builds a workbook (sheet = department, leading «Изменение» column) of the currently visible/filtered rows; reuses `exportStamp`
      users-store.ts               # localStorage `promo:users`; seed 8 users (≥2 активных Администратора + функциональные роли + mustChangePassword-пользователь; **E-4**: u-6 = «Администратор подразделения» Маркетинг, u-8 «Тошматов Фаррух» kmId=km-5 = seeded КД-substitute). helpers: authenticate, createUser (E-4: carries roles/department/position/managerId), resetPassword, setUserRole (legacy, now unused), setUserStatus, updatePassword, updateUserName (C); **E-4 additive**: `PromoUser` += `roles?[]`/`department?`/`position?`/`managerId?`/`adminScope?`/`kmId?`, `AdminScope`, `DEPARTMENTS`, `rolesOf` (read roles via this), `updateUser`/`setUserRoles`/`setDeptAdmin`, `effectiveAdminScope`/`canManageUser`; `usableAdminCount`/`canRevokeAdmin` retargeted to `rolesOf` (multi-role admins count; ≥2-guard holds). Every pre-E-4 export unchanged → /profile+auth don't regress
      kd-substitution-store.ts     # **E-4 new** — localStorage `promo:kd-substitution`: the zero-or-one active «Уполномоченное лицо КД» + history; `KdSubstitution`, getActiveSubstitution (local-midnight inclusive window)/getSubstitutionHistory/assignSubstitution/revokeSubstitution/substituteName + pure **canActAsKd**/**isSubstituteConflicted** (consumed by S3 ApprovalDetailPage/ApprovalsPage); seeded active sub-1 (u-8, 2026-06-15…12-31)
      users-xlsx.ts                # **E-4 new** — real `.xlsx` of the (filtered) user list via SheetJS (`exportUsersXlsx`; ФИО/Email/Роли/Подразделение/Должность/Руководитель/Статус/Создан); mirrors report-xlsx + reuses `exportStamp`
      audit-store.ts               # localStorage `promo:audit-live`; appendAuditEvent (пользователь + активная роль + тип действия + объект «пользователь»); живые события модуля учёток объединяются с S8-сидами в buildAuditLog (остальные экраны по-прежнему seed-stale); новые AuditActionType: «сброс пароля», «назначение прав», «отзыв прав», «блокировка», «разблокировка» (#0+A) + «смена пароля», «изменение профиля» (C) + **«изменение ролей», «назначение замещения», «снятие замещения» (E-4)**; **E-4**: `AuditEvent.targetUserId?` (для журнала конкретного пользователя на `/users/:id`); `getLiveAuditEvents()` — живые события с Date + AUD-L#### ids (buildAuditLog остаётся seed-only; per-user журнал мержит оба)
      audit-control.ts             # **E-3 new** — pure derivation layer for «Аудит-лог и контроль сроков» (reconciled by construction): `ControlPoint`/`ControlResult` + `buildPlanControlPoints` (from PLAN_APPROVALS + lead-day/SLA consts + the seeded PR-2026-006 correction chain) + `buildPromoControlPoints` (from buildReviewItems/seniorOverdueInfo/stageSlaStart + report helpers; auto-forward attributes overdue to Старший КМ) + `buildControlPoints` (plan+promo, newest-deadline-first); `PARTICIPANT_ROLES` + `ROLE_CHECKPOINTS` + `TimelinessBand`/`timelinessBand` + `ParticipantMetricRow`/`ParticipantTask` + `buildParticipantMetrics(role)` (aggregates the SAME points; КМ ranked, director roles single-representative rows; overdue = overdueDays>0 so columns sum) + `buildParticipantTasks(name, role)` (drill-down). Reads seeds only — read-only + seed-stale
      promo-export.ts              # §8 CSV export: toCsv (UTF-8 BOM, `;`-delimited) + downloadCsv + buildCalendarCsv (short calendar; **Group A T11**: `{ includeReportColumns }` opt omits «Срок отчёта»+«Отправка смежным» for ungated roles) + buildPlanCsv (3-stage approval rows; **«7-я часть» §9.5**: reads `getPlanState()` → + «Статус строки» lifecycle column + «Отклонил / Роль согласующего / Дата и время отклонения / Комментарий отклонения» from the latest `rejectionLog` rejection) + **buildFullCalendarCsv** (full-calendar feedback §13 — FLAT: one row per nomenclature, «26-N» № per Group A T8) + exportStamp
      audit-xlsx.ts                # **Group A T7 new** — generic `exportAuditXlsx({sheetName, header, rows, filename})` (SheetJS aoa idiom + `fmtAuditDate` via formatDateFull); consumed by the per-tab «Экспорт» buttons in all 4 audit tabs (each exports ITS already-filtered rows)
      permissions-xlsx.ts          # **Group A T7 new** — `exportPermissionsXlsx`: two-sheet workbook «Матрица прав» (ACCESS_AREAS × roles, level labels) + «Детальные права» (CAPABILITIES + rolesWithCapability), read LIVE from lib/permissions.ts; one «Экспорт» button on /permissions
      plan-store.ts                # **Group A T12 new** — localStorage `promo:plan-state`: PersistedPlanState (**8** PlanMode lifecycle slices — planStatus/rejectedStage/extraRows/overrides/deletedIds/sendStatus/decisions + **«7-я часть» §9 `rejectionLog`**: per-row `PlanRejectionEvent[]` {kind kd/od/return · by ФИО · role · at ISO · comment}, newest-first, absent-in-old-snapshots → {}), getPlanState/persistPlanState/clearPlanState, per-field defensive parse + Date↔ISO; hydrated lazily in PlanMode (one snapshot read → lazy initializers → one write-through effect) so план-акций drafts/sends/decisions/отклонения survive reload. **Волна 4:** +9-й слайс **`rowJournal: Record<rowId, PlanRowJournal>`** (пер-строчный журнал: `cycles: PlanApprovalCycle[]` {no · sentAt · sentBy · kd? · od? · closedAt? · closedReason "return"|"edit"} + `removal?: PlanRemovalRequest` {requestedAt · requestedBy · reason · requiredStages · kd? · od?} + `removalHistory?`), плюс экспортируемые `PlanReviewerStage`/`PlanStageDecisionKind`/`PlanStageDecision`; отсутствует в старых снапшотах → `{}`; `rejectionLog` помечен **legacy read-only**
      plan-approval.ts             # **Волна 4 new (R29.5/R30.1/R30.2)** — чистый слой над журналом строки плана и ЕДИНСТВЕННАЯ деривация её состояния: таблица, панель истории и CSV читают только отсюда, поэтому разъехаться не могут (сид `PLAN_APPROVALS` — fallback ВНУТРИ этих функций, а не отдельная ветка у каждого потребителя). **Чтение:** `currentCycle`/`openCycle`; `marketingStageCell`/`directorStageCell` → `StageCellData` {reviewedAt · sentAt · decidedAt · decision · status · overdueDays · **unit "cal"|"work"** · by · cycleNo} — маркетинговый дедлайн в КАЛЕНДАРНЫХ днях (`startDate − PLAN_MARKETING_SUBMIT_LEAD_DAYS`), SLA КД/ОД в РАБОЧИХ (`+PLAN_DIRECTOR_SLA_WORKING_DAYS`), старт этапа ОД = согласование КД, пять исходов включая «ожидает, но срок прошёл» и «этап не начат»; `planRowLifecycle` (единственное новое состояние — `removal-pending`); `approvedStages` (снимок согласовавших этапов; у сид-строк этап считается согласованным по наличию `decidedAt` при статусе ≠ `waiting`), `removalNeedsApproval`, `pendingRemovalStageFor` (гейт ТОЛЬКО по роли и `requiredStages`, без `planStatus`), `removalFullyApproved`/`removalRejected`, `latestJournalRejection`, `STAGE_LABEL`. **Запись** (все чистые, возвращают новый журнал, элементы `cycles` заменяются целиком, а не мутируются): `withSend` (повторно не открывает уже открытый цикл), `ensureOpenCycle` (сид-строка материализуется как цикл №1 с `marketing.sentAt` из сида — SLA считается от реальной отправки), `withDecision` (явное ветвление `kd`/`od`, не вычисляемый ключ), `withCycleClosed`, `withRemovalRequest`/`withRemovalDecision`/`withRemovalArchived`. Без React, без `window`/`localStorage`
      permissions.ts               # D — canonical role→permission SoT (документ + консолидация): AccessLevel + ACCESS_LEVEL_META (4 levels, soft tints light+dark), ACCESS_AREAS (5 Appendix-D areas) + ACCESS_MATRIX (9 roles × 5 areas, cells {level,note} w/ spec wording), FINAL_APPROVAL_NOTE (§11.1); Capability + CAPABILITIES (21 actions, 6 groups — **E-4** added group «Пользователи и доступ»: manage-users-global/dept, assign-kd-substitute, act-kd-substitute) with allowed(role) **derived live** from getFullCalendarAccess/getReportAccess/getPromoTypeSettingsAccess/canCancelCampaign/canRequestLineRemoval/canApproveLineRemoval/canManageDeadline/canApproveDeadline + enforcedIn pointers; rolesWithCapability, getPermissionsScreenAccess (КД+Админ). Reads existing helpers — does NOT rewrite them
      full-calendar-status.ts      # **«10-я часть» Волна 2 new (R46)** — pure per-line status layer for `/full-calendar`: `LineStatus` (10 values) + `lineDisplayStatus(campaign,line)` (priority removed→exclusion-pending→rejected-repeat→pending-repeat→cancelled→campaign.status/line.rejected) + `matchesStatusFilter` (+ synthetic «На согласовании (общее)» via `STATUS_FILTER_UMBRELLA`/`PENDING_APPROVAL_STATUSES`) + `STATUS_FILTER_OPTIONS` (11 filter values) + `isRepeatActionPending` (light-orange row condition) + `lineHasRejection` (КМ red-dot source) + `mergePendingChange` (edit→pending diff builder). Re-exports `LinePendingChange`. Reads types only
      approval-card.ts             # **«10-я часть» Волна 3 new (R57)** — чистая модель строк карточки согласования: `ApprovalRowKind` (change/addition/removal требуют решения · approved-earlier · rejected-repeat · primary — прежний поток S3 · context) + `ApprovalRow` + `buildApprovalRows(campaign, lines, item)` + `approvalCounters` + `rowMarkerLabel` (для «context» откатывается на реальный `lineDisplayStatus`) + `decisionActionFor`. «Согласовано ранее» ставится ТОЛЬКО реально согласованным строкам — чужая незакрытая позиция помечается своим статусом
      line-decision-store.ts       # **«10-я часть» Волна 3 new (R57 §16)** — localStorage `promo:line-decisions`: решения проверяющего по повторным действиям (`LineDecision` {lineId, campaignId, action change|addition|removal, kind approved|rejected, by, at, reason?}), `getLineDecisions`/`recordLineDecision(s)`/`clearLineDecisions` + чистый **`applyLineDecisions(lines)`** (согласовано «изменение» → значения из `pending.fields[].value` применены и `pending` снят · «добавление» → согласована · «исключение» → `removed` · отклонено → `pending.rejected`; отклонённое исключение записывается как разрешённое повторное действие, чтобы КМ увидел «Было/Стало» + причину). Применяется и в карточке согласования, и при засеве строк `FullCalendarPage`
      plural.ts                    # **Волна 3 /simplify** — `pluralRu(n, [one, few, many])` + `LINE_FORMS`/`POSITION_FORMS`; заменил три копии русской плюрализации (approval-card, FullCalendarPage)
      full-calendar-rejection-store.ts # **«10-я часть» Волна 2 new (R46/Блок 6)** — localStorage `promo:fc-rejection-seen:<userId>` (anon fallback): the per-КМ set of rejected lines whose «Детали изменений» panel was opened → clears the red indicator; getSeenRejections/markRejectionSeen (defensive typeof-window/JSON idiom)
    styles/                         # index/tailwind/theme/fonts/globals (copied from Dashboard)
```

## Routes

| Route | Component | Status |
|---|---|---|
| `/login` `/login/forgot-password` `/login/reset-password/:token` | auth pages | Done — **без 2FA**; проверка email+пароля по `users-store.ts`; 5 попыток / 15-мин. блокировка. Демо-учётки: `admin@texnomart.uz` / `Admin2026!` (Администратор); `newuser@texnomart.uz` / `Temp1234!a` (принудительная смена пароля при первом входе) |
| `/change-password` | ForcePasswordChangePage | Done — принудительная смена временного пароля (первый вход); использует `NewPasswordForm`; недоступен, если `mustChangePassword = false` |
| `/users` | UsersPage | **Done (E-4 — user/role/substitution management)** — scope from the logged-in `useCurrentUser()`: global «Администратор» → all + «Создать пользователя» (global-only) + grant global admin + `KdSubstitutionPanel`; «Администратор подразделения» → own-department users only, no create/global-grant/substitution + a notice; null → access-denied. `UsersFilters` (search/подразделение/роль/статус) + «Показано: N» + real `.xlsx` «Экспорт» (users-xlsx); `UserFormDialog` (create+edit, multi-role); reworked `UsersTable` (Роли/Подразделение/Должность/Руководитель + «Открыть» + `canManage`/`canToggleGlobalAdmin` scoping); row reset/toggle-admin(→`setUserRoles`)/toggle-status; every audit carries `targetUserId` |
| `/users/:id` | UserDetailPage | **Done (E-4)** — Pattern-D detail: **Профиль** (InfoRows + «Редактировать» → `UserFormDialog` edit; `handleEditSubmit` re-checks admin-role changes vs `isGlobalAdmin`+`canRevokeAdmin`) · **Роли и доступ** (role chips + admin scope + dept-admin grant + reset/block; the «Администратор» chip is locked for non-global admins) · **Журнал действий** (`getLiveAuditEvents()`+`buildAuditLog()` filtered by `targetUserId`). Scope matches `UsersPage` (`effectiveAdminScope(currentUser)` ?? god-mode-Админ fallback) |
| `/profile` | ProfilePage | **Done (C, все роли)** — Мой профиль: Tabs «Основная информация» (аватар-инициалы, **редактируемое ФИО** → `updateUserName` + live-refresh шапки, read-only Email/роль/статус/дата/последняя смена пароля) + «Безопасность» (**добровольная смена пароля** через `NewPasswordForm` + поле «Текущий пароль»; примечание «вход без 2FA»). Смена ФИО/пароля → живой аудит |
| `/settings` | SettingsPage | **Done (C + B, все роли)** — Настройки: «Язык интерфейса» (RU / O'zbek Кир. / O'zbek Лат. — display-only) + «Тема оформления» (Светлая/Тёмная/Системная — через `useTheme()`/`ThemeProvider`, persists `promo:pref-theme`; **B: полноценная рабочая тёмная тема**) + «Связанные настройки» (role-gated кросс-ссылки) |
| `/` | → redirects to `/short-calendar` | Done |
| `/short-calendar` | ShortCalendarPage | **Done (S1 + 2026-06-19 feedback; + 2026-06-29 feedback V2 — COMPLETE)** — Промо-календарь: **header «Фильтры» toggle** (§7 — in the PageHeader beside the title, before «Экспорт», widened + active-facet badge) collapsing the grouped Операционные/Контрольные/Распределение filters (**searchable multi-select № промо** §9, period date-range, **single «Статус КМ по акции»** filter §5, distribution filters auto-expand) + collapsible «Распределение по категориям» block + **compact** rows (§11) showing **«26-N» № промо** (§6b) → period → крайний срок КМ → **«Срок отчёта»** (§12) → «Статус готовности акции» per-cell 5-segment bar (§2/§3) → **«Отправка смежным отделам»** (§12) → per-КМ columns last (**clickable** §10: review stages → /approvals, final → /full-calendar; cells filtered by «Статус КМ по акции» §5) + **единый sticky-BOTTOM viewport scrollbar (7-я часть §4 — top strip removed)** + page-sticky header (§13) + full-bleed + filter-aware CSV export (incl. «26-N» + report columns). «План акций»: stepper + per-campaign 3-director approval-stage table + **кликабельное «Отклонено» → панель истории строки (кто · роль · дата-время · комментарий + история, 7-я часть §9)**, отклонения персистятся и выводятся в CSV плана. **Волна 4 (R29.5/R30.1/R30.2):** колонки КД/ОД показывают **дату решения + «В срок»/«Просрочка +N раб. дн.»** из пер-строчного журнала (раньше только сид), маркетинговый срок — в кал. днях, метка **«Цикл N»** при повторной отправке (прежние даты и решения не затираются, а уходят в историю и аудит); действие **«История»** у каждой строки открывает `PlanRowHistoryDrawer`; **удаление согласованной строки идёт через согласование** — запрос с обязательной причиной, статус **«Удаление на согласовании»**, решение принимают согласовавшие её КД/ОД из боковой панели пер-строчно (вне цепочки `planStatus`), после всех согласований строка уходит из плана, отклонение возвращает её; живые события плана пишутся в `/audit` |
| `/short-calendar/:promoId` | ShortCalendarDetailPage | **Done (S1)** — Pattern D full page |
| `/full-calendar` | FullCalendarPage | **Done (S2 + S4 + client feedback 2026-07-01)** — editable Pattern F grid: inline editing, live validation, warehouse popover, bulk-select, nomenclature entry (1С Command picker) + duplicate check, Excel/CSV import + 1С banner/re-check/submit gate, unplanned creation + integrate + edit-until-first-send, mobile per-line Sheet; **S4 Phases 1–3**: «История» → three-view `VersionHistoryDrawer`, edit-after-approval (baseline diff, amber changed-cell, «N изм. после согл.», Маркетинг re-approve → КД «Согласовать и отправить»), period change (bold + ✏️), cancellation + line exclusion + «Скрыть отменённое» (ON). **Client feedback 2026-07-01 (14 items) + 7-я часть §3/§4:** sticky header + единый sticky-BOTTOM viewport scrollbar (top strip removed) + `CLAMP2` 2-line wrap (§1/§2); gray/bold header + column dividers (§9); unified alignment — text left, numbers/money/%/dates/checkboxes/badges centered (§12); Тип/Название/**Период** as columns + band «PR-… · N позиций» + **дедлайн removed (chip & S4 change-deadline buttons)** (§4); «Показано: N промо · M позиций» counter + flat CSV **«Экспорт»** (§13); per-line **«Изменить»/«Удалить»** + autosave, no «Сохранить черновик» (§3); **«Бренд»** + **«Наличие в магазинах, %»** columns before Остаток (§5/§6); **КМ** sees only own promos/lines, others see all + КМ filter (§7); searchable **«№ промо»** multi-select + **«Период акции»** date-range filters (§10); all column groups ON by default (§11); create dialog defaults to **«Выбрать плановое промо»** (auto-pull) (§14); gifts reworked to **«Подарок №1/№2»** columns + **«подарок на выбор»** sub-rows (merged main nomenclature + «+ Добавить подарок») (§8). **«10-я часть» Волна 2 (R46 Блоки 1–7 + R44):** single **«Все статусы»** filter applied **per line** (11 values + «На согласовании (общее)»); **светло-оранжевая подсветка только для повторных действий** (изменение/добавление/исключение на согласовании), первичное/согласованное — обычным видом; «Черновик» chip; отменённые серый+зачёркнуто+«Отменено/Удалено»; **иконка-глаз** → `LineDetailsDrawer` «Детали изменений» (Поле/Было/Стало · запрос · отклонение); **таблица показывает согласованные данные** (`line.pending`, новые значения — только в панели, Блок 2); красный индикатор КМ по отклонениям (per-user, гаснет при открытии); убраны янтарная обводка/бейдж «N изм.»/статусные плашки (Блок 1.2/13); подарки → **«Подарок (1)/(2)»** + подпись **«Подарок на выбор»** + чип механики (R44) |
| `/approvals` | ApprovalsPage | **Done (S3 — Phases 1–3 + client feedback 2026-07-02)** — reviewers: review queue (**calendar band layout** — page-sticky header + единый sticky-BOTTOM viewport scrollbar [7-я часть §4] + dividers + compact columns/2-line wrap [§6.2/§3], §6) with the **stage-aware SLA** «N раб. дн. (до DD.MM.YYYY)» (§7/§9) + «Не участвует»/«Авто-передано» tags + `displayKmStatus` (§8); enlarged FilterBar (Тип · Признак §5 · № промо + Период §2 · **КМ multi-select persisted per user** §4 · Статус согласования §3 · Срок согласования §1); **Старший КМ + КД see both stages** (§3). КМ: **reworked «Мои участия»** — own promos only, card fields + «SLA КМ» + 4 filters + «Заявка о неучастии отправлена» state (§10) |
| `/approvals/:id` | ApprovalDetailPage | **Done (S3 — Phases 1–3 + client feedback 2026-07-02)** — split view: submitted snapshot + per-line/bulk approve·reject + «Не участвует» lifecycle + live auto-escalation + advance gate + comment log/history + mobile action bar; **FB:** `displayKmStatus` + «Авто-передано: просрочка у старшего КМ» badge (§8), **stage-КД SLA** «осталось N раб. дн. (до DD.MM.YYYY)» (§7/§9), amber **senior-overdue note** + История auto-forward entry (§9). **«10-я часть» Волна 3 (R57, 16 пунктов):** карточка **повторного согласования** (`kind: "repeat"` — правки/добавления/запросы на исключение по уже согласованной акции): «Получено на согласование» вместо «Отправлено» (этап + дата повторной отправки, §1/§3) + «Авто-передано КД» с датой (§2); **весь список номенклатур акции** с read-only «Согласовано ранее» + замком (§4/§5); светло-оранжевые строки повторных действий (§6) → **`LineChangeDrawer`** с «Было/Стало», комментарием КМ, датой и просрочкой (§7–§10) и решением прямо из панели (§15); тумблер «Все строки / Только изменения» (§11), счётчик (§12), без колонки статуса (§13); решения только по строкам, требующим решения (§14); согласование применяет новые значения и снимает подсветку, отклонение возвращает данные КМ с причиной в истории (§16, `line-decision-store`) |
| `/reports` | ReportsPage | **Done (S5 + 5th-part feedback E-1 §1–§3)** — role-gated Маркетинг/Закуп/Аналитика tabs + campaign picker. **E-1:** columns derived from full-calendar `gridFields` per dept (Marketing incl. Бренд/Наличие %); **band-layout table** (frozen «Изменение»+«Номенклатура», sticky header + единый sticky-BOTTOM viewport scrollbar [7-я часть §4] + gridlines + 2-line wrap/compact widths [§3/§6]) → Mode-B cards; **«Изменение»** column (Добавлено/Изменено/Исключено, excluded kept+struck) + changed-cell «Было→Стало·дата» tooltip + three version-total counters + «Всего позиций» + «Только изменения»; **per-column typed «Фильтры»** + «Сбросить фильтры» + «Показано: N»; real **`.xlsx` «Экспорт»** (SheetJS, filter-aware); **per-version read-only snapshots** + «Текущая версия» (shared `VersionHistoryDrawer` `snapshotFor`); **per-user acknowledgement** (`report-ack-store`) + «Ознакомиться со всеми изменениями (N)»; role-gated **«Кто ознакомился»** drawer. Mock limits: per-browser ack; seeded roster/snapshots; god-mode user not a roster member; in-browser QA pending |
| `/notifications` | NotificationsPage | **Done (S6 + E-2 live+persistent + E-2b per-role config)** — Центр уведомлений: live bell + nav badge + page share one read/unread store (provider above AppShell); 6 notification types; date grouping, «Непрочитанные»/«Прочитано» sections, type filter, per-item + bulk «Ознакомлен». **E-2** — live emission from S4/S5 handlers + localStorage persistence (`promo:notifications-live` + per-user reads) + toast. **E-2b** — visibility via the editable per-role config (`notificationsForRole(role,list,config)`, Администратор bypasses), a «Роли: …» tag, and «Открыть промо/согласование/отчёт» deep-links |
| `/notification-settings` | NotificationSettingsPage | **Done (E-2b)** — Настройки уведомлений (Администратор): per-role notification config — 9 role blocks × 6 category Switches (Администратор block disabled+on «видит все уведомления») + «Сбросить к умолчаниям»; persisted to `promo:notification-role-config`, applied live to the center + bell; nav-`roles` + in-screen guard |
| `/audit` | AuditPage | **Done (S8)** — Аудит-лог (read-only, all roles, spec §11.9): Tab 1 «Аудит-лог» — filterable action log (mono ids, tabular date+time, action chip, object cell, статус до→после, comment; пользователь/роль/тип/объект/диапазон-дат filters; sticky-header table → Mode-B cards + «Фильтры» Sheet below md); Tab 2 «Свод контрольных событий» — per-campaign milestone timeline (horizontal md+/vertical mobile) + red overdue nodes (OverdueTag + responsible) + summary strip (count·overdue·avg раб. дн.) |
| `/permissions` | PermissionsMatrixPage | **Done (D — 3rd-round, КД + Администратор)** — Матрица прав: read-only role access matrix («документ» = Appendix D, 9×5 level grid + legend + §11.1 note) + «Детальные права» («консолидация» = 17 granular actions with allowed-role chips **derived live** from the real `getXAccess`/`canX` helpers + code-enforcement pointers); Pattern-J Tabs; active role brand-highlighted; in-screen guard + nav-gated to КД/Администратор; dark-aware; mobile per-role cards |
| `/promo-types` `/promo-types/:ruleId` | PromoTypesPage (under PromoTypesLayout) | **Done (S7)** — two-pane required-field rules: list (search/create/copy/status badges) + editor (name, promo-type chip multi-select, category-grouped checklist, §9.3 effect preview, Черновик→На подтверждении→Утверждено(+Архив) workflow, §9.5 re-confirmation + change history); КД edit+confirm / Администратор edit / others read-only; nav item КД/Администратор-gated |

## Roles (9) — `role-context.tsx`

Коммерческий директор · Операционный директор · Директор маркетинга · Категорийный менеджер (КМ) · Старший КМ · Сотрудник маркетинга · Сотрудник закупа · Сотрудник аналитики · Администратор.

- The active role lives in `RoleContext` (`useRole()`), persisted to `sessionStorage` (`promo:current-role`), default **Коммерческий директор**.
- The switcher is in the AppShell **avatar dropdown** + a **pill** beside the avatar — implemented as an optional, backward-compatible extension to the shared `AppShell` (`roleSwitcher` prop + `RoleSwitcherConfig` type). Apps that omit it (Dashboard) are unchanged.
- Nav items gate by the **active role** (`NavItem.roles`); the `/promo-types` item is КД/Администратор only.
- Full access matrix: prompt-pack **Appendix D**.

## Shared (`@texnomart/shared`) changes made for Promo

- `types`: added `RoleSwitcherConfig`; **B**: added `ThemeController` ({ value, onCycle }).
- `app-shell.tsx`: optional `roleSwitcher` prop → role pill + user-menu switcher; nav gating uses the active role (falls back to `config.user.role`). The Promo wrapper drives the existing `maxWidth` prop per-route (`100%` for `/full-calendar`, the `/short-calendar` list, and — **Group A T6, tracker V1-3.4** — exact `/reports`, `/approvals`, `/audit`; `1400px` elsewhere, incl. the short-calendar + approvals detail pages) via `useLocation`. **S6**: optional `notificationsHref` prop — when set, the bell popover's «Показать все» button (now a controlled Popover) closes + navigates there; Dashboard omits it → unchanged. **Sidebar active state**: a nav item is active via `isNavItemActive(pathname, href)` = exact match OR child path (`pathname.startsWith(href + "/")`), so detail/internal routes (`/short-calendar/:id`, `/approvals/:id`, `/promo-types/:ruleId`) keep their parent item highlighted; the root `/` item stays exact-only. General improvement (Dashboard detail routes benefit too) — verified `build:dashboard` passes. **B**: optional controlled `theme` prop (`ThemeController`) — when provided (Promo passes `{ value, onCycle }` from `useTheme`), the header theme toggle reflects/persists via the project's `ThemeProvider` so the header ↔ Settings stay in sync; omitted ⇒ the shell keeps its self-contained local toggle (Dashboard unchanged). `<main>` already had `dark:bg-background`.
- `components/*` (B): added `dark:` variants to the pattern components Promo renders (page-header, filter-bar, info-row, mobile-list-card, timeline, detail-page-hero, list-page-header, document-thumbnail, status-badge) — light mode byte-identical, verified `build:dashboard`. `status-badge.tsx` dropped its dead `hover:${bg}` (the shadcn Badge hover is `[a&]`-gated, so a `<span>` badge never hovered) so config tint strings can carry `dark:` variants.
- `components/filter-bar.tsx`: added optional `className` prop (merged via `cn`) to override the container — **all** Promo FilterBars pass `bg-transparent px-0` so the dropdowns sit **flush-left in line with the page title/grid** (short-calendar, full-calendar, approvals, notifications; the audit filters + reports pickers align the same way without a FilterBar); Dashboard usages keep the default gray box, unchanged.
- `components/page-header.tsx`: header height relaxed `sm:h-14` → `sm:min-h-14` so tall titles (32px H1 + subtitle) aren't clipped; backward-compatible.
- `utils/formatters.ts`: added `formatSum` (UZS «сум»).

## Conventions

- All UI text **Russian**. Currency UZS via `formatSum` → «1 299 000 сум». Dates DD.MM.YYYY (`RuDate`), date+time in history.
- Always label deadlines «календарные дни» vs review SLAs «рабочие дни».
- Cancellation is a **separate state** (`cancelled`), never inside «Признак акции» (план/внеплан only).
- «Ознакомление» ≠ «Согласование». Rollback unsupported — reverting is a new correction.
- Statuses render only through `<PromoStatusBadge>` (soft tint, color + text). Overdue via `<OverdueTag>` — never blocks.
- **Dark theme (B)**: new screens must work in both themes. Prefer semantic tokens that adapt automatically (`bg-card`/`bg-background`/`bg-muted`/`bg-accent`/`text-foreground`/`text-muted-foreground`/`border-border`/`bg-primary`/`text-primary`). For hardcoded light colors, KEEP the light class and APPEND a `dark:` variant (`text-gray-900 dark:text-gray-100`, `bg-white dark:bg-card`); soft status tints → `bg-X-50 dark:bg-X-500/15` + `text-X-700 dark:text-X-300`; brand yellow is the `primary` token (never `bg-[#FFD60A]`). Leave solid `-500` fills/dots/bars as-is. Statuses come from `<PromoStatusBadge>` (already dark-aware). Don't add a fixed page background that fights the theme.
- All data mock in `src/lib/`. Detail views are full pages, never drawers (drawers only for config/version history).
- **Ограничения модуля учёток (#0+A)**: localStorage — хранение на браузер (не общий бэкенд); пароли хранятся строками (не хэшированы — только прототип); временный пароль показывается администратору на экране (email не отправляется); управление учётками гейтировано по **активной роли** в god-mode переключателе ролей; только действия этого модуля добавляются в живой аудит — остальные экраны по-прежнему seed-stale.
- Аудит-лог (`/audit`) теперь **объединяет** живые события из `audit-store.ts` с сидовыми данными S8, сортируя всё по дате (сначала новые). Живые события создаются только при действиях в модуле управления учётками.

## When Building a Section (S1–S8)

1. Read the matching S-prompt + Appendices in `docs/promo_prompt_pack.md`.
2. Replace the route's placeholder component with the real screen under `src/app/components/<feature>/`.
3. Reuse `@texnomart/ui` primitives, `@texnomart/shared` patterns, and the Promo primitives in `src/components/`.
4. Extend `promo-mock-data.ts` for screen-specific data; gate actions by `useRole()`.
5. Follow Patterns A–K, design tokens, and Russian-only locale.
