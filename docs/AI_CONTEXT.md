# AI Context — Texnomart Monorepo

> Last updated: 2026-07-10 (Promo: **Краткий промо-календарь «6-я часть» client feedback COMPLETE (7 items)** — the «Комментарии общие» PDF (08.07.2026), i.e. the тракер's «🆕 новое» items **V2·14–18** (were ⛔ «ждёт корректировки ТЗ») + two new asks, applied to the **S1** «План акций» tab by layering a **per-row send lifecycle** `Черновик → выбрать → Отправлено → Согласовано/Отклонено` over the «8-я часть» reviewer decisions (seed splits by `getPlanApproval`). **№1** auto № промо (`nextPlanPromoNo` → read-only field). **№2** черновой план без типа: тип необязателен → «Черновик» (ред./удал.), red «Тип не выбран» marker + banner, тип обязателен при отправке. **№3** «Директор маркетинга» → доступ к `/promo-types` (nav-роль + `getPromoTypeSettingsAccess` `canEdit`, утверждение за КД). **№4** fixed the rejected-plan stepper (all stages green ✓ on reject) → `rejectedStage` → red ✗ «— отклонил» + per-row «Отклонено». **№5** explicit «Отправить на согласование» (не авто). **№6** ред./удал. черновиков; правка отправленной → «Черновик» + повторная отправка. **№7** чекбоксы выбора + «Отправлено» блокирует повтор + **подсказка о пропущенных датах** (`findCoverageGaps` → баннер + toast). 4 files (`PlanMode.tsx`, `PlanApprovalTable.tsx`, `promo-mock-data.ts`, `shell-config.tsx`), Promo-local; `build:promo` green + in-browser QA (Дир. маркетинга/КД, 1440/390px). Mock: plan state session-local (reload reseeds). See HISTORY.)
> Prev: 2026-07-09 (Promo: **Краткий промо-календарь «8-я часть» client feedback COMPLETE (5 items)** — applied «Тест первой MVP промо-календаря» to the **S1 «Краткий промо-календарь»** module. **Context:** the screenshots are from **`promo.tm.uz`** (a **backend-integrated** deploy — server-loading text, raw role codes `marketing_director: approved`, real data «Мазза нарх тест»/«Бонус тест»), **NOT this mock repo** (GitHub Pages; grep for those strings = zero hits); confirmed with the user to **fix in the mock repo** (backend team mirrors the logic). **№4** — `getReportSendStatus` returns `overdueDays > 0` only on **actual late send** (`getOverdueDays(deadline, sentAt)`); «Срок отчёта» = crayний-срок date only, «Отправка смежным отделам» = fact only («Не отправлено» / «Отправлено · дата · в.N» + «+N дн.» when sent late), «отчёт просрочен» removed (grid + mobile card + CSV). **№3 (+№1+№2)** — `PlanApprovalTable` gained a `selectable` mode (leading checkbox column + «Выбрать все» + per-row «Согласовано/Отклонено» badge + row tint); `PlanMode` models per-row decisions per reviewer stage (КД/ОД) with a «Согласовать/Отклонить выбранные (N)» strip + progress «Согласовано N из M на этапе X» — approve one/several/all, advance (КД→ОД→Утверждён) only when all approved, reject→«Отклонён»→«Вернуть на доработку» resets; the same table shows № / тип / название / период so №1+№2 (full data at the decision point) are covered. **№5** — new pure `isPlanApprovedByDirectors(c)` (both `PLAN_APPROVALS` director stages decided, not «waiting») gates the calendar list for the **Категорийный менеджер (КМ)** role → sees only fully-approved plans (fixes «Найдено 0 акций»; other roles unchanged). 6 files, Promo-local (shared/Dashboard untouched); `build:promo` green + in-browser QA (КД/ОД/КМ, 1440/390px): №4 zero premature overdue + 1 sent-late tag; №3 partial 2/15 stays, select-all→advance, reject→return, 15 mobile checkboxes; №5 КМ «Найдено 5 акций» (26-1/2/3/5/7). See HISTORY.)
> Prev: 2026-07-07 (Promo: **5th-part feedback — sub-project E-4 «Управление пользователями, ролями и временным замещением» COMPLETE** — the LAST 5th-part sub-project. Reworked `/users` from the #0+A account screen into a full **user/role/substitution** module via subagent-driven development (13 impl tasks / 5 phases, per-task spec+quality review, a whole-branch **opus** review, one fix wave, in-browser QA). **Data (additive/backward-compatible):** `users-store` gains multi-role `roles[]` (read via `rolesOf`), подразделение/должность/руководитель, `adminScope` («Администратор подразделения»), `kmId`, scope helpers (`effectiveAdminScope`/`canManageUser`), and the ≥2-usable-admin guard retargeted to `rolesOf`; new **`kd-substitution-store.ts`** («Уполномоченное лицо КД» + pure `canActAsKd`/`isSubstituteConflicted`, seeded active substitution); `AuditEvent.targetUserId` + 3 new action types. **UI:** `/users` scoped by the logged-in `useCurrentUser()` (global «Администратор» vs «Администратор подразделения» → own-department only), filters, real `.xlsx` export (SheetJS), `UserFormDialog` (create+edit, replaces `CreateUserDialog`), `KdSubstitutionPanel`, reworked `UsersTable`, + a new **`/users/:id`** detail page (Профиль / Роли и доступ / Журнал действий); `permissions.ts` +4 capabilities. **КД substitution → S3 (functional):** an active substitute acts on КД-stage items (approve/reject stamped as «Коммерческий директор») with a same-person **conflict-of-interest guard** + a «замещение» banner; `ApprovalsPage` shows them the КД queue. Opus review **NOT-READY → fixed → APPROVED** — it caught cross-task seam defects the per-task reviews missed: a **Critical** (the `/users/:id` edit dialog bypassed the admin-grant + ≥2-admin gates the row-menu path enforced → privilege escalation + guard regression) + an **Important** scope-precedence mismatch between `UsersPage`/`UserDetailPage` + a duplicate «Экспорт». Both builds green; in-browser QA verified dept-admin scope, substitute acting + conflict block, `/users/:id`, `/permissions`. Identity = seeds (u-6 dept-admin «Маркетинг», u-8 «Тошматов Фаррух» km-5 = substitute); create is global-admin-only; god-mode switcher kept. Promo-local only. **All four 5th-part sub-projects (E-1 Reports · E-2/E-2b Notifications · E-3 Audit · E-4 Users) are now COMPLETE.** See HISTORY.)
> Prev: 2026-07-07 (Promo: **5th-part feedback — sub-project E-3 «Аудит-лог и контроль сроков» (2→4 tabs) COMPLETE**. Reworked the **S8 `/audit`** screen from 2 tabs to **4** and renamed it «Аудит-лог и контроль сроков» (deadlines · responsible people · overdue reasons), via subagent-driven development (11 tasks / 5 phases, per-task review, whole-branch opus review, one fix wave, full in-browser QA). A new pure **`Promo/src/lib/audit-control.ts`** derives a flat `ControlPoint[]` from existing seeds (`PLAN_APPROVALS`, `buildReviewItems`, version/report helpers, `CATEGORY_MANAGERS`); Tabs 1–2 («Сроки по плану» / «Сроки по промо и отчётам») render filtered slices through a shared band-layout table+filters, Tab 3 («Показатели участников») **aggregates the same points** into per-participant timeliness ratings (КМ ranked; директор roles aggregated; drill-down drawer; «Справочно» post-approval counts kept out of the rating), Tab 4 («Аудит-лог») adds a **«Ключевые действия» (default) / «Все действия» (Администратор-only)** toggle. Access per matrix: КМ own rows only (Tabs 2–4) + a plan-tab notice; others see all. The old horizontal `ControlEventsTimeline` was deleted (superseded by Tab 2). Exported `KM_FILL_SLA_CALENDAR_DAYS` + new `OWN_AUDIT_KM_ID="km-3"`. Opus review READY-WITH-FIXES → the per-task reviews (tabs in isolation) missed **cross-tab reconciliation breaks**: Старший КМ blank (name-vs-label), КД stuck 0% (plan-stage checkpoint uncounted), pending-past-deadline points not summing, Tab 3 ignoring `access`, inert/duplicated page-filters — all fixed in one wave + re-reviewed. In-browser verified (Администратор + КМ): all 4 tabs; Старший КМ 20% / КД 50% non-blank; columns sum; КМ own-scope + locked role selector; toggle fix; «Все действия» 16→21. Both builds green; Promo-local only. **Mock limits:** seed-stale; no per-person identity beyond `CATEGORY_MANAGERS` (КМ scope via `OWN_AUDIT_KM_ID`; director ratings single-representative rows); КМ timeliness skews «Низкая» for km-3 (bands exercised via Старший КМ/КД). **Next: E-4 Управление пользователями (+ КД временное замещение) — the last 5th-part sub-project.** See HISTORY.)
> Prev: 2026-07-06 (Promo: **5th-part feedback — sub-project E-2b «Уведомления» per-role config COMPLETE**. Built the original PDF «Уведомления» asks the E-2 live+persistent build didn't cover, via subagent-driven development (7 tasks, per-task review, whole-branch opus review, full in-browser verification): an **Администратор-editable per-role notification config** at `/notification-settings` (`notification-settings-store.ts` `promo:notification-role-config`; a `NotificationSettingsProvider` mounted ABOVE the AppShell; `notificationsForRole(role,list,config)` filters by it, Администратор bypasses [sees all]; `DEFAULT_ROLE_CONFIG` faithfully inverts the pre-E-2b audiences → **no visibility regression**), a **«для роли X» tag** on each notification (`rolesForType`, active role brand-highlighted), and **«Открыть промо/согласование/отчёт» deep-links** (`notificationLinksFor`; `/reports` `?promo=` pre-select). Interpretation of the paraphrased asks was proposed+approved in brainstorming (user delegated). In-browser verified: settings screen 9×6 toggles (admin block disabled); toggling a category off → persisted + filters that role's bell/list live across reload; «Роли:» tag + all 3 deep-links; `?promo=` pre-select (sent→selected, non-sent→default); Администратор sees all; reset restores. Opus review READY-WITH-FIXES → 1 Important fixed (the «Открыть отчёт» link is now gated on `getSentCampaigns()` so a non-sent campaign's `data-changed` doesn't silently open a different report). Both builds green; Promo-local only. **Mock limit:** the «для роли X» tag shows the full configured target set — per-user multi-role filtering is deferred to E-4. With E-2 + E-2b, «Уведомления» is fully covered. **Next: E-3 Аудит-лог (2→4 tabs), E-4 Управление пользователями (+ КД temp-substitution).** See HISTORY.)
> Prev: 2026-07-06 (Promo: **5th-part feedback — sub-project E-2 «Уведомления» (live + persistent) COMPLETE**. Reworked the **S6 `/notifications`** module from seed-stale/in-memory to **live + persistent**, via subagent-driven development (5 tasks, per-task review+fix, whole-branch opus review, in-browser end-to-end verification). **Scope was proposed+approved in brainstorming** (no discrete client items in the session docs) — direction **Live + persistent + toast**, deliberately lean UX. **Differs from the original PDF E-2 asks** (per-role notification sets, multi-role dedup «для роли X», specific deep-links) — those remain a possible follow-up. **What shipped:** additive data layer (`NotificationInput`, `notificationAudienceFor`, pure `createLiveNotification`); new `notifications-store.ts` (localStorage — live list cap 50 + per-user read-set, anon fallback); `NotificationsProvider` reworked into a persistent store with an imperative **`notify(input)`** (merges seeds+live, applies read-set, one SPA-safe sonner toast, **no toast on load**); **live emission** wired into 5 existing handlers — 4 in `FullCalendarPage` (cancel→campaign-cancelled, line-exclusion→line-removed, period-change→marketing-reapproval, send→data-changed) + 1 in `DepartmentReportView` (marketingApprove→ad-approval). The shared-shell bell count already updated live → **no `@texnomart/shared` change**. In-browser verified: КД cancel → toast + bell 4→5 + persist across reload; acknowledge → bell 5→4 + read persists across reload; no toast on fresh load. Opus review READY-WITH-FIXES → the 1 Important (ad-approval copy claimed КМ-notified but audience excludes КМ) fixed. Both builds green; Promo-local only. **Next: E-3 Аудит-лог (2→4 tabs), E-4 Управление пользователями (+ КД temp-substitution).** See HISTORY.)
> Prev: 2026-07-06 (Promo: **5th-part feedback — sub-project E-1 «Отчёты смежным отделам» COMPLETE**. Reworked the S5 `/reports` module per the «Отчёты смежным отделам» section (§1–§3), executed via subagent-driven development (9 tasks, per-task review + fix loop, whole-branch opus review). **§1:** report columns now **projected from the full-calendar `gridFields.ts`** per department (Marketing wide + new **Бренд**/**Наличие в магазинах, %**; Закуп/Аналитика компенсация set); a collapsible **«Фильтры»** panel with **one typed filter per visible column** (text/enum/number-range/date-range + synthetic «Изменение»/«Ознакомление») + **«Сбросить фильтры»** + active-facet badge + **«Показано: N позиций»**; a real **`.xlsx` export** (new **SheetJS** dep) of the visible/filtered rows. **§2:** a leading **«Изменение»** column with **Добавлено/Изменено/Исключено** plashki (excluded rows kept + struck, keyed off `removedLineIds`); changed cells highlight with a **«Было → Стало · дата/время»** tooltip (enriched `ReportChangeSet.changedCells: ReportCellChange[]`); three **version-total** counters + «Всего позиций»; toggle renamed **«Только изменения»**; **per-version read-only snapshots** + **«Текущая версия»** marker in the shared `VersionHistoryDrawer` (additive `snapshotFor` prop — approvals + full-calendar consumers unaffected); **per-user acknowledgement** (new localStorage `report-ack-store`, keyed campaign+department+version+line+user) + **«Ознакомиться со всеми изменениями (N)»**; a role-gated **«Кто ознакомился»** detail drawer (Администратор/КД/Дир.маркетинга/ОД/Ст.КМ). **§3:** the report table rewritten to the **band-layout Pattern-F split pane** (frozen «Изменение»+«Номенклатура», sticky header, synced top+bottom scrollbars, unified gridlines) + mobile Mode-B cards. Promo-local only (+ root `pnpm-lock.yaml` for `xlsx`); both `build:promo` + `build:dashboard` green; shared/Dashboard untouched. **In-browser visual QA still pending** (built in a no-browser session). Mock limits: per-user ack is per-browser localStorage; the «кто ознакомился» roster + per-version snapshots are seeded; the current god-mode user isn't a roster member, so the acknowledge→who-acknowledged loop shows only seeded roster users; export cells are pre-formatted strings. **Next: E-2 Notifications, E-3 Audit (2→4 tabs), E-4 Users (+ КД temp-substitution) — each own spec→plan→build.** See HISTORY.)
> Prev: 2026-07-02 (Promo: **Согласование client feedback (4-я часть, 10 items) COMPLETE** — applied «Комментарии по промо-календарю, раздел согласование» to the **S3 «Согласование и проверка»** module (`/approvals` + `/approvals/:id` + КМ «Мои участия»), in 4 phases (all builds green) + full in-browser QA. **P1 filters/roles/persistence:** «Срок согласования» Select replaces the overdue toggle (Все/Требуют решения сегодня/Только просроченные, §1); «№ промо» + «Период акции» filters (§2); «Статус согласования» stage Select (§3); «Плановое/внеплановое» Select (§5); the «КМ» filter is a **multi-select** persisted per user (`promo:approvals-km-filter:<id>`, §4); **Старший КМ + КД now SEE both review stages** (`visibleReviewQueue`) while acting stays gated (§3). **P2 stage-separated SLA:** `stageSlaStart`/`itemSla` — КД stage counts from the auto-forward moment / `kdStageStartedAt` (§9); SLA shows «N раб. дн. (до DD.MM.YYYY)» / «+N дн. просрочено» (§7); escalated items display «На согл. у КД» + «Авто-передано: просрочка у старшего КМ» (`displayKmStatus`, §8); senior-overdue on the card + История (`autoForward` drawer prop, §9). **P3 unified styling (§6):** `ReviewQueueTable` rewritten to the calendar band layout — page-sticky header + synced top/bottom scrollbars + dividers (single pane, no frozen columns). **P4 КМ «Мои участия» (§10):** own-promos-only (representative КМ), card fields + «SLA КМ» (seeded `kmSubmissionSla`) + 4 filters + the «Заявка о неучастии отправлена» → «Не участвует» transition. Promo-local only → Dashboard/shared untouched. Mock limits: no per-person КМ identity (representative КМ; Старший-КМ «their КМ» = all КМ); «SLA КМ» + review seeds are deterministic mock; КМ-filter persistence needs a logged-in user. See HISTORY.)
> Prev: 2026-07-01 (Promo: **Полный промо-календарь client feedback (3-я часть, 14 items) COMPLETE** — applied «Комментарии по полному промо-календарю» to `/full-calendar`, in 4 phases (all builds green). **§1/§2** synced sticky top scrollbar + sticky header (short-calendar Pattern-F scroll; scroll ancestor = the page's internal `overflow-auto`, so `top-0`); **§9** darker-gray bold header + column dividers; **§12** unified alignment (text left / numbers·money·%·dates·checkboxes·badges centered; money not bold; `EditableCell` gained `align="center"`); **§4** Тип/Название/**Период** (2 date cols merged) shown as columns + band simplified to «PR-… · N позиций» + **дедлайн chip & S4 deadline-change buttons removed** (kept in data/short-calendar); **§13** «Показано: N промо · M позиций» counter + flat CSV **Экспорт** (`buildFullCalendarCsv`, № промо per row); **§3** per-line **«Изменить»/«Удалить»** in the frozen pane + `delete` reducer + autosave indicator (no «Сохранить черновик»), editability status-gated (`isCampaignFreshEditable`); **§5/§6** «Бренд» + «Наличие в магазинах, %» columns before Остаток (`NomenclatureItem.brand`, `getStoreAvailability` — active stores, central warehouses excluded, «94,87%»); **§7** plain КМ sees only own promos/lines (Старший КМ/КД/Админ see all + КМ filter); **§10** searchable multi-select «№ промо» (`PromoNoFilter`) + «Период акции» date-range filters; **§11** all column groups ON by default; **§14** create dialog defaults to «Выбрать плановое промо» (auto-pull preview); **§8** gifts reworked — `PromoLine.gifts: GiftItem[]` + «Подарок на выбор» type, fixed «Подарок №1/№2» columns and choice sub-rows (main nomenclature merged + «+ Добавить подарок», variable row height synced across both panes), rippled to S4 diff / S5 reports / S7 rule seed / export / mobile Sheet. `build:promo` + `build:dashboard` pass, dev boots clean; **in-browser visual QA pending** (no browser tool this session). Promo-local only (shared untouched). See HISTORY.)
> Prev: 2026-06-30 (Promo: раунд фидбэка #3 под-проект **D — матрица прав** complete — the **last** of the four 3rd-round sub-projects (after #0+A, C, B); **the whole round is now done**. New read-only **«Матрица прав»** screen at `/permissions` (КД + Администратор) presenting prompt-pack **Appendix D** as a matrix («документ») **plus** a new canonical `Promo/src/lib/permissions.ts` that **consolidates** the scattered role→permission gating into one source of truth («консолидация»): `ACCESS_MATRIX` (9 roles × 5 areas, spec wording) + 4 access levels, and `CAPABILITIES` (17 granular actions) whose `allowed(role)` predicates are **derived LIVE from the real `getXAccess`/`canX` helpers** (no rewrite of those — zero regression to S2–S8). Two Pattern-J tabs («Сводная матрица» with legend + §11.1 note · «Детальные права» with per-action allowed-role chips + code-enforcement pointers); active role brand-highlighted; in-screen role guard. Promo-local only (shared untouched, Dashboard unaffected). Verified: `build:promo` + in-browser (light/dark, mobile cards, access guard). See HISTORY.)
> Prev: 2026-06-30 (Promo: раунд фидбэка #3 под-проект **B — тёмная тема** complete. Made the dark theme work end-to-end: new `theme-context.tsx` (`ThemeProvider`/`useTheme`, persists `promo:pref-theme`, live OS-pref listener) + an inline boot script in `index.html` (applies `.dark` before paint — no FOUC) + a theme-aware `<Toaster>`; rewrote the `.dark` palette in `theme.css` (layered dark surfaces, **brand `#FFD60A` primary preserved**, status colors tuned); an additive controlled `theme` prop on the shared `AppShell` (header ↔ Settings share one source via the provider; Dashboard untouched, `build:dashboard` green) + `dark:` variants on the shared pattern components; ~270 hardcoded light colors across the 49 Promo screen files migrated (hybrid: semantic tokens + `dark:` variants) — primitives by hand, screens via 6 parallel agents. Verified by both builds + an in-browser dark walk of all routes at 1920/390px + functional checks (no-FOUC, persistence, toggle sync, light-mode regression). Only **D** (матрица прав) remains. See HISTORY.)
> Prev: 2026-06-29 (Promo: раунд фидбэка #3 под-проект **C — Профиль/Настройки** complete. Персональные экраны `/profile` (Tabs: Основная информация — редактируемое ФИО `updateUserName` + read-only роль/статус/дата/последняя смена пароля; Безопасность — добровольная смена пароля, переиспользует `NewPasswordForm` с опциональным полем «Текущий пароль» + `verifyCurrentPassword`) и `/settings` (Язык — display-only · Тема — `.dark`-переключатель, полноценная тёмная тема = под-проект B · Связанные настройки — role-gated кросс-ссылки). Оба роута уже были в дропдауне shared `AppShell` — теперь живые, **без изменений в shared**. Новые `AuditActionType` «смена пароля» / «изменение профиля» пишутся в живой аудит. Остаются под-проекты B (тёмная тема), D (матрица прав). See HISTORY.)
> Prev: 2026-06-29 (Promo: раунд фидбэка #3 под-проект **#0+A — auth & accounts** complete. Стор пользователей (localStorage `promo:users`), вход без 2FA с реальной проверкой пароля по стору, временный пароль + принудительная смена при первом входе (`/change-password`), экран управления учётками `/users` (Администратор: создание / сброс пароля / назначение-отзыв прав / деактивация-активация; guard «≥2 рабочих администратора»), текущий пользователь в шапке (`current-user-context.tsx`), живой аудит-лог действий модуля учёток (объединяется с S8-сидами в `buildAuditLog`). See HISTORY.)
> Prev: 2026-06-29 (Promo Краткий промо-календарь client feedback V2 — **COMPLETE (all 13 items)**. Фаза A + §1/§13 done earlier; **Фазы B–D now done**: §6b `formatPromoNo` «26-N» № промо (short calendar + detail + export), §9 searchable multi-select № промо filter (`PromoNoFilter`, promoId→promoIds[]), §11 table compactness (row 104→80, header 48→40), §7 collapsible filter block (+ active-facet count badge), §10 clickable КМ-status deep-links (`kmStatusDeepLink` → /approvals?promo=&km= for review stages / /full-calendar?promo= for final; banners + clear on both targets), §8 sidebar-collapse verified (shared AppShell, no code change), §12 «Срок отчёта» + «Отправка смежным отделам» columns (`getReportSendStatus`) + export + mobile)
> Prev: 2026-06-29 (Promo Краткий промо-календарь client feedback V2 — Фаза A + §1/§13. Global `KmStatus` taxonomy rework — «Согласовано КД» / split «Не заполнено» + «Переотправлено на корректировку КМ» / +«Отменена» / removed «Согласовано ст. КМ» auto-flip; `ReadinessCell` → «Статус готовности акции» per-cell collapsible 5-segment proportional bar (§2/§3); single «Статус КМ по акции» filter with promo+cell filtering (§5); shortened status labels (§6a); sticky TOP horizontal scrollbar synced with the bottom (§1/§13))
> Prev: 2026-06-19 (Promo Краткий промо-календарь client feedback — 8 items: operational/control/distribution filters + date-range; collapsible «Распределение по категориям» block; merged «Общий статус / готовность акции» column; КМ columns last; bottom-only horizontal scroll + page-sticky header; per-campaign План-акций approval stages; CSV export; lock icons removed)

All UI is mock data — no backend or API integration exists yet. Architecture, structure, and conventions are in `CLAUDE.md` (root + per-project).

## Deployment

Both apps deploy to **GitHub Pages** via `.github/workflows/deploy.yml` (on push to `main` + manual run) under subpaths — Dashboard at `…/Texnomart/dashboard/`, Promo at `…/Texnomart/promo/` (no root landing page). Vite `base` (from `BASE_PATH` env), React-Router `basename` (from `import.meta.env.BASE_URL`), and a root `404.html` + `index.html` restore snippet handle the subpath + SPA deep-link routing. One-time manual step: repo Settings → Pages → Source = "GitHub Actions". See root `CLAUDE.md` § Deployment.

**Demo login**: **Broker Dashboard** — `admin@texnomart.uz` / `Texnomart2026`, email/password only, **no 2FA**, login always succeeds (one click → dashboard). **Promo** — теперь тоже без 2FA; проверка по стору (`users-store.ts`); 5 попыток / 15-мин. блокировка. Демо-учётки: `admin@texnomart.uz` / `Admin2026!` (Администратор, сразу в приложение) и `newuser@texnomart.uz` / `Temp1234!a` (демонстрирует поток принудительной смены пароля — перенаправляет на `/change-password` перед входом в приложение).

## Project Status

| Project | Status |
|---|---|
| **Broker Dashboard** | All 14 prompt pack pages complete, 22 routes. Prototype stage (mock data). |
| **Texnomart Promo** | Bootstrap + Master shell + **S1 (Краткий промо-календарь)** complete; **S2 (Полный промо-календарь) complete (Phases 1–5)**; **S3 (Согласование и проверка) complete (Phases 1–3)** — runnable app (`pnpm dev:promo`): auth, 7-module nav, 9-role switcher, Promo primitives, seed mock data, S1 short calendar, S2 editable Pattern F grid (inline editing, live validation, остаток ✏️ + warehouse popover, installment auto-recalc, gift conditional fields, «В рекламу» bulk-select, functional draft/submit action bar; nomenclature entry via 1С Command picker + duplicate check; Excel/CSV import + 1С availability banner/re-check/submit gate; unplanned-campaign creation/integrate + edit-until-first-send + mobile per-line Sheet), and the **S3 approval workspace** at `/approvals` (+ `/approvals/:id`): role-aware review queue with a working-days SLA timer (Pattern C table → Mode-B cards) + «Не участвует»/«Авто-передано» tags, a split-view detail with per-line/bulk Принять·Отклонить (`ReasonDialog` comment), status transitions (Старший КМ → КД auto-forward → «Принято КД»; any line rejected → whole КМ set returns to «Не заполнено»), the full **«Не участвует»** lifecycle (КМ «Мои участия» raises → Старший КМ → КД; КД sets it directly), **live auto-escalation** to КД after the 2-working-day breach, a non-blocking **просрочка** note, a per-campaign **advance gate**, review comments surfaced in the **`VersionHistoryDrawer`**, and a mobile fixed action bar. **S4 (Управление изменениями) — complete (Phases 1–3)**: Phase 1 — the three-view `VersionHistoryDrawer` (Только изменения / Полный актуальный отчёт / История версий) with a per-field diff, opened from a per-campaign «История» button; Phase 2 — edit-after-approval (edits to an approved campaign are diffed against a baseline → amber changed-cell highlight + «N изм. после согл.» badge + a «Черновик — не отправлено» drawer section), «Ожидает повторного согласования маркетинга» routing (§11.8; additions skip it), Сотрудник маркетинга «Согласовать (маркетинг)» → КД «Согласовать и отправить смежным отделам» appending a live new version (incremental send), and period change (§11.5: КД «Изменить период» → bold + ✏️, re-approval re-triggered); Phase 3 — **campaign cancellation** (КД, required reason → separate `cancelled` state + «Отменена» + red/struck band + «Отмена» version + departments notified), **line exclusion** (КМ requests with reason → КД re-approves → «Исключена из акции», incremental notify), **«Скрыть отменённое»** FilterBar switch (ON by default, hides cancelled campaigns + removed lines), and **deadline change** (§4.7: КД initiates with reason → Операционный директор approves → `fillDeadlineOverride` effective, logged in the drawer). **S5 (Отчёты для смежных подразделений) — complete**: a read-only, versioned `/reports` screen — three role-gated department views (Маркетинг / Закуп / Аналитика — Tabs on sm+, Select on mobile) + a campaign picker; per-department Appendix-C M/P/A field subsets (table → Mode-B cards below md); versioning («получено» + version + 17-кал.-дн. overdue marker + «История версий» drawer + «Только изменённые» highlight); ознакомление («Ознакомиться со всеми»/per-line clears the highlight, explicitly ≠ согласование, §11.7); and the marketing-only editable «В рекламу (выбрано маркетингом)» checkbox + bulk-select + «Согласовать выбор (маркетинг)» (§7.2) with a §11.8 re-approval reminder. **S6 (Центр уведомлений) — complete**: a `/notifications` center + a live top-bar bell — a `NotificationsProvider` mounted above the AppShell so the bell, the sidebar «Уведомления» badge, and the page share one read/unread store (acknowledging updates all counts live); ~8 seed notifications across 6 types (новые/изменённые данные · отмена акции · удаление позиции · повторное согласование маркетинга · назначение КМ · утверждение «В рекламу») with role-based visibility (§11.3.1), date grouping («Сегодня»/«Вчера»/дата), a «Непрочитанные» section over a muted «Прочитано» section, a type filter, per-item + bulk «Ознакомлен», and quick links to the campaign/report; the shared AppShell bell «Показать все» now navigates to `/notifications` (additive optional `notificationsHref`). **S7 (Настройки типов промо) — complete**: a role-gated two-pane `/promo-types` settings screen where КД/Администратор define which full-calendar fields are required per promo type as reusable rules — a `PromoTypesProvider` (useReducer) above the list + `:ruleId` routes, a list (search/create/copy/status badges) + an editor (name, promo-type chip multi-select, category-grouped required-field checklist derived from gridFields, §9.3 effect-preview note); workflow Черновик→На подтверждении→Утверждено (+Архив, no hard delete) with КД-only confirmation, §9.5 re-confirmation when an approved rule is edited, and a per-rule change history; КД edit+confirm / Администратор edit / others read-only (S7 builds the settings screen + effect explanation only — it does not rewire the full-calendar required-field validation; optional follow-up). **S8 (Аудит-лог) — complete**: a read-only `/audit` screen (all roles, spec §11.9) with two tabs — **«Аудит-лог»**, a dense filterable action log (~21 reconstructed events: mono ids, tabular date+time, tinted action chip across 8 action types, object cell across 4 object types, статус до→после `PromoStatusBadge` pair, comment; пользователь/роль/тип действия/объект/диапазон-дат filters; sticky-header table → Mode-B cards + deferred «Фильтры» Sheet below md), and **«Свод контрольных событий»**, a per-campaign milestone timeline (план → отправка данных КМ → согл./откл. старшим КМ → согл./откл. КД → «Не участвует» → отправка отчёта; +отмена) rendered horizontal on md+ / vertical on mobile with red overdue nodes (`OverdueTag` + responsible КМ) on the КМ-fill (start − 21 кал. дн.) and report (start − 17 кал. дн.) deadline breaches + a summary strip (campaign count · overdue events · avg approval time in рабочие дни). The log + timeline are reconstructed from the existing seeds and are seed-stale. **The Promo prompt pack S1–S8 is now fully built.** See `Promo/CLAUDE.md`. |

## Known Issues & TODOs

- [ ] Geography map uses SVG placeholders — needs Yandex Maps API
- [ ] Dark mode untested (CSS variables defined) — **Promo dark theme now complete + QA'd (sub-project B, 2026-06-30)**; this item now refers to **Dashboard** only (its light mode is untouched, dark not yet built/verified)
- [x] Applications table: pagination (10/20/50/100), real filters (Статус/Партнёр/Филиал Selects), sortable columns (Создано/Сумма/Статус), and search — done 2026-06-15 (Kanban view removed; table-only per feedback)
- [x] Applications filters are now real (other list pages — clients/users/partners — already had real filters)
- [ ] `ApplicationDetailDrawer.tsx` is legacy — remove (note: `KanbanView.tsx` removed 2026-06-15)
- [ ] No runtime i18n system — language selector is display-only. Dashboard now offers RU + O'zbek (Лат.) only (Cyrillic removed per feedback, header + Settings i18n table + Profile selector); Promo keeps all three
- [ ] MUI Material in dependencies but unused — remove
- [ ] `globals.css` is empty
- [ ] No tests
- [ ] No API layer
- [ ] ApplicationsPage list still needs mobile Pattern K (card list below md) — the 2026-06-15 rework kept one horizontally-scrolling table, per the "display only as a table" feedback
- [ ] Detail pages other than Applications still use sticky action bars (can float mid-screen on short tabs); Applications uses the flex-column fixed-footer layout — consider converting the rest for consistency
- [x] **Promo — раунд фидбэка #3, под-проект #0+A (auth & accounts, 2026-06-29)**: стор пользователей (`users-store.ts`, localStorage `promo:users`) с ≥2 активными администраторами + guard «≥2 рабочих администратора»; вход без 2FA — email+пароль проверяются по стору, `/login/2fa` и `Login2FAPage.tsx` удалены; временный пароль при создании пользователя + `mustChangePassword` → `/change-password` (`ForcePasswordChangePage` + `NewPasswordForm`); экран управления учётками `/users` (только Администратор: список + создание / сброс / назначение-отзыв прав / деактивация-активация); текущий пользователь в шапке (`current-user-context.tsx`, sessionStorage `promo:current-user-id`); живой аудит модуля учёток (`audit-store.ts`, localStorage `promo:audit-live`) — объединяется с S8-сидами, 5 новых типов действий. Mock-ограничения: localStorage на браузер (не бэкенд), пароли-строки (не хэш), временный пароль показывается на экране (email нет), управление гейтировано по активной роли god-mode переключателя; только действия модуля учёток в живом аудите.
- [x] **Promo — раунд фидбэка #3, под-проект B (тёмная тема, 2026-06-30)**: full working dark theme. **Infra** — `theme-context.tsx` (`ThemeProvider`/`useTheme`: `"light"|"dark"|"system"`, persists `promo:pref-theme`, live `prefers-color-scheme` listener), inline boot script in `index.html` (applies `.dark` before React paints → no FOUC), theme-aware `<Toaster>` in `App.tsx`, `SettingsPage` toggle wired to the provider. **Palette** (`styles/theme.css`) — rewrote `.dark` as a layered neutral scale (background darkest, card/popover/sidebar lighter), **kept brand `--primary #FFD60A` / black fg** (not stock shadcn near-white), `.dark` `--status-*` tuned, brand-yellow active sidebar item. **Shared (additive)** — `ThemeController` type + optional controlled `theme` prop on `AppShell` (provided ⇒ header ↔ Settings share the provider; omitted ⇒ Dashboard unchanged) + `dark:` variants on the pattern components Promo uses; dropped the dead `hover:${bg}` in `StatusBadge` (shadcn Badge hover is `[a&]`-gated). **Screens** — ~270 hardcoded light colors across 49 files migrated hybrid (semantic tokens + `dark:` variants; brand `#FFD60A` → `primary` token; soft tints → `dark:bg-X-500/15 dark:text-X-300`; solid `-500` fills left), primitives by hand + 6 parallel agents for the folders + the 3 data tint-maps in `promo-mock-data.ts`. Verified: `build:promo` + `build:dashboard` pass; negative-lookahead greps clean (no brand literals besides one intentional `accent-[#FFD60A]`, no inline colors, no unpaired light utilities); in-browser dark walk of all routes at 1920/390px; no-FOUC + persistence + header/Settings sync + light-mode regression all pass. Mock limits: prefs per-browser (localStorage); «Системная» follows the OS; Dashboard dark out of scope.
- [x] **Promo — раунд фидбэка #3, под-проект C (Профиль/Настройки, 2026-06-29)**: персональные экраны `/profile` + `/settings` (всё гейтится по активной роли). **`/profile`** (`profile/ProfilePage.tsx`, Pattern-J Tabs): «Основная информация» — аватар-инициалы + **редактируемое ФИО** (`updateUserName` → `useCurrentUser().refresh()` обновляет шапку вживую), read-only Email (= логин) + блок «Учётная запись» (роль · статус · дата создания · последняя смена пароля); «Безопасность» — **добровольная смена пароля** через переиспользованный `NewPasswordForm` (расширен опциональным `verifyCurrentPassword` + полем «Текущий пароль»; неверный текущий → inline-ошибка без записи, верный → `updatePassword`) + примечание «вход без 2FA». **`/settings`** (`settings/SettingsPage.tsx`): «Язык интерфейса» (RU / O'zbek Кир. / O'zbek Лат. — display-only, `promo:pref-language`), «Тема оформления» (Светлая/Тёмная/Системная — переключает класс `.dark`, `promo:pref-theme`; полноценная тёмная тема = под-проект B), «Связанные настройки» (кросс-ссылки Уведомления / Настройки типов промо [КД·Админ] / Управление пользователями [Админ]). Data: `users-store.ts` += `updateUserName` + `lastPasswordChangeAt`. Audit: новые `AuditActionType` «смена пароля» / «изменение профиля» пишутся живьём через `appendAuditEvent`. `routes.tsx` += `/profile` + `/settings`. **Shared не менялся** (дропдаун уже ссылался на эти роуты). Mock-ограничения: prefs на браузер (localStorage); язык display-only (нет i18n); тёмная тема дорабатывается в B; пароли строками (прототип); роль/права тут read-only (матрица — D).
- [x] **Promo — раунд фидбэка #3, под-проект D** (матрица прав, 2026-06-30): read-only **«Матрица прав»** screen at `/permissions` (nav-gated + in-screen guard to **КД + Администратор**) — the last 3rd-round sub-project, **round complete**. **Документ:** prompt-pack **Appendix D** rendered as a matrix — new canonical `Promo/src/lib/permissions.ts` holds `ACCESS_LEVEL_META` (4 levels, soft tints light+dark), `ACCESS_AREAS` (5 areas), `ACCESS_MATRIX` (9×5 cells `{level, note}` with the spec's own wording), `FINAL_APPROVAL_NOTE` (§11.1). **Консолидация:** `CAPABILITIES` (17 granular actions in 5 groups) with `allowed(role)` predicates **derived LIVE from the real gating helpers** (`getFullCalendarAccess`/`getReportAccess`/`getPromoTypeSettingsAccess`/`canCancelCampaign`/`canRequestLineRemoval`/`canApproveLineRemoval`/`canManageDeadline`/`canApproveDeadline`) + `enforcedIn` code pointers + `rolesWithCapability()` — existing helpers **unchanged** (still the enforcement points; zero regression to S2–S8). **Screens** (`Promo/src/app/components/permissions/`): `PermissionsMatrixPage` (PageHeader + active-role line + guard + audit-style Tabs), `AccessMatrixTable` (sticky-role-column `<table>`, active row brand-tinted + «Вы», level chips w/ note tooltips → per-role cards below md; exports `AccessLevelLegend`), `CapabilityList` (grouped, allowed-role chips, active role brand). Wiring Promo-local only (`routes.tsx` += `/permissions`; `shell-config.tsx` += `KeyRound` nav item «Система» + breadcrumb) — **no shared change**. Mock limits: read-only document (no editing — gating is code-level, no enforcement backend); §11.1/plan-chain/КМ-review actors are spec-documented predicates (no dedicated `canX` helper for those). Verified: `build:promo`; in-browser light/dark + mobile + access guard.
- [x] **Promo — Полный промо-календарь client feedback (2026-07-01, 14 items, COMPLETE)**: applied to `/full-calendar` in 4 phases (all builds green). **§1/§2** synced sticky TOP scrollbar + sticky header (short-calendar Pattern-F scroll; the scroll ancestor is the page's internal `overflow-auto`, so `top-0`, no `-top-4`); **§9** darker-gray bold header + `border-r` column dividers; **§12** unified alignment (text left / numbers·money·%·dates·checkboxes·badges centered, money not bold — `EditableCell` gained `align="center"`); **§4** Тип/Название/**Период акции** (2 date columns merged into one) as columns, band simplified to «PR-… · N позиций» + actions, **дедлайн chip + S4 «Изменить/Утвердить дедлайн» buttons removed** from the full calendar (data model + short-calendar «крайний срок» kept); **§13** filter-aware «Показано: N промо · M позиций» counter + flat CSV **«Экспорт»** (`buildFullCalendarCsv`, № промо repeated per line); **§3** per-line **«Изменить»** (per-line Sheet, all sizes) + **«Удалить»** (hard delete, drafts) in the frozen pane, `delete` reducer, autosave indicator (no «Сохранить черновик»), editability status-gated via `isCampaignFreshEditable` (fresh add/edit/delete · approved inline correction · under-review read-only); **§5/§6** «Бренд» + «Наличие в магазинах, %» columns before Остаток (`NomenclatureItem.brand`, `ACTIVE_STORE_COUNT`/`getStoreAvailability`/`formatAvailabilityPct` — active-stores directory, central warehouses incl. «Ташкент — Центральный» excluded, «94,87%»); **§7** plain КМ sees only own promos/lines (`ownKmId` = representative КМ; Старший КМ/КД/Администратор keep the full view + КМ filter); **§10** searchable multi-select «№ промо» (`PromoNoFilter`, full PR-/UN-) + «Период акции» date-range added, others kept; **§11** all 5 column groups ON by default; **§14** create dialog defaults to «Выбрать плановое промо» (auto-pull тип/название/период preview + integrate); **§8** gifts reworked: `PromoLine.giftNomenclatureId`/`giftStock` → `gifts: GiftItem[]`, new promo type «Подарок на выбор» (`giftChoice`), fixed **«Подарок №1»/«Подарок №2»** columns (номенклатура + наличие % + остаток; last two from 1С) and **choice sub-rows** (one gift/sub-row, main nomenclature + non-gift columns merged, «+ Добавить подарок», per-gift remove; variable line height computed once, applied to both Pattern-F panes) — rippled through `missingRequiredFields`, S4 `TRACKED_FIELDS`, S5 `reportFields`, the S7 rule seed (`gift1Nomenclature`), `buildFullCalendarCsv`, and the mobile `LineEditSheet` (`GiftEditor`). Mock limits: §7 representative КМ (no per-person identity); store-availability + gift 1С numbers are deterministic mock; export reflects only visible/filtered rows; **in-browser visual QA still pending** (no browser tool in the build session — run `pnpm dev:promo` to confirm the §1/§2 sticky+synced scroll and §8 «подарок на выбор» merged sub-rows). Promo-local only (shared untouched → Dashboard unaffected).
- [x] **Promo — Согласование client feedback (2026-07-02, 10 items, COMPLETE)**: applied «Комментарии по промо-календарю, раздел согласование» to the **S3 «Согласование и проверка»** module in 4 phases (all `build:promo` green + in-browser QA). **§1** «Срок согласования» Select (Все / Требуют решения сегодня — deadline today OR overdue / Только просроченные) replaces the «Только просроченные» toggle; **§2** «№ промо» (`PromoNoFilter`) + «Период акции» date-range filters; **§3** «Статус согласования» stage Select + **broadened visible queue** (`visibleReviewQueue` — Старший КМ + КД see both stages, acting still gated by `effectiveReviewer`/`canAct`); **§4** «КМ» is a multi-select (`KmMultiSelect`) persisted per user (`promo:approvals-km-filter:<id>`); **§5** «Плановое/внеплановое» Select; **§6** `ReviewQueueTable` rewritten to the calendar band layout (page-sticky header + synced top/bottom scrollbars + dividers, single pane — no frozen columns); **§7** SLA shows «N раб. дн. (до DD.MM.YYYY)» / «+N дн. просрочено»; **§8** auto-escalated items display «На согл. у КД» + «Авто-передано: просрочка у старшего КМ» (`displayKmStatus`; Старший КМ can't act); **§9** stage-separated SLA (`stageSlaStart`/`itemSla` + `ReviewItem.kdStageStartedAt`; КД stage counts from the auto-forward moment) + senior-overdue on the card & История (`autoForward` drawer prop); **§10** КМ «Мои участия» reworked — own-promos-only (representative КМ), card fields + «SLA КМ» (seeded `kmSubmissionSla`) + 4 filters + «Заявка о неучастии отправлена» → «Не участвует» transition. New: `KmMultiSelect.tsx`; helpers `visibleReviewQueue`/`reviewStageOf`/`stageSlaStart`/`itemSla`/`seniorOverdueInfo`/`displayKmStatus`/`kmSubmissionSla` + `ReviewItem.kdStageStartedAt` + the «Заявка о неучастии отправлена» badge tint. Promo-local only → Dashboard/shared untouched. Mock limits: no per-person КМ identity (representative КМ; Старший-КМ «their КМ» = all КМ, narrowable via the multi-select); «SLA КМ» + review seeds are deterministic mock; КМ-filter persistence needs a logged-in user (no-ops in an auth-bypass session, seeds on login).
- [x] **Promo — 5th-part feedback, sub-project E-2b «Уведомления» per-role config (2026-07-06, COMPLETE)**: built the original PDF «Уведомления» asks E-2 didn't cover (the user requested it after E-2), via subagent-driven development (7 tasks + 1 review fix; per-task spec+quality review; whole-branch opus review; full in-browser verification). **Built:** `promo-mock-data.ts` — `RoleNotificationConfig` type, `notificationsForRole(role,list,config?)` (Администратор short-circuit; config → filter to the role's categories; no config → old `visibleTo` fallback), `notificationLinksFor(n)` (per-category deep-links, report link gated on `getSentCampaigns()`, `[]` if no campaignId); new `Promo/src/lib/notification-settings-store.ts` (localStorage `promo:notification-role-config`; `DEFAULT_ROLE_CONFIG` = **faithful inversion of the pre-E-2b audiences → no visibility regression**; getRoleConfig/persistRoleConfig/resetRoleConfig/rolesForType); new `NotificationSettingsProvider` (mounted ABOVE the AppShell, wrapping `NotificationsProvider`) + `useNotificationSettings()`; new **Администратор-only** `NotificationSettingsPage` at `/notification-settings` (nav-`roles` gate + in-screen guard; 9 role blocks × 6 category Switches, Администратор block disabled+on «видит все уведомления»; «Сбросить к умолчаниям»); «Система» nav item (icon `BellRing` — `BellCog` absent from the installed lucide) + breadcrumb; `AppShell` + `NotificationsPage` filter the bell + list via the live config (`notificationConfig`, aliased in AppShell to avoid the shell-`config` collision); `NotificationItem` «Роли: A, B +N» tag (`rolesForType`, active role brand-highlighted) + `notificationLinksFor` deep-link buttons (retired `LINK_LABEL`/`n.href`); `ReportsPage` `?promo=` mount-time pre-select. **Verified:** `build:promo` + `build:dashboard` green; in-browser (settings 9×6 with disabled admin block; toggle a category off → persisted → that role's bell/list filter it out **across full reload**; «Роли:» tag + all 3 deep-links; `?promo=UN-2026-015` [sent] pre-selected it, `?promo=PR-2026-001` [not sent] kept default; Администратор sees all; reset restores). Opus review READY-WITH-FIXES → 1 Important fixed (`notificationLinksFor` now gates «Открыть отчёт» on sent campaigns — a non-sent `data-changed` no longer silently opens a different report; `visibleTo` JSDoc updated). **Mock limits:** role-config per-browser localStorage; the «для роли X» tag shows the full configured target set — per-user multi-role filtering is deferred to **E-4**; Администратор bypasses the config; `?promo=` on `/approvals` can soft dead-end on an empty queue for sent/cancelled campaigns; the tag uses full role names (spec's `ROLE_LABEL_SHORT` consciously dropped in the plan). With E-2 + E-2b, «Уведомления» is fully covered. Spec/plan in `docs/superpowers/{specs,plans}/2026-07-06-promo-notifications-per-role-config*`.
- [x] **Promo — 5th-part feedback, sub-project E-2 «Уведомления» live + persistent (2026-07-06, COMPLETE)**: reworked the **S6 `/notifications`** module so notifications are **live** (emitted from 5 existing S4/S5 action handlers) and **persistent** (localStorage). **Scope divergence to note:** no discrete client items were in the session docs (the E-series memory's detailed E-2 asks weren't loaded into context), so the scope was **proposed+approved in brainstorming** as **Live + persistent + toast** with deliberately lean UX — this does **not** cover the original PDF E-2 items (per-role approval-workflow notification sets, admin per-role blocks, multi-role dedup «для роли X», «Открыть промо/согласование/отчёт» deep-links), which remain a possible follow-up (**E-2b**). **Built:** additive data layer (`NotificationInput` + `notificationAudienceFor` + pure `createLiveNotification`); new `Promo/src/lib/notifications-store.ts` (localStorage — `promo:notifications-live` cap 50 + `promo:notifications-read:<userId>` per-user, anon fallback; mirrors `report-ack-store.ts`); `NotificationsProvider` reworked into a persistent store exposing `notify(input)` (merges seeds+live newest-first, applies per-user read-set at load, one SPA-safe sonner toast, **no toast on load**; `acknowledge`/`acknowledgeMany` persist reads); live emission wired into `FullCalendarPage` (`onCancelConfirm`/`onApproveRemoval`/`onPeriodApply`/`onSendToDepartments`) + `DepartmentReportView` (`marketingApprove`), each replacing its own `toast.success`. The shared-shell bell count already updated live → **no `@texnomart/shared` change**; no bell animation (shared constraint — toast is the cue). **Verified:** `build:promo` + `build:dashboard` green; in-browser end-to-end (КД cancel → toast + bell 4→5 + persist across reload; ack → bell 5→4 + read persists; no toast on fresh load); opus whole-branch review READY-WITH-FIXES → 1 Important fixed (ad-approval copy «Категорийные менеджеры уведомлены» dropped to match its marketing-only audience + `ntf-07` seed), 2 Minor accepted (neutral `toast(...)`; role-switch memo churn). **Mock limits:** per-browser localStorage; read-state keyed to the user at provider mount; live list capped at 50; emission only from the 5 wired handlers; `km-assignment` seed-only. Spec/plan in `docs/superpowers/{specs,plans}/2026-07-06-promo-notifications-live-persistent*`.
- [x] **Promo — 5th-part feedback, sub-project E-1 «Отчёты смежным отделам» (2026-07-06, COMPLETE)**: the 5th-part feedback («Отчёты · Уведомления · Аудит-лог · Управление пользователями») is scoped as **four Promo-local sub-projects** — **E-1 Reports (done) → E-2 Notifications → E-3 Audit (2→4 tabs) → E-4 Users (+ КД temp-substitution)** — each own spec→plan→build. Cross-cutting decisions: **identity = deterministic seeds** (keep the god-mode role switcher + representative КМ); **export = real `.xlsx` via SheetJS** (added `xlsx` to Promo). **E-1** reworked S5 `/reports` per §1–§3, via subagent-driven development (9 tasks, per-task review+fix loop, whole-branch opus review). **§1** columns projected from full-calendar `gridFields` per dept (`reportFields.ts` rewrite; Marketing gains Бренд/Наличие %; `GROUP_OVERRIDE` for Компенсация/Подарки); collapsible **«Фильтры»** (`ReportFilters.tsx` — one typed filter per visible column + synthetic Изменение/Ознакомление) + «Сбросить фильтры» + «Показано: N»; real **`.xlsx` «Экспорт»** (`report-xlsx.ts`, SheetJS) of the visible/filtered rows. **§2** `ReportChangeSet.changedCells → ReportCellChange[]` + `removedLineIds`; **«Изменение»** column (Добавлено/Изменено/Исключено, excluded kept+struck via `removedLineIds`) + «Было→Стало·дата» Tooltip; three **version-total** counters + «Всего позиций» (unfiltered `totalCount`); «Только изменения»; per-version read-only snapshots (`getReportSnapshot`) + «Текущая версия» via the shared `VersionHistoryDrawer` additive `snapshotFor` (approvals + full-calendar consumers unaffected); **per-user acknowledgement** (`report-ack-store.ts`, localStorage `promo:report-ack`, keyed campaign+dept+version+line+user) + «Ознакомиться со всеми изменениями (N)»; role-gated **«Кто ознакомился»** drawer (`ReportAcknowledgeDrawer.tsx`). **§3** table rewritten to the **band-layout Pattern-F split pane** (frozen «Изменение»+«Номенклатура», sticky header + synced top/bottom scroll re-measured on dept switch + `border-r`/`border-b`) → Mode-B cards below md. Verified `build:promo` + `build:dashboard` green; scope Promo-local + root `pnpm-lock.yaml` only. **In-browser visual QA still pending** (built in a no-browser session — run `pnpm dev:promo`). Mock limits: per-browser localStorage ack; seeded roster + per-version snapshots; the current god-mode user isn't a seeded roster member, so the acknowledge→who-acknowledged loop shows only seeded roster users; `.xlsx` cells are pre-formatted strings. Spec/plan in `docs/superpowers/{specs,plans}/2026-07-03-reports-adjacent-departments-rework*`.
- [x] **Promo — Краткий промо-календарь «6-я часть» client feedback (2026-07-10, COMPLETE — 7 items)**: the «Комментарии общие» PDF (08.07.2026) — the тракер's «🆕 новое» items **V2·14–18** (were ⛔ «ждёт корректировки ТЗ») + two new asks — applied to the **S1** «План акций» tab. Reworked `PlanMode` around a **per-row send lifecycle** `Черновик → выбрать → Отправлено → Согласовано/Отклонено` layered over the «8-я часть» reviewer decisions; the seed splits by `getPlanApproval` (campaigns with a `PLAN_APPROVALS` record → «Отправлено», rest → «Черновик»). **№1** free-text «№ промо» → read-only auto number (`nextPlanPromoNo`). **№2** тип optional at creation → «Черновик» (ред./удал.); red «Тип не выбран» marker (`typeMissing`) + «Черновиков без типа промо: N» banner; typeless drafts excluded from the send pool (тип required to send). **№3** «Директор маркетинга» added to `/promo-types` nav roles + `getPromoTypeSettingsAccess` case (`canEdit:true`, `canConfirm:false`). **№4** fixed the rejected-plan stepper bug (`actorForPlanStatus("Отклонён")=undefined` → all stages green ✓) — `rejectedStage` → red ✗ «— отклонил» at the rejecting stage + per-row «Отклонено». **№5** explicit «Отправить на согласование (N)» (drafts не авто-sent at creation). **№6** reused create/edit dialog + `overrides`/`deletedIds` → drafts editable/deletable; editing a **sent** row (`handleEdit`) reverts it to «Черновик» + clears its decision → re-send (mirrors S4/S7). **№7** send-stage checkbox column (`rowCheckable`) + «Отправлено» excludes re-send; new pure `findCoverageGaps(periods)` → **coverage-gap hint** (amber banner + warning toast). `PlanApprovalTable` gained the «Статус строки» `RowLifecycleBadge`, `TypeCell` marker, send/review `selectable` modes, and a marketing Изменить/Удалить column. 4 files (`PlanMode.tsx`, `PlanApprovalTable.tsx`, `promo-mock-data.ts`, `shell-config.tsx`), Promo-local; `build:promo` green + in-browser QA (Дир. маркетинга/КД, 1440/390px): №1 «PR-2026-017» read-only, №2 typeless «Тип не выбран» + banner + no checkbox, №5/№7 select-all→«Отправлено» + gap toast, №4 КД reject → red ✗ stepper, №6 edit sent → «Черновик» + pool 9→10, №3 «+ Создать правило» visible. **Mock limits:** plan state session-local (reload reseeds); gap rule = uncovered days within the promos' min→max span; plan-level status shared, per-row send/decision state on top. See HISTORY.
- [x] **Promo — Краткий промо-календарь «8-я часть» client feedback (2026-07-09, COMPLETE — 5 items)**: applied «Тест первой MVP промо-календаря» to **S1**. **Context:** screenshots are from `promo.tm.uz` (backend-integrated deploy), **not this mock repo** (GitHub Pages) — the plan-approval UI there is a server rewrite absent here; confirmed to **fix in the mock repo** (backend mirrors). **№4** `getReportSendStatus` overdue only on actual late send; «Срок отчёта» date-only, «Отправка смежным отделам» fact-only («Не отправлено» / «Отправлено · дата · в.N» + «+N дн.» sent-late), «отчёт просрочен» removed (`ShortCalendarTable` + mobile card + `promo-export`). **№3 (+№1+№2)** `PlanApprovalTable` `selectable` mode (leading checkbox col + «Выбрать все» + per-row «Согласовано/Отклонено» badge); `PlanMode` per-row decisions per reviewer stage (КД/ОД) + «Согласовать/Отклонить выбранные (N)» strip + «Согласовано N из M на этапе X» — approve one/several/all, advance only when all approved, reject→«Отклонён»→«Вернуть на доработку» resets; same table shows № / тип / название / период (covers №1+№2). **№5** new `isPlanApprovedByDirectors(c)` gates the calendar for **Категорийный менеджер (КМ)** → only fully-approved plans (fixes «Найдено 0 акций»). 6 files, Promo-local. `build:promo` green + in-browser QA (КД/ОД/КМ, 1440/390px): №4 zero premature overdue + 1 sent-late tag; №3 partial 2/15 stays → select-all advances КД→ОД → reject→return; №5 КМ «Найдено 5 акций» (26-1/2/3/5/7), others 15. **Mock limits:** plan decisions session-local (reload → «На согл. с КД»); КМ visibility keys off seeded `PLAN_APPROVALS` (line-less 008–016 hidden from КМ); plan tab itself not КМ-gated (only the calendar view, §5). See HISTORY.
- [ ] **Promo**: prompt pack S1–S8 fully built (S1 short calendar + S2 full-calendar + S3 approvals + S4 change-management + S5 department reports + S6 notifications + S7 promo-type settings + S8 audit log). No module-screen placeholders remain. **5th-part feedback: E-1 (Reports) done; E-2 (live+persistent) + E-2b (per-role config) done → «Уведомления» fully covered; E-3 (Аудит-лог, 2→4 tabs) done → «Аудит-лог» fully covered; only E-4 Управление пользователями (+ временное замещение КД) remains.**
- [x] **Promo — 5th-part feedback, sub-project E-3 «Аудит-лог и контроль сроков» (2→4 tabs, 2026-07-07, COMPLETE)**: reworked the **S8 `/audit`** screen (renamed «Аудит-лог и контроль сроков»; focus deadlines/responsible/overdue, not campaign statuses) into **4 tabs**, via subagent-driven development (11 tasks / 5 phases, per-task review + whole-branch opus review + one fix wave + in-browser QA). **New pure `Promo/src/lib/audit-control.ts`** — a `ControlPoint` model + `buildPlanControlPoints` (from `PLAN_APPROVALS` + a seeded PR-2026-006 correction chain) / `buildPromoControlPoints` (from `buildReviewItems`/`seniorOverdueInfo`/`stageSlaStart` + report helpers; auto-forward attributes the overdue to Старший КМ) / `buildParticipantMetrics` + `buildParticipantTasks` (aggregate the SAME points → reconciled) / `timelinessBand`. **Tab 1 «Сроки по плану»** + **Tab 2 «Сроки по промо и отчётам»** share `ControlDeadlinesTable` (band-layout: sticky header, synced top/bottom scroll, gridlines → Mode-B cards) + `ControlDeadlinesFilters` (период/№/ответственный/роль/точка/результат incl. «Только просроченные»); the old `ControlEventsTimeline` deleted. **Tab 3 «Показатели участников»** — role selector (КМ ranked / director roles aggregated), rating columns (место·ФИО·дедлайн·вовремя·просрочка·своевременность%+band·ср.просрочка·возвраты·повторные) + `ParticipantTasksDrawer` drill-down + «Справочно» (post-approval counts out of the rating). **Tab 4 «Аудит-лог»** — «Ключевые действия»(default)/«Все действия»(Админ-only) toggle over `NON_KEY_ACTIONS`. Page-level date-range/role/reset on the deadline tabs only. **Access:** КМ own rows only (Tabs 2–4; representative `OWN_AUDIT_KM_ID="km-3"`) + plan-tab notice; all other roles see all; «Все действия» Админ-only. Opus review READY-WITH-FIXES → fixed the reconciliation breaks the isolated per-task reviews missed (Старший КМ blank / КД 0% / columns not summing / Tab 3 access-blind / inert page-filters / dead `canSeeAll`); re-reviewed → READY. Verified `build:promo` + `build:dashboard` green + in-browser (Администратор + КМ). **Mock limits:** seed-stale (in-session actions elsewhere not appended); no per-person identity beyond `CATEGORY_MANAGERS`; senior-overdue days = SLA constant; КМ timeliness skews «Низкая» for km-3 (bands shown via Старший КМ 20% / КД 50%). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-07-promo-audit-control-deadlines*`.
- [x] **Promo — Краткий промо-календарь follow-ups (2026-06-29)**: post-V2 tweaks. **(1)** The §7 «Фильтры» toggle moved from a row above the table into the shared `PageHeader` `actions` (beside the title, calendar tab only); order set to **«Фильтры → Экспорт»** by disabling the built-in `PageHeader` export (`showExport={false}`) and re-rendering the «Экспорт» dropdown inside `actions` after the filter button; the filter button widened (`min-w-[140px]` + `justify-between`). **(2)** The short-calendar seed grew **7→16 planned campaigns** (added PR-2026-008…016 to `CAMPAIGNS` — varied types/statuses/КМ + per-КМ statuses spanning the taxonomy + `categoryDistribution` on most; **no `PROMO_LINES`** so they stay out of the full calendar/reports, render «—» plan stages, add a few approvals/audit items) → the calendar shows **15 rows** (PR-2026-004 cancelled+hidden). Verified `build:promo` + in-browser (15 rows, header order/width).
- [x] **Promo — Краткий промо-календарь client feedback V2 (2026-06-29, COMPLETE — all 13 items)**: second feedback round (13 items), phased A→D — all done. **Фаза A (§2/§3/§5/§6a) + §1/§13.** Global `KmStatus` taxonomy reworked (§5): «Принято КД»→«Согласовано КД»; «Согласовано ст. КМ (ожидает КД)» removed (auto-flip to «На согл. у КД»); «Не заполнено / Ожидание корректировки» split into «Не заполнено» + «Переотправлено на корректировку КМ»; +«Отменена» (auto on full cancellation) — propagated through S3/S8/badges/export/readiness; `aggregateKmStatuses`/`CampaignReadiness` split «на согласовании» into `atSeniorKm`(yellow)+`atKd`(orange), denominator excludes «Не участвует»+«Отменена». `ReadinessCell` → **«Статус готовности акции»** per-cell collapsible **5-segment proportional bar** (tooltips + expanded labeled blocks; §2 removes the status badge, §3); single **«Статус КМ по акции»** filter (6 statuses) showing only matching promos+cells (§5); shortened status labels (§6a); **sticky TOP horizontal scrollbar synced with the bottom** (§1/§13, reverses the bottom-only decision). **Фазы B–D (done):** §6b `formatPromoNo` → «26-N» № промо (short-calendar table/detail/export only; full calendar keeps `PR-`/`UN-`); §9 № промо free-text → searchable multi-select `PromoNoFilter` (Popover+Command checkbox, `promoId`→`promoIds[]`); §11 table compactness (`BASE_ROW_H` 104→80, header `h-12`→`h-10`, frozen `px-4`→`px-3`); §7 collapsible filter block («Фильтры» toggle + `countActiveFilters` badge + «Очистить» when collapsed); §10 clickable КМ-status badges → `kmStatusDeepLink` (review stages → `/approvals?promo=&km=`, final → `/full-calendar?promo=`; receivers search all items / focus the campaign + show a dismissible banner); §8 sidebar collapse verified (shared AppShell «Свернуть» — no code change); §12 `getReportSendStatus` → «Срок отчёта» (start−17 кал. дн.) + «Отправка смежным отделам» («✓ Отправлено … · в.N» / «Не отправлено» + «отчёт просрочен») columns + mobile card line + 2 CSV columns. Mock limits: §12 seed-stale (report-send from seeded version chains, not in-session edits); §10 no per-person identity.
- [ ] **Promo — Краткий промо-календарь client feedback (2026-06-19)**: applied the «Комментарии по краткому промо-календарю» (8 items) to S1 only. Filters split into Операционные/Контрольные/Распределение (`CalendarFilters.tsx`) with a period **date-range** + a КМ-status filter; the table (`ShortCalendarTable.tsx`) reorders to период → крайний срок → **collapsible «Распределение по категориям»** (День/дата once-per-group + Категория + Ответственный КМ as structured sub-rows, variable row height synced across both Pattern-F panes) → **merged «Общий статус / готовность акции»** (`ReadinessCell.tsx` — status + «X из Y», «Не участвует» excluded — replacing the old готовность+статус columns, §6/§7) → per-КМ columns last; **one bottom horizontal scrollbar + a page-sticky header** (vertical scroll stays on the main page; no top scrollbar, no internal table scroll), **full-bleed** width; lock icons removed (§4). «План акций» now shows a **per-campaign 3-director approval-stage table** (`PlanApprovalTable.tsx` — Озн/Отпр + «В срок»/«Просрочка +N дн.»/«Ожидает этапа», 63/60 кал. + 3 раб. дн SLA). **CSV export** per tab (`promo-export.ts`, filter-aware, BOM). New mock data: `categoryDistribution`, `campaignReadiness`, `PLAN_APPROVALS`/`getPlanApproval` (all invented/seeded). Mock limits: distribution + plan-approval are seeded illustratively; session-added План rows aren't exported; export access-gating is a no-op (exports only visible/filtered rows); sticky-header internal scroll engages only when the list exceeds the viewport
- [ ] **Promo S8** (original 2-tab build — **SUPERSEDED by E-3 «Аудит-лог и контроль сроков», 2→4 tabs, 2026-07-07**; kept for build history): `/audit` Аудит-лог (read-only, all roles, spec §11.9), two tabs. Tab 1 «Аудит-лог»: dense filterable action log (~21 reconstructed events; mono ids, tabular date+time, 8 action types as tinted chips, 4 object types акция/строка/отчёт/план, статус до→после `PromoStatusBadge` pair, comment) — пользователь/роль/тип действия/объект/диапазон-дат filters; sticky-header `Card` table → Mode-B cards + deferred «Фильтры» Sheet below md. Tab 2 «Свод контрольных событий»: per-campaign milestone timeline (план → отправка данных КМ → согл./откл. старшим КМ → согл./откл. КД → «Не участвует» → отправка отчёта; +отмена terminal) horizontal md+/vertical mobile, red overdue nodes (`OverdueTag` + responsible КМ) on the КМ-fill (start−21 кал.дн.) and report (start−17 кал.дн.) breaches, desktop notes legend, summary strip (count·overdue·avg раб.дн.). Mock limits: the log + timeline are **reconstructed from the existing seeds** (campaign statuses, version chains, review comments, cancellation/non-participation/report seeds) and are **seed-stale** — in-session actions on other screens are NOT appended; no per-person identity (role labels / first-participating-КМ name); the КМ-fill SLA (21 кал.дн.) is a mock value; the «отправка данных» date + its breach use the seeded version chain only (unseeded campaigns show the node without a date); avg approval time approximates КД-approval via the latest version date. **S8 complete — the prompt pack S1–S8 is done.**
- [ ] **Promo S7**: complete. `/promo-types` (+ `/promo-types/:ruleId`) — role-gated two-pane required-field settings: a `PromoTypesProvider` (useReducer; save/send/confirm/archive/create/copy) above the list + selected-rule routes; `RuleListPanel` (search/create/copy/status badges) + `RuleEditor` (name, promo-type chip multi-select, category-grouped checklist derived from gridFields `COLUMNS`/`COLUMN_GROUPS` + the 3 frozen identity columns, §9.3 effect-preview, gated actions, §9.5 change history). Workflow Черновик→На подтверждении→Утверждено (+Архив, no hard delete); «Утвердить правило» is КД-only; editing an approved rule knocks it back to Черновик (§9.5 re-confirmation). КД edit+confirm / Администратор edit / others read-only. **Scope:** settings screen + effect-preview explanation only — does NOT drive the full-calendar required-field validation (still the static `gridFields.required` flag); optional follow-up. Mock limits: in-memory only (reload reseeds); no per-person identity; the pending→approved step exists for workflow meaning though КД both edits and confirms; actions toast (no notification append). **S7 complete — next: S8.**
- [ ] **Promo S6**: complete. `/notifications` Центр уведомлений + live top-bar bell. `NotificationsProvider` mounted in `ProtectedLayout` above the AppShell so the bell, sidebar «Уведомления» badge, and page share one read/unread store (acknowledging updates all counts live — unlike the S3 below-shell badge). ~8 seed notifications across 6 types with role-based visibility (§11.3.1), date grouping, «Непрочитанные»/«Прочитано» sections, type filter, per-item + bulk «Ознакомлен», quick links. Shared additive change: AppShell `notificationsHref` wires the bell «Показать все» → `/notifications`. Mock limits: in-memory only (reload reseeds — read state resets); notifications are role-scoped, not per-person; seed is static (in-session edits don't append notifications); quick links land on the module route, not a campaign/line anchor. **S6 complete — next: S7–S8.**
- [ ] **Promo S5**: complete. `/reports` — read-only versioned department reports: role-gated Маркетинг/Закуп/Аналитика tabs (Select on mobile) + campaign picker; Appendix-C M/P/A field subsets (group-header table → Mode-B cards); «получено» + 17-кал.-дн. overdue marker + «История версий» (reuses `VersionHistoryDrawer`) + «Только изменённые» highlight (amber changed-cell ring + emerald added rows); ознакомление («Ознакомиться со всеми»/per-line, clears highlight, ≠ согласование §11.7); marketing-only editable «В рекламу (выбрано маркетингом)» + bulk-select + «Согласовать выбор» (Сотрудник маркетинга, §7.2) + §11.8 re-approval note. Mock limits: in-memory only (reload reseeds — marketing flags + acknowledgements + only-changed reset); the change-set/overdue are seed-stale (audit trail mocked — in-session edits don't append report versions); no per-person identity (any marketing-role user toggles the flag for anyone); «Согласовать выбор» + acknowledgements are toasts (notifications module is S6); seed: PR-2026-003 (gift type, overdue first send) + UN-2026-015 (on-time, one incremental correction — change/added-row demo). **S5 complete — next: S6–S8.**
- [ ] **Promo S4**: complete (Phases 1–3). Phase 1 — versioning model + three-view `VersionHistoryDrawer` + per-field diff. Phase 2 — edit-after-approval change tracking (baseline diff, amber cell highlight, «N изм. после согл.» badge, live draft section), «Ожидает повторного согласования маркетинга» (§11.8, additions skip it), Сотрудник маркетинга re-approve → КД «Согласовать и отправить» appending a live version (incremental send re-baselines/clears the draft), period change (§11.5 bold + ✏️). Phase 3 — **campaign cancellation** (КД only, required reason → separate `cancelled` state + «Отменена» + red/struck band + live «Отмена» version + "departments notified" toast), **line exclusion** (КМ request with reason → `removalPending` → КД approve/reject → `removed`/«Исключена из акции», dropped from validation; distinct from S3 `rejected`), **«Скрыть отменённое»** FilterBar `Switch` (ON by default; hides cancelled campaigns + removed lines), **deadline change** (§4.7: КД «Изменить дедлайн» with reason → `deadlineChange` pending chip → Операционный директор «Утвердить дедлайн» → `fillDeadlineOverride` effective, logged in the drawer). Mock limits: in-memory only (reload reseeds); no per-person identity (any КМ-role user requests removal on any line, КД/ОД act for anyone); departments/notifications are toasts (no S5/S6 module yet); deadline-change has only the single ОД approver (no further chain); seed: cancelled PR-2026-004 has lines (filter demo) + approved UN-2026-015 has a `removalPending` line (КД-approval demo). **S4 complete — next: S5–S8.**
- [ ] **Promo S3**: complete (Phases 1–3). Phase 3 added: full «Не участвует» lifecycle (КМ «Мои участия» raises → Старший КМ → КД; КД sets directly), live auto-escalation to КД after the 2-working-day breach («Авто-передано» tag, derived from the SLA — no timers), non-blocking просрочка note + history surfacing in `VersionHistoryDrawer`, per-campaign advance gate («Готовность кампании: N из M», can't advance until every КМ is final), mobile fixed action bar. Mock limits: in-memory only (reload reseeds the provider; direct deep-links to `/approvals/:id` reseed too — navigate via the queue/panel to keep in-session state); no per-person КМ identity, so the КМ «Мои участия» panel lists every (campaign, КМ) participation and КД can set «Не участвует» for any КМ; the sidebar «Согласование» badge reads the seed baseline (AppShell is above the provider), not in-session decisions — the queue itself updates
- [ ] **Promo S2**: complete (read-only skeleton + inline editing/validation + nomenclature entry & duplicate check + Excel import & 1С states + unplanned creation/integrate/edit + mobile per-line Sheet); installment values are an illustrative model (not exact bank formulas)
- [ ] **Promo S2**: edits/added/imported/created lines & campaigns live only in in-memory React state (no persistence — «Сохранить черновик»/«Отправить» are mock toasts; reload reseeds). No per-person КМ identity, so any КМ-role user edits all КМ lines and new/imported lines + created campaigns attach to a default/the campaign's existing КМ (mock simplification, same as S1). Прогноз продаж↔цена and цена↔скидка% are not auto-linked
- [ ] **Promo S2 Phase 5**: unplanned creation generates a № промо (UN-2026-1xx) and enforces ≥3 календарных дней before start; `firstSendDone` (тип/период lock after first send) is in-memory only and the deadline rule isn't re-enforced when editing an existing campaign; the mobile per-line Sheet is reachable at any width via the row chevron (shown below md)
- [ ] **Promo S2 Phase 3**: duplicate detection covers same-promo and overlapping-period cross-promo, but seed campaigns have no overlapping periods, so the cross-promo branch isn't reachable from the seed UI (same-promo path is). Per-line «история» (dup additions, `LineHistoryEntry`) is stored on the line but not surfaced in the version drawer — the S4 `VersionHistoryDrawer` shows campaign-level versions + the live edit-after-approval diff, not the per-line dup log
- [ ] **Promo S2 Phase 4**: «Excel» import is client-side CSV (no .xlsx parser dep); the 1С re-check always passes (the failed-row-with-reason path is represented by the existing rejected/reason marker, not a separate 1С-failure simulation); import is scoped to a chosen campaign (no № промо column in the file) and doesn't detect duplicates among rows within the same import batch
- [ ] **Promo**: «План акций» mode is a mock state machine (local state + toasts) — no persistence; plan-row creation adds to local state only
- [ ] **Promo**: overdue tags are computed against the real `new Date()`; seed PR-2026-007 exists so the OverdueTag path is visible at today's date

## Next Steps

1. Mobile responsiveness for ApplicationsPage list
2. API integration layer (replace mock data)
3. Cleanup: legacy `ApplicationDetailDrawer.tsx`, unused MUI/Emotion deps
4. Dark mode visual QA — **done for Promo (sub-project B, 2026-06-30)**; remaining: build + QA **Dashboard** dark mode (its `.dark` variables exist but the theme isn't built/verified)
5. i18n for Uzbek language support
6. **Texnomart Promo** screens from `docs/promo_prompt_pack.md` — S1–S6 + **S7** (Настройки типов промо) + **S8 complete** (Аудит-лог: filterable action log + per-campaign control-events timeline with overdue nodes + summary strip). **The Promo prompt pack S1–S8 is fully built.** Optional follow-ups: wire S7 rules into the full-calendar required-field validation; per-person identity + persistence so the audit log reflects live in-session actions
